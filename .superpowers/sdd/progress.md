# Employee Onboarding (Phase 1) — SDD Progress

Plan: docs/superpowers/plans/2026-06-30-onboarding.md
Spec: docs/superpowers/specs/2026-06-30-onboarding-design.md
Branch: feat/employee-onboarding
Verbatim doc content: docs/onboarding-templates/

- Task 1 (migration 024): complete (applied + recorded; Arabic doc-type names fixed via UTF-8 file)
- Task 2 (arabic helper): complete (commit 4ad774b, review clean after 2 fixes)
- Task 3 (offer-letter-pdf): complete (commit ec0f1bd, review clean; target words-form dropped intentionally — HTML had stale hardcoded words)
- Task 4 (nda-pdf): complete (commit ed6f6be, review clean after dept-suffix fix)
- Task 5 (asset-handover-pdf): complete (commit 5ecde5a, review clean; col-header truncation noted for T11 visual)
- Task 6 (HR helpers + /api/users DRY): complete (commit 664e48b, review clean)
- Task 7 (create/list route): complete (commit 77081b5, review clean after 4 fixes)
- Task 8 (detail/status/task/regenerate routes): complete (commit 173da9c, review clean after NDA-date fix)
- Task 9 (constants + types): complete (commit cfd9c99, tsc clean, self-verified)
- Task 10 (hooks + UI): complete (commit 4dc2d33, review clean after checklist-confirm fix)
- Task 11 (wiring + docs + MANDATORY live smoke): pending

---

# Remote Production Tracking — SDD Progress (2026-07-03)

Plan: docs/superpowers/plans/2026-07-03-remote-production-tracking.md
Spec: docs/superpowers/specs/2026-07-03-remote-production-tracking-design.md
Branch: main (established repo workflow — direct commits + auto-deploy)
BASE at start: aaa547e

- Task 1: complete (commit 80ea8fd, review clean; Minor noted: sendText silent-failure shape = accepted Phase 11 trade-off)
- Task 2: complete (commit 842c5c1, review clean; assignees WA at assignees/route.ts:114; board-members site correctly skipped)
- Task 3: complete (commit fc819cb, review clean; Minor: duplicated admin query in mutually-exclusive branches — accepted)
- Task 4: complete (commit b509caf, review clean; schema-existence check pre-verified against live DB)
- Task 5: complete (commit 41cf451, review clean; security lens passed — no manage/approve in BASE)
- Task 6: complete (commit 9de1b17, applied+recorded 033_production_board, review clean; checksum verified 64-char in DB; pg_dump gap -> spawned chip task_f71af325; DATABASE-SCHEMA.md boards drift noted for T14)
- Task 7: complete (commits b352fd5 + fix 0a00556, 9/9 tests; Important pairing bug fixed w/ RED evidence, re-review clean)
- Task 8: complete (commits 73705c2 + fix d1a0eff, review clean; plan defect fixed: work-days fallback -> DEFAULT_WORK_DAYS per CLAUDE.md lock; Minor v1.1 note: absent_days counts approved-leave days as absent — matches existing attendance-summary semantics)
- Task 9: complete (commit 29eb7f4, review clean, zero findings; own-scope security verified structurally)
- Task 10: complete (commits c4e99e9 + fix ec6ccd8; Important stale-ref chime-suppression bug found & fixed, re-review clean; Minors noted: newest-unread pick order, burst double-chime — low impact)
- Task 11: complete (commits a89e1a0 + fix 635a332; CRITICAL plan defect caught: UI was in dead TaskDetailDialog, ported to live TaskSheet; branches now structurally exclusive; drag intercept live; dead-code cleanup -> chip; pre-existing untoasted moveToColumn error noted for final review)
- Task 12: complete (commit 1adc9b1, review clean; Minor v1.1: unassigned-overdue tasks skip admin alert)
- NOTE: reordered — T14 docs + final review + push BEFORE T13 ops (n8n needs deployed endpoints)
- T14 docs: commit 9ef5a68 | Final whole-branch review (fable): 0 Critical, 2 Important -> fix wave a2fcf97 (8 fixes: scope guards via lib/auth/task-scope.ts, moveToColumn toast, docs wording, WA copy, dark pairs, WA self-skip, logError, month-scoped drilldown) | re-review: READY TO PUSH
- Latent v1.1 note: approve route (boards.manage) has no board-scope check — matters only if boards.manage ever granted via extra_permissions
- Task 13: complete (automatable part): PyraHR_Cron created+published (AeXwITpSmaZ5jg9V, daily 05:00 UTC deadline + */30 4-12 UTC attendance); scoped key perms extended (user-approved); PROD smoke: deadline 200 processed:0, attendance 200 reminded:2 (verified in pyra_notifications). HUMAN steps handed to Abdou: WA routing rows (needs phone numbers), schedule-times confirm, E2E dry run
- PUSHED to production: aaa547e..a2fcf97 (18 commits). ALL 14 TASKS COMPLETE.
- Post-launch wave 2 (realtime): migration 034 (publication was EMPTY since inception — root of all delay), useRealtimeBoardTasks + wired board, admin notifs on ALL pipeline moves (b920b15 users self-edit fix + d9a080d), review clean; follow-up noted: cross-board move leaves source board stale (pre-existing)
- Post-launch wave 2 complete: bell redesign b5b6301 + employee home panel e2c9a5e (review clean, pushed). All 5 user complaints addressed: realtime root fix (034), stage-move notifs, WA-to-admin (phone was null), bell UX, employee home.
- Post-launch audit wave (24 findings, self-verified): 3 fix commits pushed 9c5bce0(metrics)+aa9518e(security)+998617c(ux); adversarial review clean, approve-gate delivery trace confirmed no legit-path block. Deferred v1.1: cross-board journey(#7), drill-down avg consistency(#8), markRead race(#17), shared assigneeSearch(#20), midnight clock(#21), moveToColumn UX(#22), evening-shift cron(#24), column-reorder invariant(#4), duplicate stale completion(#3)

---

# Bilingual i18n Phase 0+1 — SDD Progress (2026-07-05)

Plan: docs/superpowers/plans/2026-07-05-i18n-phase-0-1.md
Spec: docs/superpowers/specs/2026-07-05-bilingual-i18n-design.md
Branch: main (established repo workflow — direct commits; push at Tasks 10 + 19)
BASE at start: 48b346f
- Task 1: complete (commit ade33fa, review clean; Minors for final review: dynamic-import context fragility note in loadLocaleFiles, shallow cross-file namespace merge relies on one-namespace-per-file discipline)
- Task 2: complete (commit 1f7ca4a, review clean; backup skipped by controller adjudication — tier-1 additive + known pg_dump/SUPABASE_DB_URL gap; DB state independently verified by controller: both columns + migration record present)
- Task 3: complete (commit 1875b90; review Needs-fixes on MISSING RUNTIME EVIDENCE only — code itself clean; controller ran live verification: no-cookie=ar/rtl, en-cookie=en/ltr both confirmed on :3003; evidence appended to task-3-report.md; stale orphan dev server on :3000 killed; Minor noted: scroll-area inline ternary duplicates dirFor)
- Task 4: complete (commit 6aa0237, review clean; Minors: client document.cookie writes lack conditional secure flag (fix folded into T7 scope), LocaleSync ref-across-refresh reasoning unevidenced (T10 browser QA covers); browser click-through deferred to T10 QA)
- Task 5: complete (commit 499ef4d, review clean, zero findings of substance; success-path cookie observation deferred to T10 browser QA)
- Task 6: complete (commit bc9a711, review clean; Minors folded into T7: LocaleSelect label htmlFor pairing; also T4's client-cookie secure flag)
- Task 7: complete (commit ba250ec, review clean; Fix A secure-cookie + Fix B label/useId both verified in; Minor: portal ProfileForm LocaleSelect sits alone in a 2-col grid row — cosmetic, final-review triage)
- Task 8: complete (commit 7718ed9, review clean; 266/266 tests; Minors: DUE_LABELS doc example ar-only, no invalid-locale hardening test)
- Task 9: complete (commit dc9796e, review clean — reviewer independently reproduced all 3 live-run proofs; Minors: findOrphanKeys array-branch unexercised, collectSourceFiles silent on typo'd manifest path)
- Task 10: complete (PHASE 0 SHIPPED — pushed dc9796e, Coolify deployed, production smoke 4/4: dashboard+portal x ar-default+en-cookie; automated QA incl. bogus-cookie fallback; REMAINING for Abdou (non-blocking): authed browser QA — switcher click round-trip, profile selects, toasts/dark-mode in LTR)
- Task 11: complete (commit f74e9f6, review clean; parity 29/29 first-run; Minor: 3 parallel hardcoded entity lists (JSON/union/test) — future tie-in candidate)
- Task 12: complete (commit a536ada, review clean — programmatic deep-equality verification; note: T12 reached production unreviewed via hotfix push, review confirmed clean post-hoc)
- INCIDENT (2026-07-05): production 500 on PATCH /api/profile reported by Abdou clicking the new LocaleSwitcher. ROOT CAUSE: PRE-EXISTING bug — route wrote non-existent pyra_users.updated_at (42703) since before i18n; every self-service profile edit was silently broken. HOTFIX 484a383 (line removed + apiServerError err/request observability) pushed. Lesson: T4/T6 evidence gap — authed PATCH was never runtime-exercised pre-ship.
- Task 13: complete (commit 1a33b0d, review clean; 71 items byte-parity verified programmatically; BONUS FIX: mobile-nav was pre-existing stale (16 missing items + bogus dup CRM group w/ wrong perms) — unified as strict superset, permission-filter parity confirmed identical; plan doc typo: '64 items' should read 71)
- Task 14: complete (commit 82d5281, review clean; 49/49 legacy segment keys verified in catalog; Minors: dead cn import pre-existing, role-line ar-fallback asymmetry inherited from spec)
- Task 15: complete (commit 9c7adc7, review clean; palette-specific AR labels preserved vs nav.items unification traps; recents now path-keyed; Minors: dirFor() not reused inline ternary, pre-existing financeHome/financeAlerts path collision documented)
- Task 16: complete (commit fc3f296, review clean; both special patterns verified against real callers; unreadAria wiring adjudicated as completing a T12-specified key, non-regressive; dead components/error-boundary.tsx chip already pending task_3aafb9af)
- Task 17: complete (commit dfd6007, review clean; error-detect conditions byte-identical; raw Supabase error passthrough eliminated; live curl proof AR/EN flip)
- Task 18: complete (commit 2469aa9, review clean; 3 named-risk checks verified incl. auth-route byte-identity; 4 new auth.api keys AR-verbatim; route-handler t() pattern established + live-proven)
- Task 19: complete (commit 317fa96 + final-review hardenings 6704342; manifest caught+fixed a leftover literal in portal session route)
- FINAL whole-branch review (fable): READY TO MERGE, zero Critical. Important #2+#3 fixed pre-push (loud-fail manifest, i18n:check wired into pnpm check); Important #1 (move-route awaited WA sends, non-i18n commit 85db6a3) -> chip task_b73f2afc. Payload measured: ~5-6KB gz vs 150KB threshold.
- PHASE 1 SHIPPED: pushed 6704342, deployed, production smoke green (pages AR/EN both surfaces + API getTranslations pattern GET+POST; initial AR-only API reads were a container-swap transient, re-verified clean).
- Phase 2 planning notes from final review: first-login cookie seeding decision, module-guide migration priority, per-phase payload re-measure, backlog: dirFor reuse, namespace-disjoint test, entity-list tie-in, financeAlerts anchor, breadcrumb dead cn import.

---

# i18n Phase 2 — Work Core — SDD Progress (2026-07-05)

Plan: docs/superpowers/plans/2026-07-05-i18n-phase-2.md
Discovery: .superpowers/sdd/p2-discovery/*.txt (6 inventories, string-verbatim)
Branch: main. BASE at start: <plan-commit>
Coordination: chip task_b73f2afc (move-route WA) running in separate session — Task 8 last + pull-first.
- P2 SUITE-COUNT CORRECTION (2026-07-06): .claude/worktrees checkouts were being scanned by vitest+tsc — ALL historical test counts (259/271/300) were worktree-inflated; TRUE suite = 16 files/130 tests. Fixed in 00ca9e7 (vitest exclude + tsconfig exclude). No verdicts affected (all runs were passing).
- P2-T1: implementer DONE (commit fa723e0); review in flight
- P2-T1: complete (commit fa723e0, review clean; reviewer independently reran suite 16/130 + byte-diffed api.common vs census)
- P2-T2: complete (commit 319b003, review clean; ICU runtime empirically verified via createTranslator vitest — adopted as STANDARD METHOD for future ICU-unverified flags)
- P2-T3: complete (commit d86d5e1, review clean; both judgment calls adjudicated sound; dubaiDayKey semantics verified)
- P2-T4: complete (commit 28f3211, review clean; dayNames-as-object deviation REPRODUCED+justified (array poisons next-intl type inference — LESSON for all future catalogs: no JSON arrays in namespaces, keyed objects only); MyWorkInbox type-narrowing verified behavior-preserving; Minors for T9: normalize redundant 'as Locale' casts x4, translator-shadowing readability in calendar route
- P2-T5: complete (commit 8ce4ced, review clean, zero findings)
- P2-T6: complete (commits 06c725e deletion 1031 lines byte-exact + 53607a7 migration, review clean; drag-lock keywords ZERO hits; file 2540->1509; Minors: gatedColumnBlocked dual-maintenance note, PipelineView t-shadowing rename candidate)
- P2-T7: complete (commit 9782803, review clean; cross-file hunk = 1-line signature threading, drag-filter zero hits; first t.rich() verified; Minor: AR plural-completeness pass = future effort-wide item)
- P2-T8: complete (commit 32f0a64, review clean; zero logic drift audited across 18 files; hazard doc exact; census reconciled)
- P2-T9: complete (commit cdca09a + fast-follow cdc00ee: dubaiDayKey overdue fix + plan annotation)
- FINAL whole-branch review (fable): READY, zero Critical; drag integrity byte-proven at every anchor; catalog parity 894/894 keys 8/8 namespaces; payload 11.7KB gz AR vs 150KB threshold; Important #1 (pre-existing UTC day-key in board-view) FIXED pre-push; Minors triaged (as-Locale sweep x10, t-shadowing renames, gatedColumnBlocked sync-note, AR-plural-completeness = coordinated future pass needing sign-off, api.json one/other dup)
- PHASE 2 SHIPPED (2026-07-06): pushed cdc00ee (13 commits), deployed, verified live via catalog markers in RSC payload + html lang/dir smoke. REMAINING for Abdou (important): live drag QA in BOTH locales on a real board + task-sheet pipeline buttons + calendar views — the plan's closure gate item that automation cannot cover (all surfaces auth-gated).

---

# i18n Phase 3 — CRM — SDD Progress (2026-07-06)

Plan: docs/superpowers/plans/2026-07-06-i18n-phase-3.md
Discovery: .superpowers/sdd/p3-discovery/*.txt
Key locked decisions: pipeline scroller dir → locale-conditional (sanctioned by pointerWithin direction-agnosticism, MANDATORY live EN drag QA at closure); LOST_REASON single-array localized labels; funnel drops label_ar (client accessor); insights server renders per-request locale (message + legacy message_ar); PIPELINE_STAGE_LABELS_AR stays in lib/constants (DB-write source); new entities leadPriority/insightSeverity/crmContract/milestone.
- P3-T1: complete (commit 98e73b9, review clean; all values byte-verified vs live code)
- P3-T2: complete (commit 316afb7 + cast-fix d0dc025, review clean; insight server-locale dual-field shipped; Set-guard proven semantically identical)
- P3-T3: complete (commit f0e9ee0, review clean; drag filter zero-hit reproduced; toasts control flow byte-identical; chip-identity coupling sound)
- P3-T4: complete (commit 41871a6, review clean; bonus: latent filter-reset-on-locale-switch bug fixed via key/label split; 418/418 key parity; reassign-modal scope pull adjudicated sound)
- P3-T5: complete (commit 255341e, review clean, zero findings; Object.values canonical-iteration deviation adjudicated better-than-literal)
- P3-T6: complete (commit c22db39 + fix e153864; review found REAL Important: CLDR few = n mod 100 diverged from original flat threshold at 109+ months — fixed the SIMPLEST way per Abdou's directive: 4 flat keys + threshold back in code (no plural machinery to mis-evaluate); catalog diff noise = pure json reflow; LESSON: never pipe pnpm build through tail in a && chain — masks exit code)
- P3-T7: complete (commit b8d0b56, review clean; zero logic drift across 19 routes; splice fix verified; Minor: report key-table undercount — docs only)

P3-T8 final-review fix: complete (commit 265a3b1 after rebase — stage names locale-aware in pipeline-column/board/mobile-sheet/lead-header + dead .toUpperCase() fallback removed + label_ar deprecated). Drag-keyword filter ZERO HITS. check+130 tests+build clean. Phase 3 pushed to origin/main (12 commits, 4834583..265a3b1).

P4-T1: complete (commits 1430520..76a2416, review clean — spec ✅, quality Approved; controller-recovered after process death; reviewer independently re-verified everything). Residual risk noted: gap-key VALUES (in_progress/draft/contractType/periodCycle) have no legacy pin — reviewer manually verified all against live sources.
Side-quest (user chip): formatCurrency currency-threading landed ac51b65 (statement API + 4 components; incl. contract_value toAED conversion fix). P4-T2/T5 implementers must PRESERVE the explicit currency args in client-statement/* and invoice-new/*.

Phase-3 production deploy CONFIRMED live (Coolify Success 10:18:31 UTC, commit 265a3b1, 4m33s). Lesson: CSS-hash/login-chunk markers are INSENSITIVE to behind-auth page changes — use Coolify status or a changed-page chunk as deploy marker. Locale smoke on prod: ar/rtl default + en/ltr cookie both correct.

P4-T2: complete (commits ac51b65..7fc87aa, review clean — spec ✅, quality Approved; 154/154 key parity; milestoneTypes deduped; pagination reuse; money byte-preserved incl. ac51b65 args). Minor (accepted): dialog-local save/cancel judgment call; deleteFailedCount grammar quirk preserved byte-identically.

P4-T3: complete (commits 7fc87aa..5793aed, review clean — spec ✅, quality Approved; 137/137 parity; signed-drift fixed to canonical موقّع; sendResult trio deduped; CURRENCIES keyed-object w/ byte-identical AR; 24-string fidelity sample 0 mismatches). Minor logged for T9: dead key finance.quotes.new.meta.title (no generateMetadata on new page) — prune or wire at closure.

P4-T4: complete (commits 5793aed..13b8910 + fix 4c61134, review: spec ✅ except same-line exempt tags — Critical fixed inline by controller, scanner-logic verified SCANNER-CLEAN on both files; 3 drift conversions verified canonical; 254/254 parity; 23-string fidelity sample clean). Minor logged for T9: expenses-client 388 lines >300 (pre-existing); server-message passthrough toasts depend on T7.

P4-T5: complete (commits 4c61134..51cdbcd, review clean — spec ✅, quality Approved; PO vocabulary functional fix verified against DB enum; periodCycle ×6 dedup; recurring/new dirFor fix; ar-EG parametrized in place; Recharts threading correct; ac51b65 currency args byte-preserved; implementer self-caught formatDate signature footgun). v1.1 notes: recurring-status + supplier active/inactive stay page-scoped keys.

P4-T6: complete (commits 51cdbcd..32fd2b7 + fix 1d0dd72, review: spec ✅ except one same-line exempt tag (Important) — fixed inline by controller, SCANNER-CLEAN verified; server AR labels runtime-verified byte-identical; getLocale default 'ar' for uncookied callers; 42-string fidelity 0 mismatches; 93/93 parity). NOTE FOR T7: app/api/finance/dashboard/route.ts:241 'غير مصنف' fallback is UNTAGGED — T7 must exempt-tag or migrate it. Same-line exempt-tag is a RECURRING implementer blind spot (T4+T6) — brief T7/T8 explicitly.

P4-T7: complete (commits 1d0dd72..2342a5f, review clean — spec ✅, quality Approved; 106/106 (a)-strings, 41/41 files reconciled, reviewer-independent scanner run 0 dirty lines, 51-sample fidelity, 8 ICU keys byte-verified, zero logic drift incl. flip-and-warn/rollback/terminal-transition paths; 233/233 api.json parity). Minor: report doc nit re noValidFieldsToUpdate reuse listing.

P4-T8: complete (commits 2342a5f..04349ac, review clean — spec ✅, quality Approved; 57/57 activityLabels byte-identical; 10 reused keys verified no drift; ar-SA hero preserved; scanner-clean pre-verified). Note: near-duplicate ACTION_LABELS in app/dashboard/activity/activity-client.tsx → Phase 6 scope (admin tail).

P4-T9: complete (closure commit 4989462 + final-review fix 3f88fbd). Full-manifest scan: 19 stragglers fixed (2 doc-comment tags, aging bilingual payload tags, hook JSDoc→English). Payload 36.3KB gz AR / 32.1KB gz EN vs 150KB. FINAL WHOLE-BRANCH REVIEW: YES WITH FIXES — Important #1 (4 missing T5 subcomponent manifest dirs) FIXED in 3f88fbd; Minor #2 dead key finance.quotes.new.meta.title → Phase 5 backlog; #3 cross-module AR value duplication = established convention, no action. All 8 cross-task checks confirmed (catalog 1046/1046 parity, 21 accessor sites consistent, money invariance verified range-wide, server DEFAULT_LOCALE ar for uncookied, zero out-of-plan files except benign task-sheet tag). PHASE 4 PUSHED: 265a3b1..3f88fbd (13 commits).

P5-T1: complete (commits be67150..6092c3b, review clean — spec ✅, quality Approved; reviewer independently re-verified all 9 entities byte-identical, paymentSourceType no-drift re-grepped, evaluation orphan confirmed, 45/45+324/324 parity, suite green). CENSUS GAP found by reviewer: app/api/dashboard/timesheet-periods/** ABSENT from p5-hr-apis.txt — T9 brief MUST add it to scope. Deferred to T9/T10: cross-module near-dup families (noFieldsToUpdate/entityNotFound/actionBlockedByStatus).

P5-T2: complete (commits 6092c3b..b79c60c, review clean — spec ✅ (one Low: 2 extra DEFENSIVE dir props on timesheet dialogs, undisclosed but harmless — recorded here for audit: '2 required dir fixes executed as 4 defensive dir props across the 2 Dialog-bearing files'); 30+ fidelity samples byte-identical; dayNames reuse verified; clock tick shape unchanged; weekend fallback untouched; scanner independently re-run clean; 114/114 parity).

P5-T3: complete (commits b79c60c..685912f + separator tag 643a611, review clean — spec ✅, quality Approved; leave-type NAME comparisons byte-verified untouched ×3; /api/approvals/team additive-only (type_name) with consumer-graph check; colloquial verbatim; t.rich date-ranges runtime-proven; bundled fixes #1 raw-'Annual' + #2 raw dates). MyWorkInbox same-bug chip pending (task_8a8e0dab). leaveType lowercase-keys root cause = documented display gap, backlog.

P5-T4: complete (commits ..ba81fc2, review clean — spec ✅, quality Approved; 21 money sites disciplined; mixed-entity map split verified per-site; paymentSourceType union re-verified; CREATABLE restriction preserved; 388/388 parity). User-visible by design: pending badge معلق→بانتظار الموافقة on 2 surfaces (QA flag T10).

P5-T5: complete (commits ba81fc2..7a3a4d0, review clean — spec ✅, quality Approved; evaluationForm/evaluationPeriod wiring verified per-site; orphaned evaluation entity untouched; +2 census-flagged entities (evaluationType, evaluationCriteriaCategory); 11 bilingual DB-row sites traced individually; 6 dir fixes = census H5 sanctioned; category bug converged structurally; 25-sample fidelity clean). Deferred note: periods.dateRange raw ISO strings (pre-existing, T10 discussion).

PHASE 5 PARTIAL PUSH (user-directed rest point): be67150..7a3a4d0 pushed to origin/main — plan+censuses, 5.1 foundation, 5.2 attendance+timesheet, 5.3 leave (+separator tag), 5.4 payroll, 5.5 evaluations. All 5 tasks reviewed clean. Safe intermediate state (D4/D7). T6 implementer IN FLIGHT (uncommitted working tree — HR overview + deriveAlerts); on its completion: hold for user's كمل, then review→T7→T8→T9→T10 closure remain. MyWorkInbox leave-type chip (task_8a8e0dab) running in a separate user session.

---
## Phase 5 RESUMPTION (2026-07-09) — completing T7–T10 on branch integrate-pending-fixes (== origin/main 789f142)
Context: Phase 5.1–5.6 already shipped to prod (b79c60c..5398ec6, folded into the pending-fixes merges). BASE for T7–T10 = 789f142. Pending-fixes merges verified NON-regressive to i18n (11 catalogs intact, parity OK).
- P5-T6 review: FOLDED into the T10 whole-branch review (T6 already committed 5398ec6 + shipped; whole-branch final review covers it).
- P5-T7: first implementer (background) DIED mid-file (connection dropped ~70%: API JOIN+types+catalogs+7 files done; work-schedules trio + my-documents-client + UploadDialog + UserDocumentsTab NOT done). Recovered: verified partial tree (tsc = 2 expected errors in unmigrated EXPIRY_BADGE consumers), dispatched a scoped completion agent (da4f3d9, DONE_WITH_CONCERNS) → completed the 6 files. Agent flagged DocumentRowActions.tsx was left fully hardcoded (dead agent added hook imports only) → controller hand-fixed it (wired to existing hr.documents.rowActions keys + bilingual picker + loop-var rename dt) → tsc/eslint clean → amended into commit 1d68c82. T7 = commit 1d68c82, 20 files, AR/EN parity 780=780, 138 tests/check/build green. Task review IN FLIGHT (fable).
- P5-T7: COMPLETE (commit 1d68c82, review APPROVED by sonnet — spec ✅, quality Approved, ZERO Critical/Important; reviewer independently re-ran tsc/i18n-check/138 tests/eslint on all 18 files + cross-checked all 148 t() calls vs hr.json). Minor #1: myDocuments.alertBanner one/other (byte-identical to original ternary; full 6-CLDR-category = documented AR-plural-completeness backlog, needs Abdou sign-off since it CHANGES AR for counts 2-99 → NOT fixed). Minor #2: stale report describes pre-amend da4f3d9 (cosmetic; 1d68c82 is correct). BASE for T8 = 1d68c82.
- P5-T8: implementer DONE (fbea9ee) → controller fixed concern #1 (checklist done_at: plan said formatDate but that breaks AR byte-identity — date-fns 'dd-MM-yyyy' = Latin/dashes vs original toLocaleDateString('ar-AE') Arabic-Indic/slashes; reverted to in-place toLocaleDateString(locale==='ar'?'ar-AE':'en-GB') per Phase-2 lock + removed unused import) → amended to 477085f. BASE for T9 = 477085f. Other concerns: #2 ASSET_TYPES_AR kept as persisted array + parallel key array for dropdown (PDF-bound, reasonable — reviewer confirming); #3 census miscounted generateMetadata (2 not 3, informational); #4 useAppLocale() doesn't exist → agent correctly used useLocale() (my brief had wrong ref; T9 is server-side so N/A). Verified: compensation numberLocale=ar-AE (AR byte-identical); started/completed formatDate numeric-pattern locale-invariant. Review IN FLIGHT (sonnet).
- P5-T7: implementer agent reported "failed (connection closed mid-response)" BUT that was a late drop AFTER commit+report. VERIFIED SOUND by controller spot-check on 1d68c82: type_name JOIN landed in /api/hr/documents:127 + /api/my-documents:80 (additive); PyraEmployeeDocument.type_name? widened (types/database.ts:281); EXPIRY_BADGE reduced to className-only (labelAr dropped); hr.json parity documents96/myDocuments9/workSchedules44; T7 report exists (13.5KB); T8 reviewer confirmed tsc+i18n:check+138 tests pass on 477085f (T7 included). T7 formal review FOLDED into T10 whole-branch review (full-range diff). Commit 1d68c82.
- P5-T8: review CLEAN (sonnet, 890s): spec ✅ all 9 constraints verified against live files (190 removed-AR-line cross-ref = 0 unexplained mismatch; AR/EN parity 0 diff; onboarding/employmentType/workLocation accessors wired + 3 local maps deleted; validateStep return-key + {min}; ASSET_TYPES_AR persisted-array justification confirmed real via asset-handover-pdf.ts:288; 12 exempt tags same-line; checklist-date fix VERIFIED correct; no logic drift). Quality Approved, 0 Critical/Important. Minors: onboarding-client:99 formatDate unparam (verified zero visual impact — numeric pattern locale-invariant); good-practice t-shadow renames. Commit 477085f. BASE for T9.
- P5-T9: implementer DONE (3397e77) — 168 (a) response messages migrated across 35 route files + 3 libs, timesheet-periods census-gap closed, 61 new api keys, 384/384 parity, leave-NAME untouched, zero logic drift (all independently verified by reviewer). Review: spec ✅ / quality Needs-work → 7 punch-list findings (all string/comment-only). CONTROLLER FIXED all 7: (1 Critical) 3× Arabic-noun params → nested t() + 6 new catalog scopeNoun keys (leave.scopeNounRequests/timesheet.scopeNounEntries/scopeNounPeriods, ar+en); (2 Imp) payroll/[id]:203 name_ar DB-seed exempt tag; (3 Min) payroll:80 Arabic-comma exempt tag; (4 Imp) hr/productivity/export ~40 CSV/PDF lines exempt-tagged (Phase 9 scope — UNBLOCKS app/api/hr manifest entry at T10); (5 Imp) leave-balances:26 comment exempt; (6 Imp) approvers.ts:14-15 JSDoc exempt. Re-verified: check ✓ (tsc+i18n:check), eslint 0-err, 138 tests ✓, build ✓; re-scan HR-API = 0 untagged Arabic string-literals (69 exempt-tagged). Amended commit. BASE for T10.
- P5-T9 fixes verified: check ✓ (tsc+i18n:check), eslint 0-err, 138 tests ✓, build ✓ (337 pages), HR-API re-scan 0 untagged. Amended 0c51f97.
- P5-T10 closure: manifest += 33 Phase-5 HR paths (all dirs verified exist; app/dashboard/users + directory EXCLUDED — shadow maps). i18n:check with full manifest EXPOSED 2 T5-missed evaluations name_ar example placeholders (evaluations-client:1082 مراجعة الربع الأول 2025, evaluations-settings-client:246 المهارات التقنية) → exempt-tagged (guard did its job). Re-ran: check ✓ + build ✓. Closure commit 65df3a8. Phase-5 (T7-T10) range 789f142..65df3a8 = 4 commits. Whole-branch review dispatched on opus (covers unreviewed-T7 + T8/T9/fixes + T10). NOT pushed yet (awaiting review).
- P5-T10 CLOSED + PHASE 5 SHIPPED (2026-07-09): whole-branch review (opus) = READY TO MERGE, zero blocking; independent gates check✓/138 tests✓/i18n:check✓; hr.json 945/945, api.json 387/387 parity; AR byte-identity 40+ verified; T7(unreviewed) scrutinized clean. Pushed origin/main 789f142→65df3a8 (+ CLAUDE.md doc). Coolify auto-deploy triggered. CLAUDE.md → "Phases 0–5 shipped"; memory updated. Payload AR 51.7/EN 46KB gz. Non-blocking nit: approvers.ts:14-15 JSDoc exempt marker style (cosmetic). NEXT: Phase 6 (admin tail + module-guide 641 lines) → 7 portal → 8 notifications templates → 9 outbound docs.

# i18n Phase 6 — Admin Tail — SDD Progress (2026-07-09)
Plan: docs/superpowers/plans/2026-07-09-i18n-phase-6.md (5 sub-waves 6a-6e). Discovery: .superpowers/sdd/p6-discovery/*.txt (6 censuses, ~4700 strings / ~300 files). BASE_6a=cbf81ca.
Sub-wave map: 6a admin/rbac/settings (~803+rbac) · 6b module-guide (1131) · 6c clients/projects/files/teams (956, +projectStatus entity) · 6d content-ops (478) · 6e misc+WhatsApp-chat (1319). Each ships+deploys independently.
- P6a-T1: foundation IN FLIGHT (sonnet) — 4 ns (settings/admin/users/rbac) + errorSeverity/errorEnvironment/resolvedState entities + rbac array→keyed catalog+resolver + common.all + api dedup + reconciliation decisions. Brief scratchpad/p6a-t1-brief.md.
- P6a-T1: review CLEAN (sonnet) — spec ✅ / quality Approved. Independent: check✓, 138 tests✓, rbac 156/156 byte-identity (40 modules+116 perms), 0 arrays, shadow maps ZERO diff (untouched as required), no out-of-scope. Commit 5d80fe3. FORWARD NOTES for 6a-4/6a-5: (a) accountType entity needed for ROLE_LABELS — conflicts to reconcile: sales_agent «مبيعات»(users-client:390)/«وكيل مبيعات»(ROLE_LABELS), AND admin «مسؤول»(users-client:390 badge)/«مدير»(user-detail:98) [reviewer-caught, add to table]; employee «موظف» matches. (b) rbac.permissions = 49 prefix groups/116 leaves. Consumer swap (users/roles/profile clients, 6 lines) was build-forced + verified minimal.
- P6a-T2: settings IN FLIGHT (sonnet). BASE 5d80fe3.
- P6a-T2: settings DONE (e6a9564). Controller-verified: check✓/138 tests✓/build✓; settings.* 300/300 + api.settings.* 16/16 parity; completeness scan settings scope = 0 untagged Arabic (11 files); formatDate:728 shadow fixed IN PLACE (ar-SA kept for AR byte-identical, en-GB for EN). Accepted w/o separate subagent review (standard migration, all gates green — closure whole-branch review is the deep net). BASE for T3.
- P6a-T3: admin/error-logs IN FLIGHT (sonnet).
- P6a-T3: admin/error-logs DONE (fa82798). Controller-verified: check✓/138✓/build✓; admin.json 36 parity + api.admin/observability; admin scope completeness scan 0 untagged (5 files); resolvedState convergence limited to filter chip (badges already masc; empty-state kept fem grammatically); Phase-14.1 leak removed; observability beacon migrated. rtl chevron deferred to 6a-4 per plan. Accepted (standard-risk). BASE for T4.
- P6a-T4: users/directory/org-chart IN FLIGHT (sonnet) — shadow-map reconciliation + accountType entity → FULL review after.
- P6a-T4: review ✅ PASS / Approved (sonnet). Independent: check✓/138✓/eslint no-new-warnings; all 5 entity key-sets match shadow maps (paymentType[salary-struct] vs paymentSourceType[SOURCE_LABELS] split JUSTIFIED — share only 'commission'); accountType canonical «مسؤول»/«موظف»/«وكيل مبيعات» validated vs lib/api/auth.ts convention (admin fallback already «مسؤول»); 6 shadow maps grep-zero; users.json 151/151; locale-conditional DB rows correct; rtl chevron + localeCompare fixed. Findings = REPORT-ACCURACY ONLY (code correct): (1 Imp) Create-user dialog has NO employment/location/payment fields — only Edit does; NO i18n gap, but if those fields added to Create later they need i18n (durable note). (2 Min) workLocation.remote canonical «عن بُعد» (Phase-5 diacritized) trivially converges shadow «عن بعد» — left as canonical (Phase-5 consistency). Commit 1bc6d74. Accepted, no code fix. BASE for T5.
- P6a-T5: roles/permissions + rbac resolver wiring IN FLIGHT (sonnet).
- P6a-T5: roles/permissions DONE (8102644 → amended 59414e3). Verification: check✓/138✓/build✓/eslint 0; parity; resolver wiring already complete from T1 (verified). CONTROLLER FIX: implementer authored full CLDR 6-category AR plurals (first in migration) diverging from originals — originals were INVARIANT singular «# مستخدم»/«# صلاحية». Collapsed 4 AR keys (card.memberCount/permissionCount/permissionsSelected/warningWithMembers) → single `other` matching originals byte-identically; EN keeps one/other. Blind first-match replace initially CORRUPTED directory.memberCount («# عضو في الفريق»→«# مستخدم») + missed card.memberCount — caught via duplicate-key check, fixed both by line number. 0 six-category remain; roles/perms scope 0 untagged. Amended. BASE for T6.
- P6a-T6: users/roles API IN FLIGHT (sonnet).
- P6a-T6: users/roles API DONE (c31160f → amended 27e0eed). 72 (a) strings, 47 api.users+7 api.roles keys, parity. validateExtraPermissions t-param added (only 2 callers, both pass t; required OK). CONTROLLER FIX: 4 notification lines (users/[username]:412/414/602/604) had exempt tags on the LINE ABOVE (recurring same-line blind spot) — moved same-line. Re-scan 0 untagged (6 exempt). check✓/build✓. Amended. BASE for T7.
- P6a-T7 closure: manifest += 13 Phase-6a dir paths (dashboard settings/admin/users/directory/org-chart/roles/permissions + components/settings + api settings/admin/observability/users/roles). EXCLUDED lib/auth/rbac.ts + lib/i18n/rbac-labels.ts — Arabic is 100% CODE COMMENTS (labels moved to rbac.json); englishifying 46 comments in a 945-line critical file = disproportionate. i18n:check CLEAN with 13 paths; build✓. Closure commit 011db28. 6a range cbf81ca..011db28 (7 commits). Whole-branch review dispatched (sonnet). NOT pushed (awaiting review).
- P6a-T7 review (sonnet whole-branch): YES WITH FIXES — zero Critical/Important; check✓/138✓; all cross-task verified (catalog coherence settings300/admin36/users234/rbac156/api464/statuses246; rbac 156/156 byte-identical; 6 shadow maps grep-zero; plural fix correct; no logic drift; manifest sound). 3 Minor: (1 FIXED) admin subtitle «غير محلولة»→«غير محلول» to match converged chip; (2 FIXED) users route cross-ns t('tasks.noDataToUpdate')→added api.users.noDataToUpdate + repoint; (3 SKIPPED — cosmetic, output-identical) mixed other{X} vs one{X}other{X} invariant style. Amended closure 2e5fadc.
- ★ SUB-WAVE 6a SHIPPED (2026-07-09): pushed origin/main 4ebe166→2e5fadc (8 commits). Coolify deploying. Payload AR 63.5/EN 56.6KB gz. Admin/RBAC/Settings 100% bilingual. NEXT: 6b module-guide (1131 strings) → 6c clients/projects → 6d content-ops → 6e misc+chat → Phases 7/8/9.

# i18n Phase 6b — module-guide — SDD Progress (2026-07-09)
- P6b-T1: DONE (fe6d826). guide.json namespace (78 entries, keyed-object, tips/keywords index-keyed NO arrays); AR verbatim + EN fully authored (descriptions from descriptionEn seed, goal/tips/keywords translated); module-guide.ts stripped to structural + routePath↔slug; resolver + wired page-guide/guide-page/sidebar/mobile-nav/module-card. Controller spot-check: 0 arrays, parity 1215/1215, ~47.4KB gz. Implementer ALSO: (a) added 6b manifest paths (lib/config/module-guide.ts + app/dashboard/guide + components/dashboard/guide) — i18n:check passed WITH them; (b) ENHANCED scripts/i18n-check.ts with an orphan-key guard (every EN key must exist in AR — D7-aligned). Concerns: keyword EN-augmentation mirrored into AR at new indices (else orphan-guard fails — search corpus, acceptable); DROPPED always-visible descriptionEn/titleEn dual-captions → locale-conditional (UX change, flag for Abdou); no live browser verify (auth-gated). 6b = 2-task wave → closure review IS T1 review. Whole-branch review dispatched (sonnet).
- P6b review (sonnet whole-branch): READY TO MERGE — zero Critical/Important. Exhaustive: 78/78 AR byte-identity, EN quality good, keyword-mirror correct (originals preserved), consumers rewired (none missed), 0 arrays, parity 1215/1215, i18n:check clean, manifest correct. Findings: (Min1 CORRECTION) orphan-key guard is PRE-EXISTING (Phase 0.9 dc9796e), NOT added by 6b — 6b only added 3 MIGRATED_PATHS lines; (Min2 FIXED) EN work_schedules.goal dropped (start_time) term → restored; (Min3 v1.1) sidebar/mobile-nav call useAllModuleGuides unmemoized (perf); QA-for-Abdou: dropped always-visible descriptionEn/titleEn dual-captions → locale-conditional (UX change).
- ★ MERGE EVENT: while 6b was under review, USER pushed "feat(hr): clearer productivity report" (a53db63) to origin/main on top of my 6b commit fe6d826 (both my 6b + user productivity now on origin). My --amend of the Minor-2 fix had landed on the productivity commit (5be03f1, divergent sibling of a53db63, delta = 1 line). RECOVERED cleanly: soft-reset onto a53db63 (adopts origin canonical, user productivity preserved), re-committed Minor-2 as 8560f62, fast-forward push. NO data lost, NO force-push. LESSON: user is actively committing in this repo — FETCH before every push (was doing); the productivity commit modified hr.json+productivity-client (user's own i18n-adjacent work, now integrated).
- ★ SUB-WAVE 6b SHIPPED (2026-07-09): origin/main a53db63→8560f62. module-guide 78 entries bilingual. Coolify deploying. NEXT: 6c clients/projects/files/teams (956, +projectStatus) → 6d content-ops → 6e misc+chat → Phases 7/8/9.

# i18n Phase 6c — clients/projects/files/teams — SDD Progress (2026-07-09)
- P6c-T1: foundation DONE (be6ed04, committed NOT pushed — scaffolding only, ships with the rest of 6c). Verified by implementer: check✓ (incl. orphan-guard), 138 tests✓, build✓ 337 pages, parity statuses 265/265 + api 473/473, AR byte-identity 28/28. Built: clients/projects/files/teams namespaces registered; NEW entities projectStatus (5 keys, zero conflict, matches server validStatuses), fileCategory (9 keys, image plural/singular reconciled→plural), clientSource (5 keys incl. export-only 'import' as display-only); api.{clients,projects,files,teams} dedup seed (9 keys, zero api.common cross-match). RECORDED (not wired) decision tables in scratchpad/p6c-t1-report.md: invoice/quote shadow drift (missing partially_paid, dead 'accepted'), client active/inactive «معطل»→canonical «غير نشط», ACTION_LABELS home = common.activityActions.{timeline,badge}.* (NOT yet created — COORDINATE 6c vs 6e, whichever wires first creates it).
- ⏸ REST POINT (user-directed, 2026-07-09): session stops here. NEXT SESSION RESUMES AT: 6c-T2 (clients stack — use census p6-clients-projects.txt + the T1 decision tables; then T3 projects, T4 files+teams incl. SHARED components/files/* [migrate in 6c per implementer concern #2 — they serve dashboard+portal, flag for Phase 7 no-double-migrate], T5 APIs, T6 closure+deploy). User was committing CONCURRENTLY (productivity report a53db63 + unstaged HR edits in working tree at rest time) — FETCH before every push, never touch user's unstaged files. After 6c: 6d content-ops → 6e misc+chat → Phase 7 portal → 8 notifications → 9 outbound docs.

---

# Call Tracking (Server + CRM) — SDD Progress (2026-07-10)

Plan: docs/superpowers/plans/2026-07-10-call-tracking-server.md
Spec: docs/superpowers/specs/2026-07-10-call-tracking-design.md
Branch: integrate-pending-fixes
BASE at start: 18cf517
Note: live curl verification of Tasks 3-5 is deferred into Task 8 (needs temp
test agent creds the implementers don't have); implementers verify tsc/tests.

- Task 1: complete (commit f141636, review clean; Minors noted: text-vs-varchar style, no SQL comments, no -- DOWN block)
- Task 2: complete (commits 7867d85 + 3d2c05f, review clean after 3 fixes: report-file heredoc path bug, dropped unsafe casts, missed-duration guard; Minors: report RED label mismatch, rounding + Dubai-midnight edge tests uncovered)
- Task 3: complete (commits 8b60f53 + a506ad5, review clean after 2 fixes: lockout reset moved to post-password-verify [Phase D lock], insert-before-deactivate key rotation; implementer also fixed middleware blocking /api/mobile/* [CSRF+session exemptions mirroring /api/external — reviewer verified scoping]; Minors: deactivate-update error not logged, concurrent-login zero-active-keys edge, dead middleware line, signOut cookie behavior unverified)
- Task 4: complete (commits 628079f + 9a4f467, review clean after 2 fixes: durable-insert-first + in-batch dedup [processedKeys], 23505-differentiated errors + new 'error' response status [authorized contract extension — document in CALL-TRACKING.md at Task 8]; Minors: bump skipped on activity-failure path undocumented, error-key stays in processedKeys, back-fill failure leaves null activity_id, sequential perf at batch 100)
- Task 5: complete (commits 8b0cf5f + 4854d05, review clean after fixes: 3 unchecked-supabase-error paths closed [re-match SELECT throws, retroLink SELECT logged, ignore upsert throws/flip logged] + 2 minors; Minors remaining: updated_calls=0 on flip failure, linked_calls counts attempted not confirmed, l! assertions, cross-agent retro-link verified by inspection only. OPERATIONAL NOTE: sayed is status='inactive' in prod — active agent for rollout is youssef; confirm live agent list at Task 8)
- Task 6: complete (commit f886f39, review clean; Minors: report omits zero-call agents [UI must handle — passed to Task 7], RBAC 403/200 + own/all scope not E2E-exercised [deferred to Task 8], no unit test for month-bounds glue [duplicated in 2 files], team-perf callsError no logError [inherited convention])
- Task 7: complete (commit df29894, review clean; Minors: CRM nav group 11 items vs max-10 comment, tooltip new Date(dateOnly) UTC-midnight edge, icon-chip markup duplicated, redundant aria-label+label)
- Task 8: complete (commit 3c07ddd, E2E green: login rotation + sync + b2c quick-add + notification + ignore + authenticated report scope=own via session cookie; zero-sweep clean across 9 tables + Auth; 'error' status + wrong-role login inspection-verified only)
- Final whole-branch review (fable): READY WITH FIXES → fixed in 9dc6643 (sync ignored-list SELECT now aborts batch on error; CALL-TRACKING.md provisioning sentence + wildcard-key note + v1.1 backlog section; processedKeys edge documented). All ledger Minors triaged: none block push, all backlog.
- EFFORT COMPLETE 2026-07-10: commits f141636..9dc6643 (13), pushed. Rollout notes: sayed inactive in prod (rollout agent = youssef); post-deploy 2-min check = admin session on /api/crm/calls/report → scope "all"; Plan 2 (Android app) next — contract locked in docs/CALL-TRACKING.md.

---

# Call Tracking — Android App (pyra-calls-app) — SDD Progress (2026-07-10)

Plan: docs/superpowers/plans/2026-07-10-call-tracking-android.md
Contract: docs/CALL-TRACKING.md (locked, live-verified)
Branch: integrate-pending-fixes
BASE at start: db4783a
Env verified: JDK17 Adoptium, ANDROID_HOME=%LOCALAPPDATA%\Android\Sdk (android-36, build-tools 36.1.0, emulator, system-images android-36), no standalone gradle (wrapper in Task 1), no AVD (Task 7).
WARNING: working tree has FOREIGN unstaged changes (messages/{ar,en}/crm.json, .superpowers/sdd/task-7-report.md) — implementers commit ONLY pyra-calls-app paths + named files.

- App Task 1: complete (commit 04e544d, review clean; Minors: no distributionSha256Sum, strings "Arabic seed" wording)
- App Task 2: complete (commit 919e872, review clean; time constants independently hand-verified by reviewer)
NOTE: concurrent session landed 5 CRM-redesign commits (83be441..61605b0) mid-effort. From App Task 3 onward: review packages are scoped to the implementer's reported commit SHAs (SHA~1..SHA), NOT recorded-BASE..HEAD. New foreign unstaged files now include HR attendance work — staging discipline stays pyra-calls-app-only.
- App Task 3: complete (commit f062be1, review clean; Minors: dead serializer import, deviceId first-access race)
- App Task 4: complete (commit 689c727, review clean; Minors: password field lacks KeyboardType.Password [brief-inherited], report-filename collisions across SDD efforts)
- App Task 5: complete (commits 78feaf5 + 984a37e, review clean after 1 fix: lastSyncAt heartbeat on empty passes [plan-inherited]; Minors backlog: Err not discriminating 5xx, concurrent unique-works cursor race [harmless, server dedups], readBatch loop bound counts actionable rows)
- App Task 6: complete (commit 0d9200d, review clean; Minors: ": " separator literal [brief-inherited], Home counts don't roll over Dubai midnight while open [v1 limitation])
- App Task 7: complete (commits 8d6d67f + 82c6441; E2E full-flow verified live on emulator incl. notification tap → quick-add → feedback deep-link; temp agent zero-swept; CRITICAL caught by review: live keystore password was in this tracked report file — REDACTED before any commit [6→0 occurrences, verified absent from git history]; release APK signed, final SHA-256 0ABC8CC4676139B49A3870ABA415D7B20213374F1C1A99F5EEDC167CED10F3E4)
- Final whole-app review (fable): READY WITH FIXES → fixed in 82c6441 (allowBackup=false [restore crash-loop], agent-handover guard [lastLoginUsername → installDayStart=now on agent change], IgnoreReceiver keeps notification on NetworkError, password IME type, docs: handover wipe step + app-side v1.1 backlog + silent-409 deviation note). All ledger Minors triaged: backlog, none block.
- EFFORT COMPLETE 2026-07-10: Android app built + E2E-verified. APK: pyra-calls-app/app/build/outputs/apk/release/app-release.apk. Keystore: C:\Users\engmo\pyra-keys\ (BACK IT UP — loss = re-sign = reinstall on phones). Rollout: youssef's phone per docs/CALL-TRACKING.md provisioning checklist.

---

# Call Tracking v1.1 wave (3 features, Abdou-approved 2026-07-11) — SDD Progress

Branch: integrate-pending-fixes (merge to main on Abdou's "ادمج")
Micro-design locks:
- A (calls table): NEW GET /api/crm/calls (list, page size 50, filters agent/direction/match_status), gate calls.view, non-team_view forced own; lead_name enriched. Table section under the cards on /dashboard/crm/calls.
- B (widget): render calls_month in the team-performance widget (field already in API since server Task 6).
- C (liveness): GET /api/mobile/ping (requireDeviceAuth; getExternalAuth bumps last_used_at) + SyncWorker pings on empty batch (heartbeat) + /api/cron/device-silent-check (Phase D §7 pattern; active device:% keys with last_used_at < now()-25h → notify ACTIVE admins, NotificationType 'device_sync_silent', per-device per-Dubai-day dedup via entity_id) + n8n schedule + APK rebuild (same signature = in-place update on phones).
- V1.1-B: complete (commit a5edcb4, review clean)
- V1.1-A: complete (commits 2045409 + 47684bd, review clean after file-size split; Minors: matched-lead names visible cross-agent [inherited from sync matching design — future scoping discussion], enrichment errors unguarded [sibling parity])
- V1.1-C: complete (commits a145fcd + aa5c32e, review clean; ops DONE by controller: n8n PyraCRM_Cron key granted cron.device-silent-check [scripts/sql/grant-cron-device-silent-check.sql] + workflow Z54Xnd2z6QpT0oIJ gained "Daily 09:30 Dubai" trigger → POST device-silent-check, PUBLISHED [activeVersion 2b8716b9]; heartbeat needs updated APK on phones — SHA in task-v11c-report)
- Final wave review (fable): READY WITH FIXES → done: n8n cadence tightened to every 3h (dedup caps 1 alert/device/day; published activeVersion 58bdb3b9) + CALL-TRACKING.md per-call-table bullet marked SHIPPED. Backlog from review: month-switch transient PGRST103 page fetch, notified counter counts attempts, dubaiMonthBounds 3rd copy → extract to lib/utils/format, hoursSilent clamp to days past 48h.
- V1.1 WAVE COMPLETE 2026-07-11: commits a5edcb4..aa5c32e + docs/grant. Phones need APK update for the ping heartbeat (SHA in task-v11c-report).

## V1.2 wave — self-update + error tracking + session fix (2026-07-16)
Plan: docs/superpowers/plans/2026-07-16-calls-app-v12-selfupdate-observability.md
- Task S1: complete (93ff8fa..3ddd340, review clean). NOTE: shipped as migration 039 (038 taken by concurrent Gap #3 work). Minor deferred: DATABASE-SCHEMA.md entry for pyra_app_releases + pyra_api_keys.app_version_code → fold into A5 docs step.
- Task S2: complete (commit d989394; pnpm run check + pnpm build both green, i18n:check clean). Shipped app-version/app-download/log-error routes (gate-then-service-role, `resolveChannel`/`APP_CHANNELS` factored into `app/api/mobile/_lib/app-channel.ts` per brief note — route files may only export HTTP handlers) + x-app-version device stamping in `requireDeviceAuth`. SELF-CAUGHT FIX vs. the brief's sample code: the stamping guard used `.neq('app_version_code', versionCode)`, which in SQL never matches when the column is NULL (Postgres `NULL <> value` = unknown, excluded from WHERE) — the migration's default leaves `app_version_code` NULL, so no device would ever get its FIRST stamp. Replaced with `.or('app_version_code.is.null,app_version_code.neq.<versionCode>')` (versionCode is a bounds-checked parsed integer, safe to interpolate — no PostgREST-filter-injection surface). Docs: CALL-TRACKING.md gained endpoints #6-8 + an x-app-version paragraph under the Auth model section documenting the same NULL-guard fix. logError's ErrorSeverity union confirmed to be exactly `'error'|'warning'|'info'` (matched the brief) and createSignedUrl confirmed to return `{data:{signedUrl}, error}` (matched an existing my-documents route). Backlog/concerns: none blocking — see task-S2-report.md.
- Task S2: complete (80593bd..3ce92f4, review clean). Implementer fixed brief's `.neq()` NULL bug (first-stamp never fired) via `.or(is.null,neq)`. Minors deferred: (1) mobile crash stack lands in metadata.stack, not stack_trace column (server-side Error stack is noise) — evaluate row quality in A5, fix if unusable; (2) CALL-TRACKING x-app-version wording slightly over-broad (login has no key).
- Task S3: complete (a28ffc5..4b4396f, review clean). 481-line script judged structure-not-bloat. Minors deferred: misleading "after deactivation" wording on fresh-channel insert failure; APK read twice (hash+upload); positional path silently ignored when --activate also passed.
- Task A1: complete (commit 9d0123c; `.\gradlew.bat test` + `.\gradlew.bat assembleDebug` both BUILD SUCCESSFUL). AppPrefs moved to plain `getSharedPreferences("pyra_calls_prefs", MODE_PRIVATE)` with an init-time one-shot migration from the old `pyra_calls_secure` EncryptedSharedPreferences store (security-crypto dependency kept — needed to read the old store); a migration-read throwable sets `pending_migration_loss_report` instead of losing the failure silently. Added `wasLoggedIn`, `pendingSessionLossReport`, `consumeSessionLossEvent()` (one-shot: true iff `wasLoggedIn && !isLoggedIn()`, resets both) as the tripwire API, plus 3 zero-default accessors for A4 (`lastUpdateCheckAtMillis`, `lastUpdateNotifiedCode`, `lastUpdateNotifiedAtMillis`). MainActivity calls `consumeSessionLossEvent()` in `onCreate` before `setContent` (seam comment: `// A2: ErrorQueue reports pending_session_loss_report + pending_migration_loss_report`), sets `wasLoggedIn=true` on the login callback, and sets `wasLoggedIn=false` before `clearSession()` in the logout callback. `clearSession()`'s key list is untouched (still preserves `device_id`/`last_login_username`, plus the new `migrated_from_encrypted`/`was_logged_in`/`pending_*_report` keys) — agent-handover guard verified byte-identical. HomeScreen.kt needed no change (logout logic lives in MainActivity's lambda, HomeScreen only invokes the passed-in callback). No AppPrefs unit tests exist (Android-bound, untestable without Robolectric) — behavior deferred to A5's on-device upgrade-in-place test per plan.
- Task A1: complete (78d4aeb..b849efd, review clean). Loss-tripwire flags are write-only until A2. Fold into A2: wrap deleteSharedPreferences in its own try/catch (delete-throw after successful copy would false-positive a migration-loss report).
- Task A2: complete (commit e96de15; TDD RED confirmed — 9 `Unresolved reference 'ErrorQueueLogic'` compile errors — then GREEN, 8/8 new tests + 19/19 suite total; `.\gradlew.bat assembleDebug` BUILD SUCCESSFUL). New `core/ErrorQueueLogic.kt` (pure `shouldEnqueue` Dubai-day dedupe via `DubaiTime` reuse + fail-open on unparseable timestamps, `cap` keep-newest-50) + `data/ErrorQueue.kt` (file-backed `error-queue.jsonl`, `synchronized(ErrorQueue::class.java)` read-modify-write, atomic temp-file+rename write, all IO in `runCatching`, `snapshot()` ≤20 oldest-first, `removeShipped(n)` removes first n VALID lines only — leaves any corrupt raw line untouched so it self-heals on the next `enqueue()` rewrite instead of desyncing the snapshot/remove count). `Payloads.kt` gained `ErrorEvent`/`LogErrorRequest`/`LogErrorData`. `ApiClient` gained `appVersion: Int = BuildConfig.VERSION_CODE` + `logErrors()`; SELF-CAUGHT FIX vs. the brief's sample: the brief showed `appVersion` as the LAST constructor param, but every existing call site uses trailing-lambda syntax for `deviceKeyProvider` (`ApiClient(url) { prefs.deviceKey }`) — Kotlin binds a trailing lambda to whichever param is textually last, so that ordering broke all 4 call sites (`No value passed for parameter 'deviceKeyProvider'` + type mismatch). Fixed by placing `appVersion` BEFORE `deviceKeyProvider` (still defaulted, still zero call-site churn) so `deviceKeyProvider` stays the trailing-lambda param; verified by compiling before/after. Both `get`/`post` now send `x-app-version`. Crash handler in `PyraCallsApp.onCreate` wraps the previous `Thread.UncaughtExceptionHandler` (enqueue then always delegate — never suppresses the crash). Catch points wired: `SyncWorker` Err branch (`sync_failed`, error for 5xx/401/403 else warning, enqueued before the existing `return Result.success()`) + queue-shipping snapshot/ship/removeShipped after the while-loop's successful exit (both the empty-pass and last-page break paths reach it; `Err`/`NetworkError` returns skip it by construction, matching the brief's "no retry escalation"); `QuickAddActivity` submit Err (`quick_add_failed`, warning); `IgnoreReceiver` Err (`ignore_failed`, warning, before the existing cancel-notification branch); `MainActivity` — both `pendingSessionLossReport` (`session_lost`) and `pendingMigrationLossReport` (`session_migration_failed`) consumed via the identical check→enqueue→clear pattern, right after the existing `consumeSessionLossEvent()` call. `NetworkError` never enqueued anywhere (by design). A1-carryover fix applied: `AppPrefs.migrateFromEncrypted()`'s `context.deleteSharedPreferences(...)` call now has its own inner try/catch so a delete-throw after a successful copy can't fall into the outer catch and false-positive a migration-loss report. Concerns (non-blocking, noted for A5): the crash-handler/MainActivity/QuickAddActivity enqueue calls run synchronously on their caller's thread (main thread in 3 of 4 catch points) rather than being dispatched to IO — matches the brief's own crash-handler sample verbatim and the file IO is a tiny jsonl append, but flag for A5's on-device pass if StrictMode noise shows up.
- Task A2: complete (03a6f1b..191c294 incl. fix round 1, re-review clean). Implementer fixed brief's ApiClient param order (trailing-lambda break); fix round dropped corrupt jsonl lines on enqueue rewrite (docs now truthful). Minors deferred: main-thread file IO at crash/UI catch points -> StrictMode check in A5.
- Task A3: complete (a82ee53..d8f4118, review clean). ON_RESUME re-query accepted over launcher-callback (functionally superior). Minors deferred: amber hex one-off (no theme warning token); HomeScreen card stack has no scroll container (pre-existing) - eyeball on emulator in A5.
- Task A4: complete (commit 072dc77; TDD RED confirmed — 8 `Unresolved reference 'UpdatePolicy'` compile errors — then GREEN, 8/8 new tests + full suite; `.\gradlew.bat assembleDebug` BUILD SUCCESSFUL). Gradle: `versionCode=2`/`versionName="1.2.0"` + `APP_CHANNEL` buildConfigField (debug `pyra-calls-e2e`, release `pyra-calls`). New `core/UpdatePolicy.kt` (pure `shouldCheck` 6h throttle + `shouldNotify` newer-AND-(different-code-OR-≥24h-since-last-nag-on-same-code)). `Payloads.kt` gained `AppVersionInfo`/`AppVersionData`/`AppDownloadData`; `ApiClient` gained `appVersion()`/`appDownload()` (both append `?app=` + `BuildConfig.APP_CHANNEL`, matching the S2 envelope shapes verified by reading the live routes). `Notifier` gained `CHANNEL_UPDATES` (IMPORTANCE_DEFAULT, registered in the existing `ensureChannels`) + `showUpdate()` (fixed id 9001, tap → `UpdateActivity`, autoCancel). `SyncWorker` runs the throttled check AFTER error-queue shipping, the whole block wrapped in `runCatching` so a check failure can never flip an otherwise-successful cycle to retry/failure. New `data/ApkDownloader.kt` (blocking, streams the signed download URL — deliberately NO `x-api-key` header — to `cacheDir/updates/update-{versionCode}.apk`, computing SHA-256 WHILE streaming in the same read loop, case-insensitive compare against the server's hex digest) + `ui/UpdateActivity.kt` (252 lines; `Idle→Downloading(progress)→Verifying→ReadyToInstall/Failed` state machine; archive-version guard via `packageManager.getPackageArchiveInfo` + `PackageInfoCompat.getLongVersionCode` > `BuildConfig.VERSION_CODE` — blocks a stale-APK update loop; `Settings.ACTION_MANAGE_UNKNOWN_APP_SOURCES` round-trip via `rememberLauncherForActivityResult` when install-from-unknown-sources isn't granted, resuming straight to install on return; `FileProvider` install intent). Failure severities exactly per controller resolution: HTTP/NetworkError branches (both the `appDownload()` call and `ApkDownloader`'s own streaming IOException) → `warning`; sha256 mismatch and the archive-version guard → `error`. `res/xml/file_paths.xml` (`cache-path name="updates" path="updates/"`) + manifest gained `REQUEST_INSTALL_PACKAGES`, `<activity .ui.UpdateActivity exported=false>`, and the `androidx.core.content.FileProvider` provider (`${applicationId}.fileprovider`, `grantUriPermissions=true`, meta-data → `@xml/file_paths`) — confirmed correct in the merged manifest (`cloud.pyramedia.calls.fileprovider` authority, versionCode/versionName stamped, no conflicts with WorkManager's own provider). Home footer: version line (`BuildConfig.VERSION_NAME`) + manual "التحقق من تحديث" button that bypasses `UpdatePolicy.shouldCheck`'s 6h throttle by design (newer → launch `UpdateActivity`; current → toast «أنت على أحدث نسخة»; failure → toast). All 12 new strings added to `strings.xml` (Arabic, no hardcoded literals). Self-caught fix: `Modifier` was initially imported from `androidx.compose.foundation.layout` (doesn't exist there — that package only has `Arrangement`/`Column`/`Spacer`/etc.) instead of `androidx.compose.ui.Modifier`; caught immediately by the RED `compileDebugKotlin` failure and fixed before the first GREEN run. Concerns (non-blocking, flagged for A5): (1) `ApkDownloader`'s `onProgress` callback mutates Compose `MutableState` directly from the IO dispatcher thread inside `withContext(Dispatchers.IO)` — safe per Compose's snapshot-system thread-safety guarantees and cheap (progress is percent-filtered to ≤101 calls), but not yet verified on-device; (2) HomeScreen constructs its own local `ApiClient` instance for the manual check rather than receiving one as a parameter (MainActivity/QuickAddActivity thread one through instead) — functionally identical since `ApiClient` is stateless, minor style inconsistency only; (3) no unit tests for `ApkDownloader`/`UpdateActivity` (IO- and Android-bound, untestable without Robolectric/instrumentation) — consistent with the rest of the app (only `core/` is unit-tested), full download→verify→install flow needs A5's on-device E2E pass, including exercising the unknown-sources settings round-trip and the stale-APK guard.
- Task A4: complete (7cac7bf..4b46850, review clean). Minors deferred: archive-parse-null shares "stale version" message; 0% progress when size_bytes<=0; disk-full surfaces network message; HomeScreen local ApiClient; deprecated getPackageArchiveInfo overload.
- Task A5: complete (E2E verification + first production publish + docs, 2026-07-16). Emulator `pyra_a15_test` (API 36 / Android 16) + local `pnpm dev` → prod Supabase. Built the temp `versionCode 99` debug (gradle bumped→99→assembleDebug→`git checkout` restored to 2 immediately) + the final `versionCode 2` signed release (cert `CN=Pyramedia X` verified via apksigner, distinct from debug key). BUILD GOTCHA: `cmd.exe /c "gradlew.bat …"` from Git Bash is a silent no-op (prints only the cmd banner, exit 0, builds nothing) — must invoke gradle via the PowerShell tool (`gradlew.bat --project-dir <abs> …`); three "successful" builds were actually UP-TO-DATE no-ops until caught by an unchanged APK mtime. STEP RESULTS: (1) **logout-fix / in-place upgrade** — v1 (`versionCode 1`) release installed → `adb install -r` v1.2 release upgraded in place (same keystore, no data clear) → relaunched clean (migration code runs safely on no-session). Session-SURVIVES-upgrade behavioural proof BLOCKED: test account `sayed` is `status='inactive'` AND GoTrue-`banned_until=2126` in prod (login 401s before the status gate) — un-banning/reactivating a prod account was out of scope + safety-sensitive; the active agents (youssef/cosette) are live phones whose keys the login route would deactivate (+ no passwords). Session durability WAS observed via the debug self-update below. (2) **self-update, FULL on-device flow** — published temp v99 debug to `pyra-calls-e2e`; v1.2 debug install with a session injected via `run-as` into plain prefs (`pyra_calls_prefs.xml`, device_key = a throwaway `calls:device` key attributed to active `youssef` since `sayed` can't auth) → Home «التحقق من تحديث» → app-version=99 → UpdateActivity → download → **SHA-256 verify** → «التحديث جاهز للتثبيت» → «تثبيت» → system PackageInstaller → Play-Protect "Install without scanning" → **app becomes versionCode 99** → reopened **still logged in** (plain-prefs session survived the in-place update) → re-check on 99 → «أنت على أحدث نسخة» (archive/not-newer guard held, no UpdateActivity relaunch). Screenshots in scratchpad/e2e-artifacts. (3) **error pipeline** — deactivated the throwaway key → `/calls/sync` returned `401` (exactly what SyncWorker enqueues `sync_failed` for) → reactivated → shipped a `sync_failed` batch via `/api/mobile/log-error` → `pyra_error_logs` row. ROW-QUALITY EVAL (A2 ask): fully triageable — `message` prefixed `[pyra-calls-app]`, `severity=error`, `metadata.{source=pyra-calls-app, app_source=sync_failed, agent=youssef, app_version_code=2, android_version=16}`, `request_headers.x-api-key`=**[REDACTED]**. Two harness notes: inline-Arabic via curl on Windows mangled the message's Arabic to `?` (harness artifact — re-shipping the identical batch via `--data-binary @utf8file` stored it as proper multibyte, proving the pipeline preserves UTF-8; the app sends UTF-8 via OkHttp); and PII-redaction's phone-formatting collapse strips the hyphens from `occurred_at`'s date (cosmetic, timestamp still readable). Fleet stamping confirmed: reporting key `app_version_code`→2; real fleet keys (sayed/cosette/youssef) stay NULL. (4) **hibernation** — fresh install → Permissions card «منع الإيقاف التلقائي» → «فتح الإعداد» opens system App-info → toggled "Manage app if unused" OFF → back → card gone. (5) **production publish** — `pnpm app:publish … --app pyra-calls` → v2 baseline row (is_active), SHA-256 `cbdfcb1ee1537c051dc844c6f63446620f3fa7190eac56e39de486d331d00fad` (matches Get-FileHash), 7.80 MB, Arabic release_notes stored as valid UTF-8. TEMP INFRA CHANGE (reverted): `pyra-private` bucket `file_size_limit` raised 10→20 MB to allow the 10.46 MB debug v99 e2e upload, restored to 10485760 after. CLEANUP (all verified 0 leftover): deleted the 2 probe `pyra_error_logs` rows, the 4 `pyra_agent_calls` the on-device sync wrote for youssef (`device_call_key LIKE 'e2ea5device00001:%'`, all unmatched/no activity), the `pyra-calls-e2e` release row + its storage object, the throwaway key; bucket limit restored; `sayed` device key left `is_active=true` (untouched); emulator app uninstalled. Docs: CALL-TRACKING.md gained the error-tracking-pipeline section + provisioning steps 8/9 (hibernation + self-update install-unknown-sources) + the «نشر تحديث جديد» self-update runbook + a 2026-07-16 verification record; DATABASE-SCHEMA.md gained `pyra_app_releases` + `pyra_api_keys.app_version_code` (S1 follow-up). Deferred/backlog: session-survives-upgrade behavioural test needs a usable test account (un-ban `sayed` in a controlled window, or a dedicated active throwaway agent) — mechanism is unit-tested + code-reviewed (A1); remove security-crypto dep after one fleet-wide v1.2 cycle (pre-existing v1.3 backlog).
- V1.2 WAVE COMPLETE 2026-07-16: self-update + error tracking + logout fix shipped. Production `pyra-calls` v1.2.0 (versionCode 2) baseline row published; fleet is on v1 (no updater) so ONE final manual signed-APK install round is still needed on the 3 phones (youssef/cosette + spare) — after that, updates are self-serve (`pnpm app:publish`, devices pull ≤6h). Error tracking live (`pyra_error_logs` filtered on `metadata->>'source'='pyra-calls-app'`).

## V1.2 final-review fix wave (2026-07-16)
3 findings fixed. (1) `scripts/publish-app-release.ts` — production-channel safety guard: refuses to publish to `pyra-calls` when aapt2 badging output contains `application-debuggable` or the package name isn't `cloud.pyramedia.calls`; the `--code/--name` aapt2-bypass path prints a loud (non-fatal) warning that these guards were skipped; plus two robustness touches — an upload-409/Duplicate hint naming the orphan storage path + how to unblock, and `runActivate` now prints the same `--activate <prev>` recovery hint as publish mode if `activateReleaseById` fails after `deactivateActive` succeeded (required changing `activateReleaseById` to return a result object instead of hard-failing internally). (2) `PermissionsScreen.kt` — `future.get()` in `rememberUnusedAppRestrictionsEnabled`'s listener wrapped in `runCatching { }.getOrDefault(UnusedAppRestrictionsConstants.ERROR)`, so an exceptionally-completed future can never crash the main thread on ON_RESUME (fires on both Permissions and Home). (3) `AppPrefs.kt` — `migrateFromEncrypted` now checks for the legacy store file (`shared_prefs/pyra_calls_secure.xml`) before doing anything else; on a fresh install (file absent) it sets `migrated_from_encrypted=true` and returns without ever constructing `EncryptedSharedPreferences`/its Keystore master key, closing a false-positive `pending_migration_loss_report` risk from a Keystore flake on a brand-new device. App code changed after the v2 APK shipped, so version bumped to `versionCode=3`/`versionName="1.2.1"` in `build.gradle.kts`. Verify: `.\gradlew.bat test` BUILD SUCCESSFUL, `.\gradlew.bat assembleRelease` BUILD SUCCESSFUL (signed); aapt2 confirms package `cloud.pyramedia.calls`, versionCode `3`, versionName `1.2.1`, no `application-debuggable` line. New release APK SHA-256 `83B2887BCFC50AB11DF10BABD6D5933C5E181F9D6EBA35DE7452573D34A12273`. Guard self-test run LIVE (both preconditions held: a pre-existing debug APK on disk + guard ordering statically confirmed to run before any network/DB call): `pnpm app:publish <debug-apk> --app pyra-calls --notes x` refused immediately after aapt2 detection (application-debuggable match), exit code 1, output stopped before the first REST call — confirms zero side effects. `pnpm run check` green. NOT published to any channel — production `pyra-calls` stays on the v1.2.0/versionCode-2 baseline row from A5; prod publish of v1.2.1 deferred to the owner's explicit merge go-ahead.
- Task A5 addendum (2026-07-16, coordinator follow-up): session-survives-upgrade behavioural test now **PASS** — closed via a dedicated throwaway prod agent (`e2e.upgrade@pyra.local`, GoTrue admin-created + `pyra_users` row id 16; sayed/youssef/cosette untouched). Signed v1 installed on emulator → REAL v1 login UI (EncryptedSharedPreferences write path, against prod) → `adb install -r` signed v2 → relaunched **still logged in** («أهلًا، E2E Upgrade Test», no login screen, no crash) — and the migrated device key authenticated a LIVE prod sync («متزامن»), proving the encrypted→plain migration carried a working credential. Bonus finding: v2's «التحقق من تحديث» against prod toasts «تعذر التحقق من التحديث» because prod main lacks `/api/mobile/app-version` until this branch merges — graceful failure, becomes «أنت على أحدث نسخة» post-merge. Cleanup verified: user `inactive`+`deactivated_at`, both device keys `is_active=false`, GoTrue ban to 2036 (87600h), post-ban login 401, 0 agent_calls rows, emulator app uninstalled; rows left as audit trail (no hard-delete). Evidence + screenshots appended to task-A5-report.md «Step 1 addendum».
- V1.2 MERGED TO MAIN 2026-07-20 (Abdou: "ادمج"): 3e274ab..81fbc72 -> origin/main, Coolify deploy verified live (app-version 401 gate). pyra-calls channel: v3 (1.2.1) published+active, v2 deactivated. Phones were upgraded manually BEFORE merge (07-20 morning): sessions survived on both real devices (active keys pre-dated install; youssef 13 + cosette 16 calls flowing same morning). Wave closed.

## V1.3 — Lead source picker in app quick-add (2026-07-20)
CRM-aligned `source` field on the mobile quick-add. Server: `app/api/mobile/leads/route.ts` accepts optional `source`, whitelisted to the 6 CRM `SOURCE_VALUES` (`add-lead-modal.tsx`) + the app-only `phone_call` default; invalid/absent falls back to `phone_call` (pre-v1.3 apps keep working). Confirmed no CHECK constraint on `pyra_sales_leads.source` (0 rows from `pg_constraint` query) — whitelist is app-layer only. i18n: `crm.lead.sources.phone_call` was MISSING from both `messages/{ar,en}/crm.json` (not added by the earlier call-tracking wave as guessed) — added ("مكالمة هاتفية" / "Phone call") so `<LeadSourceIcon>` renders app-created leads' label instead of falling back to the raw `phone_call` string. `docs/CALL-TRACKING.md` endpoint #3 contract updated. App: `Payloads.kt` `QuickAddRequest` gained `source: String? = null` (trailing, backwards-compatible both directions); `QuickAddActivity.kt` gained an `ExposedDropdownMenuBox` source picker (7 options, `phone_call` preselected) — required `@OptIn(ExperimentalMaterial3Api::class)` on `onCreate` (compose-bom 2024.12.01 still marks the dropdown APIs experimental; scoped to this screen, not project-wide). `strings.xml` gained 8 new Arabic strings (no Kotlin literals). `PayloadsTest.kt` gained 2 tests (source omitted when null, included when set). Version bumped `versionCode=4`/`versionName="1.3.0"`. Verify: `pnpm run check` (tsc+i18n:check) clean, `pnpm build` compiled successfully, `.\gradlew.bat test` BUILD SUCCESSFUL, `.\gradlew.bat assembleDebug` BUILD SUCCESSFUL. NOT published to any channel per instructions (`pnpm app:publish` deferred to the owner) — production `pyra-calls` stays on v3/1.2.1.
- V1.3 source picker: complete (d6ff351..11c85a6, review clean). App v1.3.0/code4 built (debug only - release+publish deferred to Abdou's go). Minors deferred: no server unit test for resolveLeadSource; menuAnchor deprecation (BOM-pinned); LeadSourceIcon generic icon for phone_call; docs example without source field.
- V1.3 MERGED + PUBLISHED 2026-07-20 (Abdou: "ادمج"): d6ff351..838ab36 -> origin/main; pyra-calls channel v4 (1.3.0) active, v3 deactivated. SHA 65e2c8b6...2a34d (7.82 MB). First-ever self-serve fleet update in flight; monitor watching app_version_code flips.

---

# Employee Offboarding — SDD Progress (2026-07-20)

Plan: docs/superpowers/plans/2026-07-15-offboarding.md
Spec: docs/superpowers/specs/2026-07-15-offboarding-design.md
Branch: integrate-pending-fixes (tracks origin/main; push deploys prod — confirm first)
BASE at start: 4953c8e
Migration number: 040 (039 taken by app_releases)

- Task 1 (migration 040 + type + constants): complete (commit ab68c7d, review clean; Minor: interface placement, self-disclosed)
- Task 2 (lock-account.ts ban primitive): complete (commit 9e8e8b6, review clean; test cast as-never->as-unknown-as fixed, verified behavior-preserving)
- Task 3 (final-settlement.ts pure math): complete (commit ca2fe1f, review clean; Set<string> conversion caught a real runtime bug; 5,133.33 hand-verified)
- Task 4 (final_settlement source_type + payroll exclusion): complete (commit 7966a4f, review clean; .neq verified on the linkedPaymentIds-feeding query)
- Task 5 (PATCH lock hook + 2 defects + re-hire unlock): complete (commit 6d9e066, review clean; MINOR for final review: double-logError on lock error branch, plan-mandated + harmless)
- Task 6 (access-reconcile cron): complete (commit 2f023bc, review clean; MINOR for final review: admin-lookup error unchecked + no .limit on users fetch, both pre-existing conventions; Steps 3-4 live-trigger+n8n DEFERRED-operational)
- Task 7 (handover.ts build + execute): complete (commits 10525c5 + fix ee27c2e, review clean after 1 fix — Important board-task scope+collision resolved). MINORS for final review: (1) DRY — open-task predicate duplicated between buildHandover + getOpenTaskIds (drift risk, extract shared helper); (2) handover.ts is 449 lines — consider split handover-build.ts / handover-execute.ts (keep @/lib/hr/handover public path for Task 8 imports); (3) no unit test for getOpenTaskIds/collision-split (needs mocked supabase).
- Task 8 (exit orchestrator GET+POST): complete (commits 48982fa + fix 4150b62, opus review clean after 1 fix — Important concurrent-double-settlement closed via optimistic status-claim + exit_reason whitelist). MINOR for final: offboarding_completed NotificationType added but unused; UI (Task 10) MUST surface handover_results.errors + locked/lock_error (200-with-errors contract).
- Task 9 (useOffboarding hooks): complete (commit 568873d, opus review clean; SubmitExitResult exports handover_results+locked for the wizard). MINOR: lock_error type string vs string|null (brief-faithful).
- Task 10 (ExitWizard UI): complete (commits d63c41e + fix cc55ba7, opus review clean after 1 fix — reassign-target validation added). 8 files (buckets/parts split for size), hr.offboarding i18n 66 keys AR+EN parity, all 5 UX reqs met. NOT mounted yet (Task 11).
- Task 11 (users-client surgery + 3 buttons): complete (commit 86cb3b1, opus review clean; status fully severed from edit PATCH, 3 buttons + self-guard + ExitWizard mount, 10 i18n keys). MINOR: confirm-button dark pair (cosmetic); UserEditDialog extraction DEFERRED (authorized, users-client still 957 lines).
- Task 12 (statuses + guide + final verify): complete (commit 5dc29ec, opus review clean). FINAL VERIFY GREEN: check clean, 189 tests / 27 files pass, build ok w/ both routes (/api/users/[username]/exit + /api/cron/access-reconcile). MINORS for final: (a) guide tip overgeneralizes archive to all buckets (tasks-only); (b) StatusEntity union lacks offboarding (no call site today).

ALL 12 TASKS COMPLETE + whole-branch review clean. Baseline 4953c8e -> HEAD (18 offboarding commits). One whole-branch FIX applied: unban-on-reactivate PATCH (70d522b, opus re-review clean). All 14 accumulated Minors triaged to BACKLOG (none block merge). Feature READY. Remaining OPERATIONAL (need Abou): (1) wire access-reconcile cron in n8n PyraHR_Cron; (2) live dry-run on a test/real exit; (3) push to main (deploys prod).

## Urgent fixes wave (2026-07-25) — plan docs/superpowers/plans/2026-07-25-calls-urgent-fixes.md
Bundle chosen by Abdou: live bugs 1+2+4 + broken filters. Backfill decision: FULL cleanup (delete fake activities + correct timestamps).
- UF-T1: complete (bec8682..4027a11, review clean). isConnectedCall in lib/calls/match.ts; both call sites; 0-sec pyra_agent_calls insert preserved. Minor deferred: stale JSDoc in sync/route.ts still says connected = direction != 'missed' -> fix in T6 docs step.
- UF-T2: complete (backfill applied to prod DB 2026-07-25). Step-1 measured fake_activities=257 (exact match) and poisoned_leads=109 (audit said 107, off by 2 — new 0-sec dials synced in the ~day since the audit; not material, proceeded). Affected-leads candidate set (activity-referenced UNION poisoned) = 177; 164 needed an actual update (123 -> NULL, 41 -> earlier genuine timestamp), 13 already correct (skipped as no-ops). Dry run matched exactly, mutated nothing (re-verified). Applied: 257 activities deleted (2 batches of <=200), 164 leads updated (1 batch, single UPDATE...FROM(VALUES) statement). After: fake_activities=0 (exact). poisoned_leads=5, NOT 0 as predicted — verified NOT residual corruption: each of the 5 leads has a genuine connected call (duration 37-166s) whose called_at now correctly equals last_contact_at, which coincidentally falls in the same clock minute as an unrelated 0-sec redial attempt on the same lead (dial-no-answer-redial-connect pattern) — the audit's minute-granularity heuristic can't distinguish "value came from the bad call" vs "value coincides by chance," so this is the detector's known blind spot, not a bug. 3 spot-checked leads (1 NULL case, 2 moved-back cases) all match MAX(genuine called_at) exactly, verified row-by-row against pyra_agent_calls. Snapshot: backups/zero-duration-backfill-1784924818610.json (local, gitignored, pre-apply copy 1784924766431.json also kept from the dry run). Files added: scripts/backfill-zero-duration-contact.ts (runner, dry-run default, file-read-only env, batched writes, abort-on-first-error), scripts/sql/backfill-zero-duration-contact.sql (read-only reference/audit query, verified byte-for-byte against the runner's own numbers).
- UF-T2: complete (bf74167..d3bf8cc, review clean + independent data verification). 257 fake call_logged deleted -> 0; 164 leads corrected (123 -> NULL, 41 moved back); snapshot backups/zero-duration-backfill-1784924818610.json. Residual "5 poisoned" = false positive of the minute-granularity detector (all 5 independently confirmed: last_contact_at == genuine call's called_at). No collateral: 580 surviving call_logged = 491 genuine + 89 legacy-null-duration. Minor: snapshot omits created_by on deleted rows (brief's column list) -> note for future backfill templates.
- UF-T3: complete (code+tests, NOT yet deployed). No existing chunk/batch helper found (grepped lib/) -> TDD'd lib/utils/chunk.ts (RED confirmed: import-resolution failure with helper absent, then GREEN 4/4). Batched both named .in() sites in app/api/cron/lead-idle-check/route.ts (line ~134 activities_select, ~188 idle_dedup_select) at LEAD_ID_BATCH=150, preserving fail-closed logError/apiServerError verbatim per-batch; Q2's cross-batch merge switched from first-wins-DESC to explicit max-keeping (still correct, more robust to future batch-order changes). Scanned whole file: 1 more .in() found (notif_dedup_select, recipient_username) -> NOT batched, documented inline why (bounded by active agent/admin count, DB-verified =3, nowhere near the lead-id-count that caused the outage). Verify: 199/199 tests pass, tsc+i18n:check clean, build succeeds (350 pages). Step 5 (prod dry-fire) BLOCKED, not by API-key access (n8n handled auth fine) but by DEPLOY STATE: git merge-base HEAD/origin/main = 7070a89 with 5 commits ahead / 14 behind (fully diverged, no overlap) — this fix lives only on integrate-pending-fixes and Coolify deploys only from main (push to main forbidden without Abdou's ادمج), so a live call right now would exercise the OLD unbatched code and just reproduce the same URI-too-long failure (confirmed 11 consecutive daily failures through 2026-07-24 01:00 UTC via pyra_error_logs) rather than validate the fix. Independently verified UF-T2's data cleanup IS live in prod DB (0 zero-sec fake activities remain via a fresh join query) since data fixes bypass code deploy — only the code fix is blocked. One harmless real n8n execution WAS performed (execution 126633, "Every 5 Minutes" trigger only, hit follow-up-reminders idempotently: processed:0) while probing tool behavior; did not target lead-idle-check specifically to avoid reproducing a known-inevitable failure or copying the visible wildcard key into a throwaway workflow. Recommend: re-run Step 5 after main is updated + Coolify redeploys (natural fit for UF-T6 closure).
- UF-T3: complete (d12ea68..e8b3874, review clean). Both unbounded .in() batched at 150 via new chunk() helper; fail-closed contracts byte-preserved; max-keeping activity map. 3rd .in() (notif_dedup_select on recipient_username) left unbatched - bounded by active headcount, not data. Step 5 prod dry-fire BLOCKED until main carries the fix -> re-run at T6.
- NOTE: merged origin/main (Abdou's Codex deductions wave, 14 commits / 200 files) into the branch at this point. ZERO file overlap with our wave; tsc+i18n clean. PRE-EXISTING failure on main, not ours: __tests__/atomic-task-write-routes.test.ts > "passes every RPC parameter with the exact migration name and order" (file byte-identical between main and our HEAD; our 11 changed files are disjoint from it).
- UF-T4: complete (f88a0e1). New POST /api/cron/error-digest mirrors device-silent-check structurally (getExternalAuth -> cron.error-digest or * -> service-role -> per-select logError). Verified before writing (not assumed): pyra_error_logs has both `resolved boolean NOT NULL DEFAULT false` and `severity varchar NOT NULL` (information_schema query), and notifyMany(supabase, recipients[], input) signature matches the brief's call site exactly (lib/notifications/notify.ts). Rolling 24h window (Date.now()-24h), not a Dubai-calendar-day bucket — no per-day dedup need here (unlike device-silent-check), so dubaiDayKey() was deliberately NOT imported despite being listed in the task interfaces (documented inline in the route). Added 'system_error_digest' to NotificationType. Grant SQL applied to prod (scripts/sql/grant-cron-error-digest.sql) and verified: pyra_api_keys 'n8n PyraCRM_Cron' now carries cron.error-digest alongside its 6 existing cron perms. docs/MIGRATIONS.md Operations cron table got a new row. Verify: tsc+i18n:check clean, build compiled (350+ pages incl. the new route), 711/712 tests pass (1 pre-existing failure on main, confirmed not new). n8n: added HTTP node "POST /api/cron/error-digest" to PyraCRM_Cron (Z54Xnd2z6QpT0oIJ), wired from the EXISTING "Daily 09:00 Dubai (05:00 UTC)" trigger (same schedule as lead-idle-check, reused rather than duplicating a trigger) instead of adding a new Schedule Trigger node. publish_workflow called after update_workflow (both times — a metadata-only edit after the first publish re-drafted it, caught and re-published); confirmed activeVersionId == versionId == 31589f54-98cf-48fc-b07d-4ab54af68a39 by re-reading the workflow, with the new node present in both the top-level `nodes` and the `activeVersion.nodes` snapshot. Did NOT test-execute — route 404s until main carries this commit. Pre-existing HARDCODED_CREDENTIALS validation warnings surfaced for all 4 HTTP nodes in the workflow (including the 3 already-live ones) — the x-api-key is inlined in headerParameters rather than an n8n credential; this matches the workflow's existing pattern verbatim, not a regression introduced here (flagged as a v1.1 n8n-hygiene item, not blocking).
- UF-T4: complete (7bf4182..ab43cdc incl. fix round 1, re-review clean). /api/cron/error-digest: active-admin digest, silent on a clean day, Dubai-day dedup (fails closed), true DB count (not the capped array), grant SQL applied, n8n node published in PyraCRM_Cron (versionId == activeVersionId 31589f54). Route 404s until main carries it. Minor deferred: notifyMany delivery failure is console-only (inherited from device-silent-check) - shared follow-up.
- UF-T5: complete (f76db17..4c39fea, review clean). phone_call added to pipeline source filter + both catalogs; ?filter=at_risk now applies a client-side predicate matching deals-at-risk (approximated to last_contact_at alone - documented, provably one-directional/over-inclusive); removable chip. Kanban drag invariants untouched. FOLLOW-UP FOR T6: ai-insights/route.ts:128 links to /dashboard/crm?filter=at_risk (the dashboard, which reads no filter param) - one-line href fix to /dashboard/crm/pipeline?filter=at_risk. Minor: AT_RISK_DAYS_THRESHOLD=7 duplicated rather than shared.
- UF-T6: complete (closure). Fixed UF-T5's follow-up (ai-insights/route.ts:128 -> /dashboard/crm/pipeline?filter=at_risk, matching the other two at-risk links verified byte-for-byte before the edit). Fixed UF-T1's stale JSDoc in app/api/mobile/calls/sync/route.ts (top-of-file comment now says isConnectedCall: direction != 'missed' AND duration_seconds > 0, was direction-only). End-to-end verification (prod DB, 2026-07-25): fake_activities_remaining=0 (T2 holds). idle_warnings_last_24h=0 - EXPECTED, not a failure: T3's fix is still un-deployed (integrate-pending-fixes has not been merged to main), so this is the pre-deploy baseline, not a regression check. Cron-failure query confirms lead-idle-check is STILL failing daily against the still-deployed old code: 1 failure in the last 24h (2026-07-24 01:00 UTC, same signature as the 11-day streak - "URI too long" at stage activities_select) and exactly 3 failures over 72h (one per daily run), zero failures from any other job - no NEW failure class introduced by this wave. docs/CALL-TRACKING.md: added the 0-second dial rule as a design-lock subsection mirroring the existing missed-call rule (right after it, ~line 210), added a matching bullet to the Sync semantics summary, and added an error-digest paragraph under Admin triage (not-yet-live in prod noted explicitly). Memory file calls-system-audit-2026-07-24.md updated: bugs 1 (idle cron)/2 (0-sec contact)/4 (no alerting) plus the phone_call filter + ?filter=at_risk items marked FIXED with today's date and a note that the wave is code-complete on integrate-pending-fixes but NOT deployed; the 4 unwired crons, WhatsApp auto-assignment, and backups-pending-Abdou items left explicitly open, untouched. Verify: pnpm run check clean (tsc+i18n), pnpm build succeeded, pnpm test: 1 pre-existing failure (__tests__/atomic-task-write-routes.test.ts, inherited from the owner's separate deductions-wave merge noted in UF-T3's ledger line) - confirmed not newly broken by this task's changes (docs + 2 one-line code edits, no logic touched). WAVE COMPLETE on integrate-pending-fixes, all 6 tasks done. Deploy + Step-5 prod dry-fire of T3/T4 still pending Abdou's "ادمج".
- UF-T6 + final whole-wave review (fable): complete (c48efec..03e271b). Docs/ledger/memory + ai-insights at_risk href fix + stale JSDoc. Final review found 1 CRITICAL (operational): error-digest shared an n8n trigger with lead-idle-check and ran AFTER it -> n8n halts the execution on node failure, so the watchdog died with the job it watched. Fixed: own Schedule Trigger 06:00 Dubai, detached, published (versionId == activeVersionId b6e1c205). Also fixed: backfill NULL::timestamptz cast (all-NULL batch would 42804 AFTER deletes); chunk()+error-check the unbounded .in() in deals-at-risk + ai-insights (841 ids ~17KB, same bug class, silently degrading admin surfaces); fail-closed the unchecked activeUsers SELECT in lead-idle-check; corrected 09:00->05:00 Dubai time labels; ai-insights now degrades gracefully AND logs (round 2). WAVE CODE-COMPLETE, AWAITING DEPLOY.
- DEPLOYED 2026-07-25 (Abdou: "ادمج"): 3dc3631..a8b3057 -> origin/main. First Coolify build FAILED (cold cache after a docker prune — died in the base-image nix step before any of our code; disk 40%/RAM fine, verified via Hostinger metrics). Abdou hit Redeploy -> succeeded. POST-DEPLOY VERIFIED LIVE: lead-idle-check ALIVE after 12 days dead (leads_checked 772, leads_idle 492, activities_inserted 492, agents_notified 3 — one grouped notif each, no spam); error-digest first run OK (new_errors 1, unresolved 51, failing_jobs [lead-idle-check], 1 admin notified) and its same-day dedup verified by an immediate re-run (skipped_already_notified_today true); gap-window sweep re-ran the backfill (16 fake activities from the pre-deploy window deleted, 14 leads corrected) -> fake_activities 0; residual 4 "poisoned" leads re-verified as the known same-minute false positives (last_contact_at == genuine call). Zero cron failures in the hour after deploy.
- FOLLOW-UPS OVERDUE (2026-07-25, Abdou chose "silent catch-up then wire"): 145 past-due follow-ups sat at status='pending' because follow-ups-check-due was never scheduled (youssef 127, mo.hanach 17, kassem 1; oldest 2026-06-23). Route notifies PER ROW, so wiring it first would have dumped 145 bells. Instead: snapshotted the 145 ids (scratchpad/overdue-backlog-snapshot.json), flipped them silently via direct UPDATE (0 notifications verified), THEN granted cron.follow-ups-check-due + added a 15-min trigger to PyraCRM_Cron (published, versionId == activeVersionId b2c846b6, 5 triggers). Smoke test after wiring: {flipped:0, notified:0} — correct, backlog already cleared, so only NEW transitions notify from now on. Screens now show 145 overdue / 25 pending.

## Wave 1 (honest call metrics) + Wave 2 (agent app) — plan docs/superpowers/plans/2026-07-25-calls-w1-metrics-w2-agent-app.md
- W1-1: complete (e7dc8c6). TDD (RED confirmed: 4 new tests failed with `answered`/`answer_rate` undefined on AgentCallStats; GREEN: 8/8 pass). AgentCallStats gained `answered`/`answer_rate`; avg_duration_seconds denominator changed from `outgoing+incoming` to `answered` (direction != 'missed' AND duration_seconds > 0); answer_rate = round(answered/(outgoing+incoming)*1000)/10, 0 when attempts=0. No existing test expectation needed changing — the 2 pre-existing fixtures with non-missed calls happened to have zero 0-second dials in them, so their asserted averages (80, 90) are unchanged by the new answered-only denominator (verified by hand: same rows qualify as "answered" as previously qualified as "connected"). Verify: pnpm test calls-report 8/8 pass; full pnpm test 715/716 pass (1 pre-existing unrelated failure, atomic-task-write-routes.test.ts, confirmed not new); pnpm run check clean (tsc+i18n). Files: lib/calls/report.ts, __tests__/calls-report.test.ts. NOT pushed (per task instructions — push deferred to wave closure).
- W1-2: complete. Step 1 verified (not assumed): `app/api/crm/calls/report/route.ts` spreads `...agg.per_agent[u]` wholesale onto each agent object — `answered`/`answer_rate` already flow through with zero API changes (file untouched, not staged). Added `answered: number`/`answer_rate: number` to `CallsReportAgent` (hooks/useCallsReport.ts). Added two `<Stat>` tiles (icons `PhoneCall`/`Percent`, neither previously imported — verified) to CallsSummaryCards.tsx immediately after `missed`, grid 10->12 tiles. Grid decision: left `grid-cols-2 sm:grid-cols-5` unchanged — 12 is even (6 rows of 2 on mobile) and on sm+ gives 2 full rows of 5 plus a trailing partial row of 2, which is a normal grid-fill artifact (same pattern already implicit in the 10-tile layout's own uneven row math), not a breakage. Added `answered`/`answerRate` keys to both messages/{ar,en}/calls.json under the existing `calls` namespace (ar: "تم الرد"/"نسبة الرد"; en: "Answered"/"Answer rate") — path already covered by the i18n-check manifest's `components/crm` + `hooks/useCallsReport.ts` entries, no manifest change needed. Verify: pnpm run check clean (tsc+i18n:check); pnpm build succeeded; pnpm test 715/716 (same 1 pre-existing failure, atomic-task-write-routes.test.ts, confirmed not new). Step-5 SQL (current-month, answered-only avg) against prod: sayed attempts=3/answered=2/true_avg=10s, youssef attempts=465/answered=274/true_avg=87s, cosette attempts=382/answered=259/true_avg=60s — matches the pure `computeCallsReport` denominator (answered) the API already returns via the wholesale spread, confirming end-to-end correctness with no separate app-side fetch needed (route logic is a straight passthrough of the already-tested W1-1 helper).
- W1-2: complete (36a8ca5..7a0993f, review clean). CallsReportAgent + 2 Stat tiles + i18n both catalogs. API needed NO change (route spreads ...agg.per_agent[u] wholesale, verified by the reviewer reading route.ts:83). Minor deferred: 12 tiles leave a partial trailing row at sm:grid-cols-5 (cosmetic, grid classes left alone per brief).
- WAVE 1 COMPLETE (2deb7df..7a0993f). Report now shows answered-only average + answer rate. NOT deployed - awaits «ادمج».
- W2-1: complete (self-reviewed, not yet committed at write-time of this line). New `GET /api/mobile/my-day` (device key, `calls:device`, NO RBAC scope from that auth — every query filters by `agentUsername` itself). Step 1 schema check confirmed all 7 named columns on both tables match the brief exactly (no drift). Two lists: `follow_ups` (assigned_to=me AND status IN pending/overdue AND due_at<=now()+1d, due_at ASC, capped 20, true count via `count:'exact'` unaffected by the cap, lead name/phone batched via one `.in('id', leadIds)`) and `going_cold` (assigned_to=me AND archived_at IS NULL AND is_converted IS NOT TRUE [NULL-safe] AND greatest(last_contact_at, created_at) < now()-7d, capped 20, excludes leads already in follow_ups). The `greatest()` filter has no Supabase-js equivalent for a computed expression, so it's built from two provably-equivalent column filters (`created_at < cutoff` AND (`last_contact_at IS NULL` OR `< cutoff`) — max(a,b)<c iff a<c AND b<c, Postgres GREATEST ignores NULL args) — same reasoning applies to why the DB-side `.order()` can't sort by the same expression, so the route fetches a generous candidate cap (500, `GOING_COLD_FETCH_CAP`) and does the precise oldest-first sort in JS; `count:'exact'` still returns the true total independent of that cap (same pattern as `GET /api/crm/follow-ups`). going_cold needed NO second/batched query at all — `pyra_sales_leads` already carries name/phone/company directly, so the primary query IS the enrichment; only follow_ups needed the join. Verify: `pnpm run check` clean (tsc+i18n), `pnpm build` succeeded (route appears in the manifest), `pnpm test` 715/716 (same 1 pre-existing unrelated failure, `atomic-task-write-routes.test.ts`, confirmed not new). Step 3 could NOT use a live device call (task explicitly forbade inventing a device key or logging in as youssef/cosette, since login deactivates their live device key and would knock a real phone offline) — verified instead via 5 SQL queries replaying the route's exact filter logic against the two ACTIVE agents: follow_ups youssef=128/cosette=0 (matches the constraints doc's ground truth); going_cold youssef=139/cosette=278 (cosette's empty follow-up list is exactly why going_cold matters for her — 278 leads would otherwise show nothing to call); exclusion delta proved load-bearing (youssef 220 without exclusion -> 139 with, 81 excluded; cosette unchanged at 278, nothing to exclude); an `is_converted IS NULL` check on these two agents returned zero rows today (the NULL-safe guard is still correct/required by convention, just not currently exercised by their live data); a `GREATEST(...) ASC LIMIT 10` sample confirmed both the NULL-collapses-to-created_at behavior and a real row (`Mr Smart Dubai`) where last_contact_at > created_at sorts by the later value, matching the route's `Math.max` logic exactly. Docs: added `### 9. GET /api/mobile/my-day` to `docs/CALL-TRACKING.md` with the shape, scope rules, and the SQL verification numbers (honestly labeled as SQL-verified, not a live curl run). NOT pushed (per task instructions).
- W2-1: complete (7a0993f..d96952a incl. fix round 1, re-review clean). GET /api/mobile/my-day: agent-scoped follow-ups (<=1d ahead, cap 20) + going-cold leads (>7d, cap 20) with true post-exclusion counts. Fix round: (1) cold-pool cap 500->2000 (above the ~921 system-wide lead total) + logError warning when the exact count breaches it + DB order last_contact_at ASC NULLS FIRST, JS effective-contact sort still authoritative; (2) exclusion set now comes from a SEPARATE unlimited open-follow-up query, not the capped display list - the earlier "81 excluded" verification was mathematically impossible against a <=20-id set. Corrected live counts: youssef 222->127 going cold (95 overlap), cosette 278 (zero follow-ups ever).
- W2-2: complete (self-reviewed, not yet committed at write-time of this line). New `POST /api/mobile/call-outcome` (device key, `calls:device`, NO RBAC scope from that auth). Step 1: read `POST /api/crm/follow-ups` in full and mirrored its follow-up insert field-for-field (id/lead_id/assigned_to/due_at/reminder_at[default due_at-30min]/send_whatsapp_reminder[default true]/title/notes/status/created_by) plus the `follow_up_created` timeline activity + the `leads.next_follow_up` earliest-pending-due_at sync, both fire-and-forget exactly like the CRM route. Deliberately NOT mirrored: the `leads.assign`-gated "assign follow-up to someone else" branch and its `notify()` call — this endpoint's `assigned_to` is always the calling agent (own-lead action), so that branch would be structurally dead code. Mandatory ownership re-check at route.ts:104 (`!lead || lead.assigned_to !== agentUsername` -> 403) — schema-verified both `pyra_lead_activities` (activity_type='note', existing value, no new type) and `pyra_sales_follow_ups` columns via information_schema before writing. Rollback: last_contact_at update failure deletes the just-inserted note activity (half-write never reports success); a follow-up-insert failure AFTER the note+bump already committed is deliberately NOT rolled back (documented tradeoff — call outcome is already a true fact; a retry after this specific failure would duplicate the note, accepted for v1, no transactions in this codebase). Verify: `pnpm run check` clean (tsc+i18n), `pnpm build` succeeded (route appears in the manifest, 1.27 kB), `pnpm test` 715/716 (same 1 pre-existing unrelated failure, `atomic-task-write-routes.test.ts`, confirmed not new — no other file regressed). Step 3 could not use a live device call (same youssef/cosette constraint as W2-1) — verified by code inspection + schema check only; live E2E deferred to a later task. Docs: added `### 10. POST /api/mobile/call-outcome` to `docs/CALL-TRACKING.md` right after the my-day section, with the exact ownership-check file:line, the mirrored/not-mirrored field list, and the rollback semantics spelled out. NOT pushed yet (pending commit in this same task).
- W2-2: complete (8a766e9..dc8e62c incl. fix round 1, re-review clean). POST /api/mobile/call-outcome: ownership 403 before every write, note activity (existing type), last_contact_at bump with rollback, optional follow-up mirroring the CRM POST field-for-field. Fix round: flip-and-warn (follow-up failure now 200 + follow_up_error flag, not 5xx — a 5xx made the phone retry and duplicate the note), 60s retry dedup that FAILS OPEN, and the unchecked pending-select error. NOTE: the fix agent died mid-run before committing; controller verified check+build itself and committed the tree.
- W2-3: complete (self-reviewed, not yet pushed at write-time of this line). App-only, 3 files. `Notifier.showMatched(context, leadName, leadId)` added right after `showFeedback` — same builder shape (`FLAG_UPDATE_CURRENT or FLAG_IMMUTABLE`, `setAutoCancel(true)`, `notifySafe`), reuses `CHANNEL_FEEDBACK` (no fourth channel — a matched call is "here is something about a lead", same category as the existing feedback nudge) and deep-links `ACTION_VIEW` to `BuildConfig.BASE_URL + "/dashboard/crm/leads/" + leadId`. Notification id/PendingIntent request code = `leadId.hashCode()` so a second call to the same lead replaces rather than stacks. `SyncWorker.kt`'s results loop got one sibling `if` block (`r.status == "matched" && r.lead_id != null && r.lead_name != null -> Notifier.showMatched(...)`) added directly after the existing `unmatched` branch, verbatim per the brief's snippet — the `unmatched` branch itself is byte-identical (diff confirms only an addition, no deletion/reflow in that hunk) and the cursor/`SyncPlanner.nextCursor` logic below is untouched. Two new Arabic strings in `strings.xml` (`notif_matched_title` = "مكالمة مع %1$s", `notif_matched_body` = "اضغط لفتح كارت العميل") inserted right after `notif_feedback_body`, `%1$s` placeholder convention per the brief. No version bump (reserved for W2-5 per instructions). Verify: `.\gradlew.bat test` BUILD SUCCESSFUL (1m31s, no new failures — pre-existing `menuAnchor()` deprecation warning in `QuickAddActivity.kt`, unrelated); `.\gradlew.bat assembleDebug` BUILD SUCCESSFUL (9s). `git status --short` showed only the 3 intended files modified (plus pre-existing untracked `AGENT-ONBOARDING.md`/`AGENTS.md`/`skills-lock.json` from other sessions, left alone). Committed via exact-path staging, not pushed (per task instructions — push deferred).
- W2-3: complete (e543983..8b3b29b, review clean). Notifier.showMatched (reuses CHANNEL_FEEDBACK, id=leadId.hashCode() so repeats replace) + SyncWorker now acts on matched results instead of discarding lead_id/lead_name. unmatched branch verified byte-identical; cursor/retry logic untouched; no version bump (W2-5 owns it). Minor deferred: the three notification helpers hash three different string domains into one id space - namespace with a type prefix when notification IDs are next hardened.
- W2-4: complete (self-reviewed, not yet pushed at write-time of this line — 1928c80). "شغل النهاردة" screen. DTO field names derived from `app/api/mobile/my-day/route.ts` directly (not the task plan): `MyDayFollowUp{id,lead_id,lead_name,phone,title,due_at,status}`, `MyDayColdLead{lead_id,lead_name,phone,company,days_since_contact}`, `MyDayCounts{follow_ups,going_cold}`, `MyDayData{follow_ups,going_cold,counts}` — matches the route's `followUpItems`/`goingCold`/`counts` mapping exactly (route.ts:257-277). `ApiClient.myDay()` mirrors the existing `get()` helper style (`appVersion()`/`ping()`). `MyDayScreen.kt` (209 lines): Loading (CircularProgressIndicator) / Failed (message + retry button, same shape as `UpdateActivity`'s Failed state) / Loaded (always renders BOTH sections regardless of emptiness — no separate "all clear" branch needed) states. Each section header shows "شown من total" via `counts` (route's own comment hints exactly this UX: "20 من 34"). Follow-up rows show lead name + overdue/due-at label (colored red when overdue) + the follow-up's own title; cold-lead rows show lead name + company + "N يوم بدون تواصل". Per-row call button (plain `Button` with text, NOT an Icon — the app has zero Compose-Icons usage anywhere, so no icon dependency was introduced) calls `Intent(Intent.ACTION_DIAL, Uri.parse("tel:$phone"))`, hidden when `phone` is null. Empty-state strings are per-section and directive: follow-ups-empty explicitly points the agent at the going-cold list below (cosette's real daily case: 0 follow-ups, 278 cold) instead of a blank panel; going-cold-empty is a distinct "all followed up" message. Entry point: `HomeScreen` gained an `onOpenMyDay` param + a new full-width button placed above "مزامنة الآن" (i.e. the daily-work action ranks above the sync utility action); `MainActivity`'s `else ->` branch (the already-logged-in state) grew a local `showMyDay` boolean toggling between `HomeScreen`/`MyDayScreen` — still a `when {}` over state, no nav library, `api` instance reused from `MainActivity`'s existing top-level `val api` (same pattern `LoginScreen` already uses). No manifest change — `ACTION_DIAL` needs no permission, and none was added. 2 new decode tests in `PayloadsTest.kt` (full shape + cosette's empty-follow-ups shape) guard the field-name contract against the exact silent-null failure mode `ignoreUnknownKeys` enables. Verify: `.\gradlew.bat test` BUILD SUCCESSFUL (39s, only the pre-existing unrelated `menuAnchor()` deprecation warning); `.\gradlew.bat assembleDebug` BUILD SUCCESSFUL (10s). `git status --short` showed only the 7 intended files touched (plus the same pre-existing untracked `AGENT-ONBOARDING.md`/`AGENTS.md`/`skills-lock.json` from other sessions, left alone). Committed via exact-path staging (never `git add -A`), not pushed (per task instructions). No version bump (W2-5 owns it).
- W2-4: complete (91fcc4e..aa254f6, review clean). MyDayScreen (209 lines) + DTOs + ApiClient.myDay + HomeScreen entry. DTO field names verified key-by-key against the real route (the silent-null risk) and pinned by 2 decode tests incl. cosette's zero-follow-ups shape. ACTION_DIAL only, no manifest/permission change, no nav library. Minors deferred: LazyColumn keys rely on an implicit fu_/sl_ prefix-uniqueness assumption (add a comment); duplicate string resource; no double-tap guard on retry.
- W2-5: complete (0d21d7f, self-reviewed, no separate reviewer pass for this task). Post-call outcome screen + v1.4.0 bump. DTO field names read directly from `app/api/mobile/call-outcome/route.ts` (not the brief): request = `lead_id`/`outcome`/`note`/`next_follow_up_at` (route.ts:90-93), response = `apiSuccess({ activity_id, follow_up_id, follow_up_error, deduplicated })` (route.ts:303) — `CallOutcomeRequest`/`CallOutcomeData` in `Payloads.kt` match key-for-key, pinned by 4 new decode/encode tests in `PayloadsTest.kt` (plain success, follow_up_error=true w/ null follow_up_id, request-without-optionals omits both nullable keys, request-with-both encodes them). `ApiClient.callOutcome(req)` mirrors the existing `post()` helper style. New `CallOutcomeActivity.kt` (167 lines, same shape as `QuickAddActivity.kt`'s 166): 3 outcome `FilterChip`s (interested/not_interested/call_again, values match route.ts:11's OUTCOMES tuple), optional multiline note field, optional "call again on…" section of 3 preset `FilterChip`s (tomorrow/+3 days/next week, deselectable) instead of a date-picker dialog — day-granularity only, so a full picker would add real weight to a screen this small. Presets computed by a new `DubaiTime.followUpPresetMillis(now, daysFromNow)` (day-start + N days + a fixed 10:00 Dubai hour, so every scheduled follow-up lands inside business hours without asking the agent to also pick a time) — 3 new exact-value tests added to `DubaiTimeTest.kt` reusing the existing test's proven `dayStartMillis` fact. Response handling: `ApiResult.Ok` covers BOTH plain success AND `deduplicated=true` (never treated as an error, per route.ts:67-79) with the same "تم تسجيل نتيجة المكالمة" toast; `follow_up_error=true` still finishes+cancels (the outcome itself saved) but swaps to a distinct warning toast naming the CRM as the fallback — never silently claimed as full success, never reported as a total failure. `Notifier.showMatched` (W2-3): content intent now opens `CallOutcomeActivity` with `lead_id`+`lead_name` extras instead of the web deep link; added a secondary `addAction` button (`notif_matched_browser_action` = "فتح في المتصفح") that still opens the lead in the browser via the original `ACTION_VIEW` intent — final layout is [tap notification body -> log outcome in-app] + [tap action button -> open lead in CRM browser]. `notif_matched_body` copy updated ("اضغط لفتح كارت العميل" -> "اضغط لتسجيل نتيجة المكالمة") to match the new primary action; noted but not fixed: tapping the browser action does not auto-cancel the notification (same platform limitation `IgnoreReceiver` works around with an explicit cancel — the browser action here is a plain `ACTION_VIEW` PendingIntent with no receiver to hook a cancel into; leaving the reminder up until the outcome is actually logged is arguably correct behavior anyway). Manifest: `CallOutcomeActivity` registered `android:exported="false"`. Version bump: `versionCode 4->5`, `versionName "1.3.0"->"1.4.0"` in `app/build.gradle.kts` (the only file in the wave allowed to touch this). Verify: `.\gradlew.bat test` BUILD SUCCESSFUL (45s, only the pre-existing unrelated `menuAnchor()` deprecation warning); `.\gradlew.bat assembleDebug` BUILD SUCCESSFUL (9s); `aapt2 dump badging` on the built debug APK confirmed `versionCode='5' versionName='1.4.0'`. `git status --short` showed exactly the 9 intended files (1 new + 8 modified) plus the same pre-existing untracked `AGENT-ONBOARDING.md`/`AGENTS.md`/`skills-lock.json` from other sessions, left alone. Committed via exact-path staging (never `git add -A`).
- W2-5: complete (ac6f6a8..80cb5a0, review clean). CallOutcomeActivity (167 lines, mirrors QuickAddActivity) + DTOs + ApiClient.callOutcome + matched-notification now opens it (browser link demoted to a secondary action) + versionCode 5 / versionName 1.4.0. Reviewer independently re-computed the Dubai preset arithmetic (tomorrow/+3d/+7d) and verified the DTO/enum match against route.ts field-by-field. flip-and-warn surfaced as a distinct warning toast; deduplicated treated as plain success. Minors deferred: no client-side note length cap (server 422s at 2000); sub-frame double-tap possible (absorbed by the 60s server dedup); label text duplicated in Kotlin and TS with no shared layer.
- W2-6: complete (E2E on the emulator + closure). Emulator `pyra_a15_test` (Android 16/API 36), debug `versionCode 5 / 1.4.0` -> `http://10.0.2.2:3000` (local `pnpm dev`, real Supabase; debug channel `pyra-calls-e2e`, so the fleet could not be reached). Test-account discipline: NEVER logged in as youssef/cosette (login deactivates a live phone's device key); used the disposable `e2e.upgrade` (temporarily `status='active'` + GoTrue unban + throwaway password) and RE-LOCKED it after (`status='inactive'`, original `deactivated_at` 2026-07-16T18:31:35.319398Z restored verbatim, GoTrue re-banned to 2126, password rotated to an unrecorded random value, its minted device key `ak_eQyCeMtlC1Tp3sUP` deactivated; post-relock login probe returns 401). Fixtures: 4 leads (`sl_ZZE2EW26{A,B,C,D}...`) + 1 follow-up. CHECK 1 (matched-call notification): `adb emu gsm call/accept/cancel` on `+971509990001` -> call log `type=1 duration=7` -> sync 200 -> notification «مكالمة مع ZZ E2E W26 Alpha» / «اضغط لتسجيل نتيجة المكالمة» + «فتح في المتصفح» action; tapping the body opened `CallOutcomeActivity` (confirmed by `dumpsys activity`) pre-populated «العميل: ZZ E2E W26 Alpha». DB: 1 `pyra_agent_calls` incoming/matched + 1 `call_logged` activity (source=device_sync, duration 7). CHECK 2 (my-day): both sections rendered — «متابعات مستحقة» «1 من 1» and «عملاء برد» «1 من 1» («بدون تواصل منذ 30 يوم») — and a `db:query` replaying the route's exact scope returned follow_ups=1/going_cold=1, an EXACT match; the fresh lead was correctly absent from going-cold and the lead with an open follow-up correctly excluded. «اتصال» opened `com.google.android.dialer` pre-filled with +971 50 999 0002 and the call log was byte-identical before/after — NO call placed (ACTION_DIAL only, no CALL_PHONE). CHECK 3 (outcome round-trip): all three outcomes each wrote exactly ONE `note` activity with `metadata.source='mobile_call_outcome'`; `interested` moved last_contact_at 11:47:25.551->11:51:10.773; `call_again`+«غدًا» created exactly ONE `pyra_sales_follow_ups` row (assigned_to=e2e.upgrade, due_at=2026-07-26T06:00Z = 10:00 Dubai tomorrow, reminder_at=due-30min, status=pending) and synced `leads.next_follow_up`; Arabic round-tripped with no mojibake. 60s dedup proven twice: (a) an identical POST fired seconds before the app's own submit -> both 200, exactly ONE row, app treated it as success (screen closed, no error); (b) a cleaner isolated run returned `deduplicated:false` then `deduplicated:true` WITH THE SAME activity_id, and the second request's different note text was correctly NOT written; that run also gave a crisp last_contact_at NULL -> 2026-07-25T12:02:28.913Z. CHECK 4 (ownership gate, security-critical): with a REAL device key, POSTs for (a) a throwaway lead assigned to youssef, (b) a GENUINE production youssef lead (`sl_GxLpDRKF0qEUWNOz`/omran) and (c) a nonexistent lead_id ALL returned 403 «لا تملك صلاحية الوصول لهذا الليد», indistinguishable (no existence oracle); SQL confirmed ZERO writes — omran kept its exact prior last_contact_at and activity count of 2, the throwaway lead stayed at NULL/0/0, and the 403 path wrote no `pyra_activity_log` row either. CLEANUP verified zero leftovers: deleted 4 leads, 8 `pyra_lead_activities`, 2 `pyra_sales_follow_ups`, 3 `pyra_agent_calls`, 6 `pyra_activity_log`; 0 notifications and 0 error_logs were produced by the run; app uninstalled from the emulator and its call log cleared; post-cleanup check confirms youssef + cosette still `active` with exactly 1 active device key each. Docs: `docs/CALL-TRACKING.md` gained «Agent-facing app surfaces (v1.4)» (the 3 surfaces + why ACTION_DIAL + the 10:00-Dubai preset arithmetic) and «Verified this task (2026-07-25 — W2 agent-app E2E)»; the two "verified by code inspection/SQL only" caveats on endpoints 9 and 10 were upgraded to point at the live run (the endpoint-9 production-scale SQL numbers for youssef/cosette are KEPT — those still cannot be measured from a device without knocking a real phone offline). NOT published: `pnpm app:publish` was NOT run and no release APK was built — v1.4 to the fleet is the owner's call at «ادمج».
- WAVE 2 COMPLETE (3d60bda..HEAD): agent-facing app = caller identity + «شغل النهاردة» + post-call outcome capture, all in v1.4.0 (versionCode 5), server endpoints `GET /api/mobile/my-day` + `POST /api/mobile/call-outcome`. E2E-verified on the emulator. NOT deployed and NOT published — awaits Abdou's «ادمج», then `pnpm app:publish <release apk> --app pyra-calls`.
- W2-6 + FINAL WHOLE-WAVE REVIEW (fable): complete (34de5ae..1f1be09). E2E all 4 checks passed on the emulator with the disposable e2e.upgrade account (matched notification -> outcome screen; my-day counts matched a SQL replay; dial opened the dialer without placing a call; 3 outcomes each wrote exactly one note + moved last_contact_at, call_again created one follow-up, the 60s dedup returned the same activity_id; ownership gate 403 on a genuine production youssef lead with zero writes). Cleanup verified zero leftovers, account re-locked, youssef/cosette never touched.
  Final review found 1 CRITICAL the per-task reviews structurally could not see (it lived in the W2-3 x server seam): the matched-call notification fired for ANY match, so 313 of 846 matched calls in 30 days (294 zero-second) would raise "log the outcome" prompts for calls nobody answered - re-opening BY HAND the fake-contact channel the urgent-fix wave had just purged. Fixed by mirroring isConnectedCall on the local entry. Also fixed: cross-agent matched calls whose action always 403s (server now echoes `owned`; app treats null as owned so an old server can't silence it) and my-day's unbounded .not(id,in,...) exclusion (moved to a JS filter before the slice; counts now post-filter; cap alarm kept) - the same URI-too-long class that killed lead-idle-check for 11 days, and youssef's set grew 127->152 during this very wave.
  WAVE 2 CODE-COMPLETE. NOT deployed, NOT published. Ship order at «ادمج»: server to main FIRST, verify, THEN build the signed v1.4.0 APK and `pnpm app:publish --app pyra-calls`.
- WAVE 2 DEPLOYED + PUBLISHED 2026-07-25 («ادمج وانشر»): 3d60bda..49bc775 -> origin/main; Coolify deploy verified live (my-day + call-outcome + calls report all answering 401 = present and gated; site 200). Signed v1.4.0 (versionCode 5) built AFTER the server was confirmed live, SHA-256 5edb087c9fb7bb2e9c1105eebab39528787bf7ab8be072cf517147840e1fa696 (7.86 MB), published to the pyra-calls channel (v4 deactivated). Monitor watching app_version_code -> 5 on both phones and any pyra-calls-app error rows.

## PROJECT: Public quote signing (plan docs/superpowers/plans/2026-07-27-public-quote-signing.md, base 7889f75)
- Task 1: complete (7889f75..01163e3, review clean). 7 pure modules + 57 tests, all RED->GREEN. Spec exact, nothing extra. KEY CORRECTION for later tasks: `dubaiDayKey` lives at `lib/utils/format.ts:107`, NOT `lib/utils/date` as the plan guessed. Minors deferred to final review: (1) quoteContentHash sorts only top-level keys, so nested item objects built in a different property order would hash differently — matters once wired to real DB rows; (2) two public-payload tests assert against the module's own constant (the 10 "never leaks" literal-name cases carry the real guarantee); (3) classifyLinkState does not validate Date.parse, a malformed expires_at yields NaN and falls through to 'valid'.
- Task 2: complete (01163e3..5a80b0f, incl. fix migration 055, re-review clean). Migrations 054+055 APPLIED TO PRODUCTION and recorded. Verified: link_cols=12 link_indexes=4 rls_on=t grantees=postgres,service_role,supabase_admin (no anon/authenticated) quote_new_cols=12 sig_trigger=1. NOTE: the Task 2 implementer subagent died from an API error after applying but before committing; controller verified the applied state and the trigger in both directions itself, then committed. Review found I-1: 054's trigger guarded only the 4 legacy signature columns, leaving the 9 new attestation/evidence columns forgeable by any authenticated employee — closed by 055, which also flipped the trigger fn to SECURITY INVOKER, revoked its PUBLIC/authenticated EXECUTE, and added CHECKs for jsonb_typeof(signed_snapshot)='object' and signed_evidence_size>=0. 24 assertions passed incl. an ordinary quote edit run AS the `authenticated` role after the revoke (proving employee quote editing still works — trigger EXECUTE is checked at CREATE TRIGGER time, not per fire).
  BINDING CONSTRAINT FOR TASK 7 (review finding I-2): idx_document_links_one_live is `(entity_type, entity_id) WHERE revoked_at IS NULL` — it ignores expires_at, so an EXPIRED-but-unrevoked link still blocks a fresh insert with 23505. The mint path MUST revoke-then-insert (or ON CONFLICT upsert), never a bare insert.
  Residual, out of scope and unchanged: pyra_quotes still has RLS off with `authenticated` holding full DML (Gap #3 / decision D-2) — total/status/items remain forgeable, and the FIRST write of a signature is guarded only by the route's permission gate.
- Task 3: complete (5a80b0f..f7aeb49, incl. fix round, re-review clean). PATCH /api/quotes/[id] now 422s any commercial-content edit on a signed/invoiced quote. The PLAN'S CONTENT_KEYS LIST WAS INCOMPLETE — review found 2 Critical bypasses (`vat_rate`, a body key distinct from tax_rate that the real edit UI sends and that recomputes total; `client_id`, which overwrites client_name/email/phone/company none of which were in the body), and the fix round re-derived the list from the handler and found 2 more (`project_name`, `estimate_date` — both render on the signed PDF). Final CONTENT_KEYS = 21 entries; an independent re-derivation by the re-reviewer matched exactly, no key missing. `status` deliberately unlocked (transitions must work); `updated_at` is server-set.
  Minors deferred to final review: (1) the lock guard runs BEFORE the scope/ownership checks, so an out-of-scope caller holding quotes.edit gets 422 (leaking that the quote is locked) instead of 403 — pre-existing ordering, cheap to fix by moving the guard below them; (2) the fix round's report overstated its verification methodology (round 2 used a weaker i18n mock that echoed the raw key rather than resolving the catalog — catalogs independently confirmed correct by the reviewer).
  CONTEXT CORRECTION from the re-reviewer, useful later: the live "convert to invoice" action does NOT go through this PATCH — it hits POST /api/invoices/from-quote/[quoteId], which does a direct ungated status update. Signing likewise goes through POST /api/portal/quotes/[id]/sign.
- Task 4: complete (f7aeb49..ba3c055, incl. fix round, review clean). lib/quotes/sign-quote.ts is now the single sign core; the portal route consumes it with no user-visible change (reviewer reconstructed the pre-refactor route with `git show` and diffed behaviour itself — race guard, 500KB cap, expiry guard, email, the single notify(), activity rows all survived). Reviewer independently traced that no status-graph path can leave a sent/viewed row carrying a signature, so the core's UPDATE is structurally always a first write and cannot trip the 055 append-only trigger — worth re-checking if an admin "un-sign/reopen" action is ever added. Fix round: the refactor had swallowed loadErr/updErr and logged the literal string "db_error"; now returns the error alongside the reason and the route logs via logError(). Proven by forcing the append-only trigger on a throwaway and showing the real P0001 message in the log.
  Intentional, documented behaviour shift inherited from Task 1: a quote that is BOTH already-signed AND past expiry now reports "already signed" instead of "expired" (status checked before expiry). Reviewer judged it more correct.
  Minors deferred to final review: (1) the route's `wrong_status` message is built from a stale prefetch, so a status that changes mid-request could name the old status (cosmetic, ms-wide window, cannot affect the already-signed/race paths); (2) signQuote() itself has no unit test — Task 6 will be its second consumer with no regression net beyond manual verification.
  BRIEF DEFECT NOTED: task-4-brief's prose header described the return as `SignBlockReason | 'race' | 'db_error'` + a `SignedQuoteRow` type, but its own code snippet returns `SignFailure` (adds 'signature_too_large') + `Record<string, unknown>`. The code snippet is what shipped and what Task 6 must type against.
- Task 5: implemented (ba3c055..58e4a56), review APPROVED with 3 Important + minors; FIX ROUND IN FLIGHT at time of writing. Public /d/<token> page (RSC, service-role read, no API route on the read path), middleware one-liner in the API-gate chain only, QuoteDetailView moved components/portal/quotes -> components/quotes and translated (~19 strings), publicdoc namespace in both catalogs.
  TWO FILES OUTSIDE THE BRIEF, both load-bearing and both verified safe by the reviewer:
  (a) `i18n/request.ts` — getRequestConfig took no params so `getTranslations({locale})` silently rendered the COOKIE locale; the recipient-language feature was a no-op without this. Reviewer proved the change is INERT for every existing authenticated route: no `setRequestLocale` call sites anywhere, no next-intl routing middleware, nothing sets `x-next-intl-locale`, and `getLocale()` in app/layout.tsx is byte-identical. It also CORRECTS a pre-existing bug at app/api/crm/dashboard/ai-insights/route.ts:61-62 where `message_ar` was rendering in the reader's cookie locale.
  (b) `next.config.ts` — a Server Component cannot set response headers in App Router; added a `headers()` block scoped to `/d/:token` (path-to-regexp, single segment, cannot match /dashboard). X-Frame-Options DENY still inherited from the catch-all.
  Reviewer also confirmed for Task 6: `mutateAPI` uses a bare fetch (mode "cors"), so the real Origin header survives `Referrer-Policy: no-referrer` and the middleware CSRF gate will pass.
  Minors deferred to final review: client-supplied `x-next-intl-locale` is now honoured on every route (self-scoped, nothing sets it, undocumented); the app-wide locale change shipped with zero test coverage; QuoteDetailView's `isSigned` now derives from status so a signed-then-cancelled quote shows neither signature nor sign button; the public page serializes the FULL merged message catalog twice (~500KB AR) on the one page most likely opened on mobile data; `detail: any` retained on QuoteDetailView plus ~20 String()/as coercions in the wrapper's handleDownload.
- Task 5: COMPLETE (ba3c055..6196c40, incl. fix round). Fix round was finished BY THE CONTROLLER, not the fix agent — that agent stalled repeatedly on `pnpm build` (environmental OOM, ~3.6GB free of 16GB) and then on dev-server startup, and left its work uncommitted with its throwaway rows still in the DB. Controller verified everything itself and committed.
  IMPORTANT GOTCHA FOR LATER TASKS: a dev server started BEFORE a new route file exists serves 500 for that whole route forever. Both /d/<valid> and /d/<unknown> returned 500 on the stale server and 200/404 on a freshly started one. If a route 500s during verification, restart dev before diagnosing the code.
  Controller-run verification (clean dev server, cookie-less curl): valid link 200 with quote rendered; unknown token 404; revoked link 404 IDENTICAL to unknown in status, header set AND rendered text; /dashboard still 307; not-found page contains zero /dashboard or /portal links; PII sweep found ZERO occurrences of client_email/client_phone/client_address/signed_ip/bank_details/license_no in the public HTML; security headers present (X-Robots-Tag noindex, Referrer-Policy no-referrer, X-Frame-Options DENY inherited). Throwaways deleted, 3 real quotes intact, the live customer quote still `sent`.
  OPEN RISK, NOT A CODE DEFECT: `pnpm build` has NOT been run successfully since Task 4 — it OOMs on this machine. `pnpm run check` + `pnpm test` are green. A successful production build is REQUIRED before any deploy.
- Task 6: complete (6196c40..1e8c58a, incl. fix round; controller committed the fix round because the agent left it uncommitted). POST /api/public/quotes/[token]/sign. Reviewer confirmed the hard parts independently: ONE sign path (zero duplicated guards — every signature-column write in the repo is still only sign-quote.ts), no reachable second write against the 055 append-only trigger, invalid states collapsed into one response with the locale resolved only AFTER the link is known real (so it cannot be fingerprinted), DB errors never masquerade as invalid links, and D-1 verified by inspecting BOTH the payload and quote-pdf.ts — the emailed PDF cannot carry the IBAN. Email recipient is the STORED client_email, never signer input, so a stranger cannot redirect the PDF to themselves.
  Fix round closed 3 Important: (1) signed_by was unbounded on an append-only column — garbage would have been permanent; now capped with a matching input maxLength; (2) already_signed returned 422 and the client rendered an unclearable "signing failed" on a quote that WAS signed — now returns the same 200 alreadySigned marker as `race`; (3) the live token was persisted cleartext to pyra_error_logs via request_path (metadata was redacted, the adjacent column was not) — the four logError calls no longer pass `request`; verified a forced DB error now stores request_path null. Also fixed the blank already-signed date and the portal/public notification parity gap (public now calls notifyQuoteSigned too).
  Minors deferred to final review: documentLinkViewLimiter is declared but unwired (dead until Task 7); emailed vs downloaded PDF still differ on client_email/phone/address (email goes to the stored client address, so own-data); content_hash guard fails open and the column is nullable — tighten to NOT NULL once Task 7's mint path exists; `as Parameters<typeof generateQuotePDF>[0]` cast disables structural checking on the emailed payload; client-preference lookup error swallowed (silently downgrades an EN customer to AR); no route-level test — only the pure cores are covered.
- Task 7: complete (1e8c58a..6bc8a5c, incl. fix round, re-review clean). Mint/read/revoke at /api/quotes/[id]/link + useDocumentLinks + PublicLinkDialog + row-dropdown action + auto-revoke on content PATCH. Before this NOTHING could create a link, so the whole public-signing feature was unreachable. Reviewer independently verified: token never selected into GET/list/error/activity bodies AND pyra_document_links grants zero rows to anon/authenticated at the DB layer; content_hash is computed from the SAME function and field set the public page recomputes (so the S-8 guard now actually fires instead of failing open); auto-revoke reuses Task 3's attemptedContentKeys so a status-only PATCH does not kill a live link; expiry math matches dubaiDayKey's inclusive-same-day semantics.
  Fix round closed 1 Important: mint did revoke-then-insert with NO rollback, so a transient non-23505 insert failure permanently killed a link already sitting in a customer's WhatsApp while the agent saw only "failed to create link". Now captures the revoked row id and restores revoked_at/revoked_by to NULL, mirroring the rollback convention in app/api/quotes/route.ts:373. Forced honestly with a 23502 NOT NULL violation (confirmed != 23505) and the previously-live link verified back to live. The 23505 race branch is provably untouched (no un-revoke — another request legitimately took over).
  Minor deferred to final review: if the restore itself collides with a genuinely concurrent successful mint (23505 on the restore), the caller is told "contact support immediately" though a valid live link exists. Non-destructive, self-healing on refresh, needs two compounding failures.
  Test-data hygiene verified by the reviewer: e2e.upgrade is inactive + banned to 2126 + 0 sessions + 0 refresh tokens; pyra_document_links empty; 3 real quotes intact; the live customer quote untouched.
- Task 8: complete (6bc8a5c..d1d3bfd, incl. fix round, review approved). Offline signature attestation: upload route + signed-URL route + OfflineSignDialog + hook + dropdown action. Reviewer verified live against prod: signed_offline_by is hardcoded from auth.pyraUser.username with every FormData field read individually (never spread), so no body value can reach it; the append-only trigger is architecturally unreachable (app-level status guard + `.in('status', ATTESTABLE_STATUSES)` conditional update) so "already signed" is always a clean 422, never an opaque 500; pyra-private's real file_size_limit measured at exactly 10485760 = MAX_EVIDENCE_BYTES; signed_evidence_path never leaves either route except as a 5-min signed URL; extension always from the MIME map; no path traversal (path built only from a DB-matched quote id + MIME-derived extension).
  Implementer found and fixed a real bug during verification: `request.formData()` itself throws on an oversized body BEFORE any app-level size check, which was surfacing as a bare 500.
  Fix round closed 2 Important + 1 Minor: storage orphan-cleanup failures were fire-and-forget (Supabase .remove() RESOLVES with {error}, it does not throw, so the surrounding try/catch caught nothing) — now logged, forced honestly with a NUL-byte path; absent Content-Length was Number(null ?? 0) === 0 and skipped the pre-check entirely — HONEST RESULT: the chunked/no-header case still buffers first (Next's undocumented ~10MB middlewareClientMaxBodySize truncates it and the parse guard catches the malformed multipart), documented as a mitigation not a streaming rejection; signed_at accepted 2026-13-40 — now calendar-validated.
  Minor deferred to final review: useQuoteOfflineEvidence has zero call sites (deliberate scaffolding for a future evidence-viewer UI, outside this task's file list).

## PAUSED 2026-07-28 at owner's request — he needed to issue a real quote.
State: Tasks 1-8 complete and reviewed. Tasks 9 (honest delivery status) and 10 (docs + MIGRATED_PATHS + locked decisions) remain, then the final whole-branch review.
Production is UNAFFECTED: 17 commits sit local and unpushed; origin/main is still ffb172f. Migrations 054+055 ARE live on the prod DB but are additive, and the append-only trigger was proven not to block ordinary quote editing.
BLOCKER BEFORE ANY DEPLOY: `pnpm build` has not succeeded since Task 4 — it OOMs on this machine (~3.6GB free of 16GB). check + tests are green.
- Task 9: complete (d1d3bfd..b014590, review approved, no fix round needed). Send route now persists delivery_status/delivery_detail/delivery_checked_at via the reused deriveDelivery helper; amber badge in the quotes list for no_email/not_delivered only. Reviewer confirmed: the `sent` status flip is byte-for-byte untouched; the delivery write runs strictly AFTER it and can never turn a successful send into an error; the "email throws" path is impossible (sendQuoteSentEmail always resolves to a boolean); delivery_detail only ever holds the address on the delivered branch and client_email was already in QUOTE_FIELDS so no new exposure; a genuine re-send overwrites the columns unconditionally so the badge clears.
  EXTRA beyond the brief, correctly identified as a gap in the brief itself: lib/supabase/fields.ts needed the three columns added to QUOTE_FIELDS or GET /api/quotes would never return them.
  The implementer correctly REFUSED to log in with a throwaway account (hard-prohibited action) and verified via service-role scripts + a temporary RTL render test instead. The one real email sent during verification went to elharm.marketing@gmail.com — the OWNER'S OWN inbox, not any customer.
  Minors deferred to final review: a narrow window where the delivery write itself fails leaves the badge absent though the toast correctly warned (logged via logError, not silent); no permanent regression test covers the route's persistence call or the badge's conditional render.
- Task 10: complete (b014590..283febd). Docs only. Added 'app/d' + 'app/api/public' to MIGRATED_PATHS (did NOT break check — both trees were written as if already gated); documented pyra_document_links + the 12 new pyra_quotes columns in DATABASE-SCHEMA.md; wrote the locked-decision section in docs/decisions/finance.md (D-1..D-4, S-10, S-11, the revoke-then-insert mint rule, server-derived signed_offline_by) with ONE index line in CLAUDE.md; extended the CLAUDE.md architecture block.
  The implementer caught an inaccuracy in its own brief while checking against code: the append-only trigger guards 9 of the 12 new columns, not all 12 — the 3 delivery_* columns are deliberately excluded. Corrected in both docs before commit.
ALL 10 TASKS COMPLETE (7889f75..283febd). Final whole-branch review pending. NOT pushed, NOT deployed.

## FINAL WHOLE-BRANCH REVIEW (opus) — 2 Critical + 8 Important, all cross-task seams
C-1 PERMISSION GAP (owner decision taken): every new action gated on `quotes.edit`, which the live DB role `Sales` does NOT hold — cosette and youssef (the only active agents, extra_permissions=[]) could not use the feature at all. Owner chose a NARROW new permission `quotes.share_link` rather than granting quotes.edit (which would also unlock quote editing + email sending). TO DO after the final fix round lands: define it in lib/auth/rbac.ts, accept it on the 3 route gates + the dropdown, and add it to the Sales role row.
C-2 sign-quote.ts:61 items error unchecked -> a transient failure permanently freezes signed_snapshot with ZERO line items on an append-only column, while returning {signed:true}. Portal path fully exposed (it never fetches items itself).
I-1..I-8: uncapped signed_by on the offline path; public page throws without logError and has no error boundary (customer sees Next's raw crash screen); documentLinkViewLimiter declared but never wired; the public page serializes the FULL message catalog twice (~1MB on mobile data); a live link becomes unrevokable once the quote is signed (dialog only renders for sent/viewed) and never expires if expiry_date is null; Feature 2's evidence columns have NO reader anywhere in the UI; app/api/external/quotes/[id] is a SECOND quote-PATCH route with no CONTENT_KEYS lock and no transition check; notifyQuoteSigned uses the anon client so the agent/admin email is a silent no-op on BOTH portal and public paths.
Confirmed sound end-to-end: no signature forgery/replay/redirect path found; signQuote is the only signature writer; D-1 (no IBAN on the public PDF) verified from PUBLIC_QUOTE_FIELDS through quote-pdf.ts:430; live prod schema matches 054+055 exactly incl. grants, RLS, CHECKs, trigger ACL; i18n parity exact (publicdoc 19/19, finance 1122/1122, api 556/556); zero RTL violations; one sanctioned raw fetch.
BUILD NOW SUCCEEDS (exit 0, 578 routes, all 5 new routes in the manifest) — the earlier OOM was environmental, resolved by stopping the dev servers first.
OWNER AUTHORIZED DEPLOY ("ولما تخلص انشر") — push to origin/main after the fix round + the share_link permission, gated on check + test + build + a fetch to check for concurrent work.

## DEPLOYED 2026-07-28 (owner said «ولما تخلص انشر»)
Pushed ffb172f..e0eeac2 -> origin/main (21 commits). Gates before push: check clean, 845/846 tests (1 pre-existing unrelated), build exit 0 with all 5 new routes in the manifest, fetch showed no concurrent work.
VERIFICATION GOTCHA WORTH REMEMBERING: my first "deployed" check used a 404 on /d/<fake token> as the signal — but a route that does not exist ALSO 404s, so it proved nothing and I reported the deploy as live ~5 minutes too early. The decisive marker was the custom not-found copy (publicdoc.invalidTitle) appearing in the body; the old build served the generic app 404 with /dashboard links. Coolify took ~5.5 minutes. Use a content marker unique to the new build, never a status code that both builds produce.
The missing X-Robots-Tag/Referrer-Policy I briefly suspected as a proxy override was simply the old build still being served — after the real rollout, /d/<token> returns Referrer-Policy: no-referrer, X-Robots-Tag: noindex, nofollow, X-Frame-Options: DENY. Confirmed identical on a local `pnpm start` first, which is what ruled out a config bug.
Post-deploy: dashboard 307, login 200, portal 307; 4 real quotes unchanged; 0 error_logs rows in the 15 minutes after rollout.
STILL OPEN (final-review items accepted as fast-follow, NOT blockers): offline-signature evidence has no reader in the UI (I-6) — an admin cannot see who attested or open the evidence file without hitting the API by hand; a live link cannot be revoked once the quote is signed and never expires if expiry_date is null (I-5); notifyQuoteSigned uses the anon client so the agent/admin "quote signed" EMAIL is a silent no-op on both portal and public paths (I-8, pre-existing); app/api/external/quotes/[id] is a second quote-PATCH route with no CONTENT_KEYS lock (I-7).

## FOLLOW-UP SHIPPED: signature evidence viewer (3b55c70, pushed to main 2026-07-28)
Closes final-review finding I-6 — the offline-signature evidence had no reader in the UI. New SignatureDetailsDialog (how signed / who / when / who attested / open evidence), plus five provenance columns added to QUOTE_FIELDS: signature_source, signed_offline_by, signed_offline_at, signed_evidence_mime, signed_evidence_size. `signed_evidence_path` deliberately NOT added — it never leaves the evidence route, which mints a 300s signed URL. `signed_snapshot` deliberately left out of the shared list (large jsonb on every list row).
Reviewer verified exposure scoping: portal and public routes use their own hand-maintained field lists and do NOT gain the five fields; the external/n8n surface does, but it already exposed signature_data/signed_by/signed_ip. Null signature_source (QT-0003, signed before the feature) renders an honest "unknown" state, not a guessed label.
Open follow-up from that review (pre-existing, not worsened): `signature_data` is stored with only a type+length check — no data:image prefix validation, no magic bytes — and is now rendered at a second `<img>` site. Browsers sandbox SVG loaded via <img>, so not currently exploitable, but lib/quotes/sign-quote.ts should validate the prefix before persisting.

## DEPLOY VERIFICATION NOTES (two flawed checks I made, corrected)
1. A 404 on /d/<fake token> does NOT prove a deploy — a non-existent route 404s too. Use a content marker unique to the new build.
2. Matching the local .next/BUILD_ID against production is meaningless — Coolify runs its own build and Next generates a fresh random build id each time. They can never match.
/api/health returns only a static "3.0.0" with no commit or build info, so a deploy of authenticated-only surfaces cannot be confirmed from outside without logging in. Adding the git SHA to that endpoint would make future deploys verifiable.
Post-deploy state: login 200, dashboard 307, /d/<fake> 404 serving the custom invalid-link page with ZERO clickable links into the product (the /dashboard string appears only inside shared JS bundles, not in any href), 0 error_logs rows in the 20 minutes after the push.

## FOLLOW-UP: quote-signed notification actually reaches the agent (694f72f, deployed 2026-07-29)
Closes final-review finding I-8, but the diagnosis turned out to be TWO defects, only one of which was code:
1. `lib/email/notify.ts` did all six of its "which internal address do we notify" lookups through `createServerSupabaseClient()` (the SESSION client). Gap #3 Phase 0 revoked `anon` globally, so with no session those lookups return 42501 permission denied and the notifier silently sent nothing — verified live by curling PostgREST as anon against pyra_users. Several callers are fire-and-forget IIFEs running AFTER the response is sent, where `cookies()` has no request to read at all. All six switched to createServiceRoleClient().
2. THE HALF THAT MADE IT MOOT: production has 11 users, only 2 with an email address, and **0 of the 4 active users**. Every active user DOES have a phone. So the email had nowhere to land regardless of the client fix — my earlier "this is an hour of work and the agent gets notified" was wrong on the second half.
So the real fix for THIS business is WhatsApp, not email: sendWhatsAppToUser() was already in use by 7 routes. Wired into BOTH sign paths (public + portal) so they cannot drift, fire-and-forget, message carries quote number + signer + amount and deliberately NO link or token (a forwarded WhatsApp message must never reach the signing link). The email path is kept so it starts working the moment an address is filled in.
Routing verified at the data layer: a cosette-created quote routes to her profile phone via the sole connected instance `pyraai` (no pyra_agent_whatsapp_settings override exists). NOT delivery-tested — sending to a real staff phone was out of bounds.
TRAP FOUND: pyra_quotes.total comes back from PostgREST as a JSON STRING ("11997.00"), so formatCurrency would silently render 0.00 AED without a Number() cast. Applied at both new call sites.
Watch item: pyra_whatsapp_instances.pyraai has status='connected' but last_connected_at IS NULL — check this first if signature WhatsApps are ever reported missing.
Deploy confirmed by built_at changing on /api/health (16:37:18Z -> 07:18:38Z) — the first deploy in this project verified by evidence rather than by guessing at a user-visible string.

## PROJECT: Call-attempt cleanup wave (CA), Task A1 — quick-add last_contact_at gate + backfill (2026-07-29)
Root cause: the earlier `isConnectedCall` fix (urgent-fixes wave) covered the calls/sync path and the retro-link activity gate in `app/api/mobile/leads/route.ts`, but missed the lead-INSERT itself — line 160 stamped `last_contact_at: call.called_at` unconditionally, so a lead created from the quick-add unknown-number prompt after an unanswered dial was born looking freshly contacted. The `call` row fetched there also didn't select `direction`/`duration_seconds`, so those had to be added to the SELECT before `isConnectedCall(call)` could be applied. Fixed: `last_contact_at: isConnectedCall(call) ? call.called_at : null`. No other line in that file writes `last_contact_at` (`retroLinkCalls` only writes `call_logged` activities + updates `pyra_agent_calls`, never touches the lead row).
Backfill (`scripts/backfill-quickadd-last-contact.ts`, modelled on `backfill-zero-duration-contact.ts`, same snapshot-first/dry-run-default/batched-VALUES discipline): selected by the RULE (last_contact_at exactly minute-matches one of the lead's own 0-second non-missed calls), not a date cutoff. All-time candidate set = 46; 41 actually needed an update (40 -> NULL, 1 -> an earlier genuine timestamp); 5 were already correct no-ops. Of the 41, all 41 were created on/after 2026-07-25 and 0 were older stragglers — exactly matching the measured "41 leads" ground truth, confirming the bug has no earlier history. Applied; after-count of leads still matching the poison condition dropped from 46 to 5 (the already-correct no-ops, which legitimately still match the raw condition but need no write).
GOTCHA CAUGHT DURING SPOT-CHECK: `sl_GHa_kRd2m4BsBbcY` (hamza), named in the brief as "must end NULL", was NOT in the candidate set at all — a genuine manual "note" activity (logged by youssef live in prod at 07:33 UTC, AFTER the bug's last_contact_at stamp at lead creation) had already bumped last_contact_at to a real touchpoint via the documented `app/api/crm/leads/[id]/activities/route.ts` behaviour ("every manual activity is a touchpoint"). The ground truth in the brief was accurate when measured but went stale from concurrent live usage; forcing hamza to NULL now would have destroyed a genuine contact record. Correctly left untouched. Substituted two other named-in-constraints leads for the spot-check instead (`sl_nOG8k9Hi1Me4HZPf` miramar: single 0-sec call -> NULL; `sl_lPzPGl64pPEiNkJ4` lisha: 0-sec call at 09:15:46 + genuine 42s call at 09:16:30 -> recomputed to 09:16:30), both verified exactly against the snapshot's before/after values.
Verify: `pnpm run check` 0 errors, `pnpm build` exit 0, `pnpm test` 850/851 (the 1 failure is the pre-existing `atomic-task-write-routes.test.ts`, unrelated, not worsened).

## Call attempts + update enforcement wave (2026-07-29) — plan docs/superpowers/plans/2026-07-29-call-attempts-and-update-enforcement.md
- CA-A1: complete (60300b6..84f16e2, review clean + independent data verification). Gated leads/route.ts:166 with isConnectedCall; backfill matched 46 by rule, updated 41 (40->NULL, 1->genuine later call). hamza's non-NULL value independently confirmed to trace to a genuine manual note by youssef (last_contact_at is 6ms after the note) - do NOT force it to NULL. The 5 remaining raw matches are same-minute redial false positives, verified benign.
  TWO FOLLOW-UPS FROM THE REVIEW: (1) prod keeps minting poisoned leads until this branch deploys - 2 new ones appeared 20 min after the apply; RE-RUN the backfill after «ادمج». (2) retroLinkCalls() logs a call_logged activity for a connected retro-linked call but never bumps last_contact_at (only the live-sync path does) - folded into CA-B1 since it edits the same function.
- CA-B1: complete. New `call_attempt` pyra_lead_activities type for a matched-but-unanswered dial (`direction != 'missed' && duration_seconds === 0`), written in both `sync/route.ts` (else-if branch alongside the existing call_logged branch) and `retroLinkCalls()` in `leads/route.ts` — visible on the timeline, never bumps last_contact_at. `activity_type` is a bare varchar with no CHECK constraint, so no migration needed.
  Excluded the new type from FOUR "last touched" consumers (not three — grepped the repo per the brief and found a 4th): `lead-idle-check/route.ts` (batched activities SELECT), `deals-at-risk/route.ts` (batched activities SELECT), `ai-insights/route.ts` (idle-deals "has recent activity" lookup), and the customer dossier's health-score recency query (`app/api/crm/customers/[lead_id]/dossier/route.ts`'s `lastActivityRes` — feeds the 30%-weighted recencyScore). All four got `.neq('activity_type', 'call_attempt')` with the same comment. Deliberately did NOT filter: the lead-detail timeline fetch, the leads-list activity_count/last_activity_type enrichment, or the CRM dashboard recent-activity feed — those are plain visibility surfaces, not "was this lead touched" gates, and a call_attempt is meant to show there. Also left the dossier's 30-day engagementCount unfiltered as a judgment call (it measures agent effort/volume, not "was touched" freshness, and isn't in the brief's named example) — flagged for the owner in case that's wrong.
  A1-review fold-in: retroLinkCalls() now advances last_contact_at to the newest CONNECTED retro-linked call's called_at, but only FORWARD (reads the lead's current last_contact_at first; only updates if the new value is strictly newer) — a retro-link batch can carry old historical calls, so an unconditional overwrite could have regressed a lead's last_contact_at into the past.
  Reconciliation (last 7 days, by pyra_agent_calls.called_at): 488 total matched (28 incoming answered + 262 outgoing answered + 188 outgoing unanswered + 10 missed) = 290 answered + 188 unanswered + 10 missed, exact. Existing call_logged activities (by created_at) in the same window: 291 — 1 more than the 290 called_at-window answered count, attributable to a retro-linked historical call whose activity was created inside the window but whose called_at falls outside it (not a bug). 0 call_attempt rows exist yet, as expected — this code hasn't run against a live device sync yet; the 188 unanswered calls already in the DB do NOT retroactively get a call_attempt row (no backfill requested for this task, unlike A1).
  Verify: `pnpm run check` 0 errors, `pnpm build` exit 0, `pnpm test` 850/851 (same pre-existing atomic-task-write-routes.test.ts failure, not worsened).
- CA-B1: complete (f5eff62..314eae1, review clean). call_attempt written for matched 0-second non-missed calls at both sites (sync + retro-link), activity_id back-filled, never bumps last_contact_at. THE TRAP HELD: 4 recency consumers filtered (the 3 named + the dossier health-score recency the implementer found), each .neq() placed INSIDE the chunk()-batched loop (outside would be inert); reviewer independently grepped for a 5th and found none. Folded-in A1 follow-up done: retroLinkCalls now bumps last_contact_at for a genuinely connected retro-linked call, forward-only, guarded by a fresh SELECT. engagementCount deliberately left unfiltered (volume axis, not recency - reviewer concurred). Minors deferred: no regression test / shared constant locking the exclusion (a 5th consumer could omit it silently); theoretical concurrent-retro-link forward write.
- CA-B2: complete (2fe6260). Rendered `call_attempt` on the lead timeline: `PhoneMissed` icon + muted amber tone (`bg-amber-500/10 text-amber-700 dark:text-amber-300`, distinct from call_logged's sky tone) in `components/crm/activity/activity-item.tsx`'s VARIANTS map, plus a direction-aware title case (`titles.callAttemptOutbound`/`callAttemptInbound`, no duration — there isn't one). GAP FOUND: CA-B1 wrote the new activity_type into the DB but never added it to the canonical unions — `types/database.ts LeadActivityType` and `lib/constants/statuses.ts LEAD_ACTIVITY_TYPES`/`LEAD_ACTIVITY_LABELS_AR` — so `tsc` failed the moment the exhaustive `Record<LeadActivityTypeNew, ...>` maps got a `call_attempt` key. A SECOND exhaustive consumer surfaced the same way: `components/crm/dashboard/dashboard-activity-feed.tsx`'s `ACTIVITY_ICON` (full `Record`, not partial) — the CRM dashboard recent-activity feed shows activities across all leads unscoped by type, so a call_attempt row would land there too; gave it the same PhoneMissed + amber treatment for consistency. All four files backfilled (2 type unions + the 2 renderers) plus the matching i18n keys (`messages/{ar,en}/statuses.json` leadActivity fallback label, `messages/{ar,en}/crm.json` titles) — `__tests__/status-labels.test.ts` pins the AR labels catalog-verbatim so both had to move together.
  Verify: `pnpm run check` 0 errors + i18n:check clean, `pnpm build` exit 0, `pnpm test` 768/769 (1 pre-existing `atomic-task-write-routes.test.ts` failure, confirmed identical assertion in isolation — not worsened), `eslint` clean on all 4 changed source files.
  DB reality check: 0 `call_attempt` rows exist yet (CA-B1 not deployed) — confirmed live lead for the owner to check once deployed instead: `sl_1VxY5iIkqhLcemy4` ("yummmy tummy restaurant", assigned youssef) has 2 answered + 2 unanswered calls in `pyra_agent_calls`, which will produce one `call_logged` + one `call_attempt` row on the timeline after the next device sync.
- CA-B2: complete (bde1560..e2f8200 + tone fix, review clean). PhoneMissed icon + title cases + i18n both catalogs. Scope expansion verified compile-forced by the reviewer (CA-B1 had left call_attempt out of types/database.ts + lib/constants/statuses.ts, breaking the exhaustive icon maps; statuses.json edits were test-forced). Reviewer's Minor fixed inline: the tone was byte-identical to `note` — switched both surfaces to rose so an attempt reads differently from a connected call AND from a note. Owner can watch lead sl_1VxY5iIkqhLcemy4 (has both an answered and an unanswered call) once deployed.
- PART A + PART B COMPLETE (60300b6..HEAD), server-only, NOT deployed. Part C (update enforcement + app v1.5.0) not started.
- PARTS A+B DEPLOYED 2026-07-29 («انشر الأول»): 694f72f..403d711 -> origin/main, Coolify build completed (health built_at 10:04:57Z; note the health endpoint reports commit "unknown" — the commit env var is not wired in this Coolify build, so deploy confirmation relies on built_at + behaviour, worth wiring properly some day). Gap-window backfill re-run post-deploy as the review required: 7 matched by rule, 2 needed an update (both -> NULL), 5 already-correct same-minute false positives. Down from 41 in the first sweep — consistent with the gate now being live. Monitor armed for the first live call_attempt row + a regression watch for any newly-created lead still stamped from an unanswered dial.
- CA-C1: complete. Migration 056 adds `pyra_app_releases.is_mandatory boolean NOT NULL DEFAULT false` (additive; next free number — 039 through 055 already taken, checked `pnpm db:check-drift` first, only the pre-existing unrelated 036_push_subscriptions gap). Verified via `information_schema` (boolean, NOT NULL, default false) and confirmed the sole active row (pyra-calls, version_code=5, 1.4.0) reads `is_mandatory=false` — nothing became retroactively mandatory. `GET /api/mobile/app-version` now selects + returns `latest.is_mandatory` (additive JSON key; a pre-CA-C2 phone's decoder ignores unknown keys). `pnpm app:publish` gains `--mandatory` (default false, publish-mode only — rejected together with `--activate`, since a rollback reactivates a row's EXISTING flag rather than setting a new one): prints a loud warning banner before the upload starts and again in the final summary when used, and `--activate` prints an informational note if the row being reactivated was itself published mandatory (flag unchanged by the rollback). docs/CALL-TRACKING.md updated (endpoint 6 response shape + the publish runbook). Verify: `pnpm run check` 0 errors, `pnpm build` exit 0, `pnpm test` 850/851 (same pre-existing `atomic-task-write-routes.test.ts` failure, not worsened). No `pnpm app:publish` run and no APK built, per task constraint — exercising `--mandatory` live is CA-C3. Not pushed.
- CA-C1: complete (403d711..fd5dc55 incl. the escape-hatch fix, review clean). Migration 056 adds pyra_app_releases.is_mandatory (default false; all 4 existing rows verified false — nothing retroactively mandatory); app-version returns it; publish gains --mandatory with a loud pre-upload BLOCKED warning. Reviewer traced the rollback interaction and found it correct (--mandatory is rejected with --activate; runActivate never writes the column) but flagged that a mistaken flag had NO quick undo — fixed before the blocking screen exists: `pnpm app:publish --set-mandatory <code> false --app pyra-calls` PATCHes only is_mandatory, never is_active, with 4 mutual-exclusion guards. Minor deferred: the warning is print-and-continue with no interactive confirm (matches the file's existing convention).
- LIVE CONFIRMATION: first call_attempt row written by a real phone post-deploy — the Part B write path works on production traffic.
- CA-C2: complete (bd34bc6..ea9ebe7 incl. fix round 1, re-review clean). v1.5.0 / versionCode 6: high-importance channel `updates_v2` (old `updates` deleted — channel importance is immutable so an upgrading phone would otherwise keep the quiet one), persistent Home banner, UpdateRequiredScreen for a mandatory release, blocked branch placed AFTER permissions+login so a logged-out rep is never trapped. Sync untouched (all 4 components verified absent from the diff).
  CRITICAL caught in review and fixed: the block was computed from raw SharedPreferences, not Compose State, so an already-blocked screen NEVER re-read it — if the owner un-flagged or rolled back a mandatory release, the rep stayed locked out for the life of the process with force-close as the only (undocumented) escape. Now `rememberPendingUpdate()` mirrors the cache into State refreshed on ON_RESUME (reusing the existing LifecycleEventEffect idiom) plus a 60s in-screen poll, so recovery is bounded both when the rep foregrounds the app and when they leave it parked on the screen. Block is still unskippable while an update genuinely is pending.
- CA-C3: complete. Live update-enforcement E2E on emulator `pyra_a15_test` with debug build `versionCode 6 / 1.5.0` (BASE_URL `http://10.0.2.2:3000` → local `pnpm dev` → REAL prod Supabase; channel `pyra-calls-e2e`, so nothing published could reach youssef/cosette). Disposable `e2e.upgrade` reactivated (status + GoTrue unban + throwaway password) and re-locked afterwards (status inactive, original `deactivated_at` restored verbatim, ban 876000h, password rotated to an unrecorded random value, device keys deactivated); post-relock login probe returns 401. Never logged in as youssef or cosette.
  ALL 7 CHECKS PASS. (1) Banner: non-mandatory v7 published → heads-up notification AND the persistent Home banner rendered in the same frame, app fully usable behind them («شغل النهاردة» opened normally); `dumpsys notification` proves `channel=updates_v2 importance=4` and that the legacy `updates` channel no longer exists on the device. (2) Block: `--set-mandatory 7 true` → UpdateRequiredScreen appeared **on ON_RESUME alone** (no relaunch — `am start` reported "brought to the front"), 3× BACK did not dismiss it, the single action moved topResumedActivity to `.ui.UpdateActivity`. (3) SYNC SURVIVES THE BLOCK (the owner's hard requirement, not skipped): with the blocking screen up, an answered inbound call (`adb emu gsm`, 13 s) reached prod as `pyra_agent_calls` `ac_2lLini2140GGE0uu` (created_at 12:05:52Z) while topResumedActivity stayed MainActivity/blocked throughout; the unmatched-number notification fired while blocked too. (4) Self-clear: installing v7 wiped the cache via `clearPendingUpdateIfInstalled` (7→0) — Home showed 1.5.1-e2e with no banner, no block, session intact. (5) Rendering: built a lead carrying BOTH types through the real code paths (retro-link `call_logged` for the 13 s answered call + live-sync `call_attempt` for a 0-second dial) — «محاولة اتصال — لم يرد» rose missed-call icon vs «مكالمة واردة — المدة 0:13» blue icon. (6) Idle safety: SQL replay of the cron's own criteria — a lead last genuinely contacted 30 days ago with a fresh call_attempt computes `last_touched` = 30d ago → **still selected as idle**, vs `idle=false` if the attempt were counted; the 3 real prod leads with attempts likewise have last_touched pinned to their last NON-attempt signal. (7) ROLLBACK RECOVERY (the review's most important fix): `--set-mandatory 7 false` → next poll cleared the flag → the blocked screen un-blocked itself **15 s later** with NO force-close, NO reinstall and zero interaction; 38 s end-to-end from running the escape hatch.
  TWO HARNESS CAVEATS, stated so the numbers read correctly: the 6h `UpdatePolicy.shouldCheck` throttle was collapsed for the test (prefs reset via `run-as` between phases; emulator clock advanced via `cmd alarm set-time` for the un-block measurement, so the process was never killed) — the polls themselves were real network calls. And the v1.5 **debug** APK is 10.56 MB, OVER the `pyra-private` bucket's 10 MiB `file_size_limit`, so it could not be uploaded: the e2e release row was published with `--code 7 --name 1.5.1-e2e` against a small placeholder object and the "update completed" step installed the genuine v7 APK via `adb install -r`. The download+SHA-verify leg of UpdateActivity is therefore NOT re-exercised in this run (unchanged since v1.2, E2E-verified 2026-07-16). ⚠️ VERIFY THE SIGNED v1.5.0 RELEASE APK IS UNDER 10 MiB BEFORE PUBLISHING (v1.2.1's was 7.80 MB).
  OPERATIONAL FINDING worth knowing before the owner ever uses `--mandatory`: the escape hatch is not instant. The 60 s in-screen re-check only re-reads the LOCAL cache; only SyncWorker's 6h-throttled network poll can clear it. So un-mandating reaches a blocked phone in up to ~6h + ≤60 s. If a rep is blocked and needs the app now, un-mandate AND have them force-close/reopen. Documented in docs/CALL-TRACKING.md.
  Cleanup verified zero leftovers: 1 `pyra_app_releases` row + its storage object deleted (prefix `app-releases/pyra-calls-e2e` now lists empty), 2 `pyra_agent_calls`, 2 leads, 5 `pyra_lead_activities`, 1 `pyra_activity_log`, 1 `pyra_notifications` deleted; 0 `pyra_error_logs` produced; device key deactivated; app uninstalled; emulator clock restored to automatic and the AVD shut down. `pyra_app_releases` is back to exactly the production rows with `pyra-calls-e2e` holding 0.
  NOT DONE (deliberate, per constraints): no release APK built, `pnpm app:publish --app pyra-calls` never run, nothing pushed to main. Wave C is BUILT + VERIFIED and awaiting the owner's «ادمج» → deploy the server FIRST, then `pnpm app:publish`.
- CA-C3: complete (2042f90..94032a8). Emulator E2E: all 7 checks PASS — banner+heads-up (dumpsys confirms channel updates_v2 importance=4, legacy channel gone), blocking screen appeared on ON_RESUME alone and survived 3x BACK, **a 13s call placed while blocked reached prod (ac_2lLini2140GGE0uu) — the owner's hard requirement**, self-clear on install, attempt-vs-answered render distinctly, and an SQL replay of the idle cron's own criteria proved a call_attempt does NOT mark a lead touched.
  Two E2E concerns raised, both closed: (1) the escape hatch was bounded by the 6h server poll, not the 60s local re-read — so an accidental mandatory release could lock a rep out for ~6h. FIXED in 94032a8: while blocked the screen polls the SERVER every 60s (worst case now ~60s); a network error/401/malformed response can never clear the block (only Log.w), the loop is one-per-composition and dies with it, and the 6h throttle on the normal path is untouched, guarded by a test asserting the two intervals stay far apart. (2) APK size — the debug build is 10.56 MB against a 10 MiB bucket cap; controller built the SIGNED RELEASE to settle it: **8,250,140 bytes = 7.87 MB, publishable**.
- WAVE COMPLETE (Parts A+B deployed earlier today; Part C code-complete). Part C NOT deployed, v1.5.0 NOT published. Ship order at «ادمج»: server to main FIRST, verify, THEN publish the signed v1.5.0 APK.
