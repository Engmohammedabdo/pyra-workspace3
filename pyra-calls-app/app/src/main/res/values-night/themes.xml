<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night override: same style name, only the status-bar pair flips to
         match PyraTheme's dark background + light-on-dark icon contrast. -->
    <style name="Theme.PyraCalls" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/status_bar_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
