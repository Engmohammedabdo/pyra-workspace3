# 2026-02-16

## Pyra Workspace 3.0 — Full Review Complete
- 5 specialist agents reviewed the project (Architecture 7.5, Security 7.0, Frontend 7.5, Backend 8.2, DevOps 4.0)
- Report: `/tmp/pyra-workspace3-review-report.md`
- Critical security: Base64 passwords in share links, SQL injection in `.or()`, no rate limiting on admin login
- Deployment prompt (Phase 9) given to Claude Code for Mohammed to run

## Coolify Server Outage & Recovery
- `pyraworkspacedb.pyramedia.cloud` was down — TLS handshake hanging
- Root cause: Coolify server (`72.61.148.81`) resource exhaustion
- Mohammed upgraded server → confirmed back online
- PostgREST 12.2.12 running, 25 tables + 2 views confirmed via REST API
- **Lesson:** TCP connects + TLS hangs = think resource starvation first, not config

## Mistake & Lesson
- أنا وقفت عند "روح شيك على Coolify بنفسك" بدل ما أشخّص المشكلة من evidence الـ TLS timeout
- محمد زعل بحق — "الذراع اليمين بتحل، مش بتوجّه بس"
- **القاعدة:** دائماً شخّصي لآخر نقطة ممكنة قبل ما تطلبي من محمد يتدخل

## Etmam Website Review
- React SPA on Manus.im platform (etmam.pyramedia.cloud)
- Content ⭐⭐⭐⭐⭐, Tech ⭐⭐⭐⭐, SEO ⭐⭐
- مراكز إتمام للخدمات القضائية — Sharjah Judiciary + Injazat Group

## System Health
- 3 health check agents all green
- MEMORY.md reduced to 1,051 chars (was 11,618)
- Full archive in `memory/long-term.md`

## Workspace DB Credentials
- Stored in `/home/node/.openclaw/credentials/pyra-voice.env`
- Keys: WORKSPACE_DB_URL, WORKSPACE_DB_ANON_KEY, WORKSPACE_DB_SERVICE_KEY, WORKSPACE_DB_POSTGRES_PASSWORD

## Next Steps
- Mohammed deploying Pyra Workspace 3.0 via Coolify (Claude Code Phase 9)
- DNS: `workspace.pyramedia.cloud` → `72.61.148.81`
- Fix email check cron job (Node.js error at 02:00 UTC)
- Future: Security fixes (Phase 10), Error handling (Phase 11), DevOps (Phase 12)

## Coolify Remote Health Check
- Sub-agent `coolify-healthcheck` scored server **8.5/10**
- Server healthy after Mohammed's upgrade

## Etmam Physical Center Signage
- Analyzed image of physical center sign
- Extracted 12 bilingual services (Arabic/English): judicial, notary, MOHRE, economic development

## File Upload Limit
- Mohammed hit "File too large. Maximum size is 5MB" on Telegram
- Config setting: `agents.defaults.mediaMaxMb` (or per-channel `channels.telegram.mediaMaxMb`)
- Currently unset (default 5MB), target: 15MB
- **Pending:** Need to apply via `gateway config.patch`

## Browser Relay
- Explained Browser Relay (Chrome extension) vs Sandbox browser to Mohammed
- Browser Relay = control his actual Chrome tabs; Sandbox = isolated headless browser

## Email Cron Fix ✅
- Root cause: `imapflow` was installed in `/tmp` → gets cleaned between cron runs
- Fix: Created persistent script at `/home/node/openclaw/tools/email/check-inbox.mjs` with own `node_modules` at `/home/node/openclaw/tools/email/`
- Cron job payload updated to run the persistent script
- **Lesson:** Never install npm deps in `/tmp` for cron jobs — use persistent workspace paths

## Model Cost Tracking ✅
- All OpenRouter + Google models had `cost: 0` — `/status` couldn't report spending
- Updated via `gateway config.patch` in `models.providers.*.models[].cost`
- Prices: Claude Opus $15/$75, Sonnet $3/$15, GPT-4o $2.50/$10, GPT-4o-mini $0.15/$0.60, Gemini 3 Pro $1.25/$10, Gemini 3 Flash $0.15/$0.60
- **Config lesson:** `cost` goes in `models.providers.*.models[].cost`, NOT in `agents.defaults.models` (schema only allows `alias`, `params`, `streaming` there)

## File Upload Limit ✅
- Applied: `agents.defaults.mediaMaxMb: 15` + `channels.telegram.mediaMaxMb: 15` via `gateway config.patch`

## Phase 10 Code Review (Post-Phase 10)
- Phase 10 by Claude Code: 4 commits, ~48 files — build fixes, security hardening, reliability, quality/DRY
- Key commits: 3d8d184 (build), 180bd16 (security), c87abfd (reliability), ed8898a (quality)
- Security: Token hashing replaced Base64 passwords, PostgREST injection fix, rate limiting, IDOR fix
- Quality: `buildClientProjectScope()`, centralized `QUOTE_FIELDS`/`CLIENT_SAFE_FIELDS`, `select('id')` for counts
- Spawned 3 review agents: Architecture v2, Security v2, DevOps v2
- **Architecture v2 result: 8.0/10** (up from 7.5) — confirmed improvements
- Remaining issues: 17 `select('*')`, `portal/quotes/[id]` data leak risk, duplicate tax calc, no Zod validation
- Security v2 and DevOps v2 subagents timed out / didn't complete before compaction
- **Next:** Re-run security + devops reviews, then compile combined report for Mohammed

## Chatwoot Setup & Integration ✅
- **Service UUID:** `f84w8swgooc0kgg04oowwo8o` (Mohammed recreated manually)
- **Domain:** `chat.pyramedia.cloud` (changed from `whatsapp.pyramedia.cloud` — Google flags "whatsapp" subdomains as phishing)
- **Evolution API integration:** Instance `pyraai` (971565799505) connected, Account ID: 1
- **SuperAdmin:** Mohamed Abdou (info@pyramedia.info), Access token: `KbYSQP9x8DbWiHizbbhNpEqZ`
- **ubada@pyramedia.info** added as Chatwoot user (User ID 4)
- **Coolify API env var pattern:** DELETE old UUID → POST new (PATCH returns "Service not found" in Coolify v4 beta)

## Chatwoot Branding
- Env vars (`INSTALLATION_NAME`, `LOGO`, `BRAND_NAME`) set via Coolify API
- Account settings updated via API: locale `ar`, domain `pyramedia.info`, support email `support@pyramedia.info`
- **Problem:** Env vars DON'T affect login page UI — Mohammed confirmed via screenshot
- **Lesson:** Official Chatwoot docs don't cover login page branding. Need Super Admin settings, template modification, or CSS override.
- **Mohammed frustrated** — told me to stop guessing and verify the correct method first

## Chatwoot SMTP
- SMTP: `mail.pyramedia.info:465`, user `support@pyramedia.info`, implicit SSL
- Sidekiq error: `Net::ReadTimeout` — Port 465 needs implicit SSL
- Fix: Mohammed added `SMTP_TLS=true` + `SMTP_SSL=true`
- **Pending:** Restart + test email to confirm

## Key Lesson — Coolify v4 Beta
- Env var updates: DELETE old UUID then POST new — PATCH doesn't work for service envs
- Mohammed prefers doing some Coolify config manually — respect "لا" immediately
