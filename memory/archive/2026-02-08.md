# 2026-02-08 — Tasheel Workflow Completion + Kimi K2.5

## Tasheel AI WhatsApp Demo — الإنجازات

### إصلاح الـ Workflow (ليلة 7-8 فبراير)

#### المشكلة الأولى: toolHttpRequest مكسور
- `@n8n/n8n-nodes-langchain.toolHttpRequest` (v1.1) عنده bug على n8n instance محمد
- يعطي "The name of this tool is not a valid alphanumeric string" حتى لو الاسم صحيح
- **الحل:** بدّلنا كل الـ tools لـ `toolCode` (JavaScript) — يعمل `fetch()` مباشرة لـ Supabase
- **5 tools شغالين:** getOrCreateClient, createCase, getClientCases, updateCase, saveDocument

#### المشكلة الثانية: مسار الصور مكسور
- Node قديم "Analyze Image (Gemini)" (code node مفصول) كان يسبب `paired_item_no_connection` error
- **الحل:** حذفنا الـ node القديم + صلحنا Set Image Msg expression

#### المشكلة الثالثة: 3 قدرات ناقصة (محمد اكتشفها)
محمد جرّب الـ bot وشاف:
1. **الصور ما تُقرأ** — الـ bot بيقول "تم الاستلام" بس ما بيحلل الصورة
2. **ما في رفع على Google Drive** — المستندات ما بتتحفظ
3. **ما يقدر يبعث صور/ملفات** — فقط text reply

**الحلول اللي أضفناها:**
1. ✅ Gemini Vision بيحلل كل صورة/مستند
2. ✅ رفع أوتوماتيك على Google Drive folder
3. ✅ Tool جديد `sendMedia` عبر Evolution API

**المسار الجديد:**
```
صورة → Get Image → Analyze (Gemini Vision) → Convert to File → Upload to Drive → Set Image Msg → AI Agent
```

### الوضع النهائي للـ Workflow
- **ID:** `fhUqa3me0sjAobWR`
- **Nodes:** 27 (بعد حذف القديم أصبحت 27)
- **Active:** ✅
- **توثيق شامل:** `docs/tasheel-project-docs.md` (657 سطر)

### تحليل Claude Code لقاعدة البيانات
محمد خلى Claude Code يحلل الـ DB وشاركني النتائج:
- Star Schema design — tasheel_clients hub بالهاتف
- ازدواجية documents_received (array) vs tasheel_documents (table) — مقبول للـ demo
- System Prompt طويل — ممكن نعمل `getServiceDetails` tool يوفر ~800 token/call
- **قرار:** نخلي الـ demo كما هو، التحسينات للإنتاج

### دروس مهمة 📚
1. **`toolHttpRequest` مكسور** على n8n instance محمد — دايماً استخدم `toolCode` بدلاً منه
2. **Nodes مفصولة** تسبب `paired_item_no_connection` — لازم تنحذف
3. **`fetch()` مش متاح** في n8n Code nodes العادية — بس متاح في toolCode
4. **Google Drive credential:** `Pf1fVOWHcEtHObjo` (eng.moabdo22)
5. **Gemini API Key:** موجود في workflows — `***REMOVED***`
6. **Evolution API test1 token:** `2A5431FD3FEA-4061-8ECE-0CC5B32BB379`
7. **Evolution API Global Key:** `U3yg60S3WFPl2aN61HeV8BSaGfhMOcEm`

### درس Sub-Agents (محمد زعل وبحق)
- قعدت أحلل وأصلح الـ workflow بالـ main context — أكلت الـ context كله
- محمد قال "ليه بتستغبي كل مره"
- **الدرس:** أول curl فشل = sub-agent فوراً
- حدّثت SOUL.md بقاعدة جديدة (#7): "ما تستغبي — وزّعي من البداية!"

---

## Kimi K2.5 (بحث)
- محمد سأل عن "وحش الصين"
- Moonshot AI — بيجينغ — تقييم $3.8B
- K2.5: 1T params (32B active), MoE, multimodal (vision + text)
- Thinking mode + Instant mode + Agentic capabilities
- منافس جدي لـ Claude وGPT خصوصاً بالـ coding

---

## Brave Search API
- ⚠️ مفتاح Brave Search مش مضبوط — `web_search` ما يشتغل
- لازم: `openclaw configure --section web` أو `BRAVE_API_KEY` env var
