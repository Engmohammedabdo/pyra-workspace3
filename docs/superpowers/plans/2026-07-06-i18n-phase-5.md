# i18n Phase 5 — HR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development, task-by-task.

**Goal:** Fully bilingual HR — attendance, timesheet, leave (+ balances + approvals), payroll (+ payslips), evaluations, HR admin suite (overview, documents vault, onboarding, work-schedules, productivity), my-documents — plus 39 HR API routes — Arabic pixel-identical.

**Ground truth inventories:** `.superpowers/sdd/p5-discovery/p5-{attendance-timesheet,leave,payroll,evaluations,hr-apis,crosscut}.txt` + controller-saved supplements `p5-{work-schedules,hr-overview-productivity,documents,onboarding}-supplement.txt` — every AR string verbatim with lines, class tags, drift tables, and the API census (a:156/b:15/c:3/pdf:6/internal:2).

## Global Constraints

All Phase 0–4 constraints hold (AR verbatim; D7 fallback; NO JSON arrays in catalogs; digit-glyph preservation via in-place locale parametrize; `localeCompare(x, locale)`; ICU `other` = today's AR; flat-threshold keys not ICU plural for AR ladders; bilingual DB rows via `locale==='ar' ? name_ar : (name || name_ar)`; **same-line `// i18n-exempt:` tags — the scanner is LINE-SCOPED and this recurred in T4+T6, brief every implementer explicitly**; per-task check+test+build; commit trailer; no push until closure). Phase-5-specific:

- **Leave-type NAME matching is DB business logic — NEVER translate the comparison strings.** `pyra_leave_types.name` values ('Annual', 'Sick', 'Unpaid'…) are matched by `.eq('name', type)` at app/api/leave/route.ts:85 + [id]/route.ts:65,228. Comparisons untouched. Display of leave types: the DB rows carry `name_ar` — display via the DB value (`dynamicBalances.find().name_ar` chain in leave-client stays); the local TYPE_LABELS shadow maps swap to keys ONLY where they label the STATIC statuses.leaveType entity keys. **Bundled bug fix (sanctioned):** approvals-client.tsx:199 shows raw English 'Annual' to managers (no name_ar fallback) — fix by resolving via the leave-types data (same lookup chain leave-client uses) with raw-name fallback; call it out in the report.
- **Bilingual DB rows this phase:** `pyra_document_types` (name + name_ar) — apply the locale-conditional pattern at the 4 frontend-only sites (documents-client filter, UploadDocumentDialog, DocumentRowActions pickers); the 3 JOIN-based sites need the API to ALSO return `type_name` — extend the `/api/hr/documents` + `/api/my-documents` SELECT/JOIN and `PyraEmployeeDocument` type (additive `type_name?: string`), then apply the pattern. **EXCEPTION (locked here):** DocTypeRow keeps BOTH names visible regardless of locale (admin catalog-management UX). `pyra_evaluation_periods`/`pyra_evaluation_criteria` name/name_ar → same locale-conditional pattern at the 6+ evaluation sites. `pyra_leave_types.name_ar` → DB data (admin-configurable), display chain stays.
- **Seeded DB content EXEMPT:** `DEFAULT_ONBOARDING_TASKS` (title_ar rows), `pyra_document_types` seed values, `pyra_evaluation_criteria` seed rows. Same doctrine as board templates.
- **New statuses entities (T1):** `timesheet` (draft مسودة/submitted مُرسل/approved معتمد/rejected مرفوض — extract VERBATIM from timesheet-client local maps; also `timesheetPeriod` open/submitted/approved/rejected if wording differs), `onboarding` (from ONBOARDING_STATUS_LABELS verbatim), `documentExpiry` (from EXPIRY_BADGE labelAr verbatim: expired منتهية/expiring_7 تنتهي خلال أيام/expiring_30 تنتهي قريباً/ok سارية/none دائمة), `employmentType` (5 keys from WizardStepPosition local map verbatim), `workLocation` (3 keys verbatim), `paymentSourceType` (from the payroll 3-way map — canonical = the most complete variant; document drift), `evaluationStatus` decision: the existing orphaned `evaluation` entity (draft/in_progress/completed) matches NEITHER real domain — REPLACE its keys with the real Evaluation.status domain (draft/submitted/acknowledged, AR from EvaluationsStatusCard/evaluations-client verbatim) + new `evaluationPeriod` entity (draft/active/closed). The extraction-fidelity test pins only legacy-map keys (objectContaining) — EVALUATION_STATUS_LABELS in lib/constants must stay matched: check its actual keys first and converge honestly; if the legacy constant itself is the orphan, leave it and give the real domains NEW entities (`evaluationForm`, `evaluationPeriod`) instead of widening `evaluation`. Implementer verifies against lib/constants/statuses.ts and documents the choice.
- **EXPIRY_BADGE refactor (locked):** drop `labelAr` from the constant (keep `className` only); labels via `useStatusLabels('documentExpiry')` at the ~4 render sites. Kills the ×3 independent «تنتهي قريباً» copies (documents filter SelectItems reuse the SAME entity keys).
- **deriveAlerts() (locked):** Approach A — inject `t` (from `getTranslations` in the `/api/hr/overview` route) as a parameter: `deriveAlerts(input, t)`. The two-tier leave-count strings stay TWO keys (flat-threshold doctrine). HrAlerts.tsx stays a dumb `alert.message` renderer. Unit tests updated with a stub `t` returning the AR strings (fidelity preserved). `computeCelebrations()` untouched (already clean). Also `active_period` string built in the overview route → per-request locale.
- **wizard-helpers validateStep (locked):** return-KEY pattern — returns `hr.onboarding.wizard.validation.*` keys; NewHireWizard resolves `toast.error(t(key))`. `{min}` param for PASSWORD_MIN_LENGTH. Bundled fix: WizardStepPersonal placeholder «8+ أحرف» → key with `{min: PASSWORD_MIN_LENGTH}` (closes the documented single-source violation).
- **Doc-name triad convergence (sanctioned drift fix):** ONE key group `hr.onboarding.docNames.*` with majority phrasing (عرض العمل / اتفاقية السرية (NDA) / نموذج تسليم العهدة); wizard-helpers' divergent «خطاب العرض» converges — call out. DB-seeded name_ar rows untouched.
- **Asset condition values (جديد/جيد/متوسط) are PERSISTED data** — SelectItem values stay Arabic; labels render the same string; add same-line exempt tags; backlog the enum-key migration. Arabic EXAMPLE placeholders for name_ar DATA fields (الأسبوع العادي, جواز السفر, منشئ محتوى, التسويق, الاسم الكامل بالعربية) STAY hardcoded Arabic + exempt (data-field examples, board-template precedent).
- **Day names:** AttendanceCalendar's local DAY_NAMES_AR (abbreviated) → reuse `calendar.dayNames` keyed object VERBATIM (crosscut confirmed identical). work-schedules DAY_LABELS (FULL forms with ال) → new `hr.workSchedules.dayNamesFull` keyed object (sun..sat) + index→key ASCII array in schedule-form.ts; DAY_LABELS deleted from the pure helper; resolved at the 2 component call sites.
- **13 hardcoded-locale format sites:** dates/times → shared helpers with locale (formatDate/formatTime(x, …, locale)) or in-place `'ar-AE'↔'en-GB'` parametrize where a helper doesn't fit (TodayClockCard live clock: parametrize in place, keep AR glyphs byte-identical). Money-adjacent `toLocaleString('ar-AE')` in onboarding/payslips: parametrize locale in place ONLY (do NOT reroute to formatCurrency — rendering change; the د.إ mislabel bug is a separate user chip). my-payslips' LOCAL formatCurrency duplicate: leave its output byte-identical; add locale param handling consistent with its current shape; backlog consolidation.
- **MONTH_NAMES_AR direct imports** (CreatePayrollDialog, PayrollRunRow, my-payslips) → `monthNamesFor(locale)` (exists since 4.1).
- **Structural dir fixes (sanctioned):** AdminAttendanceDialog DialogContent `dir="rtl"` → `dirFor(locale)`; timesheet Tabs `dir="rtl"` → `dirFor(locale)`; work-schedules mixed-direction paragraph → `dir="ltr"` scoped to the time-range span only. Numeric-input `dir="ltr"` stays.
- **Colloquial register:** approvals-client Egyptian colloquial strings extracted VERBATIM; EN authored neutral/formal.
- **Payroll money discipline:** every formatCurrency keeps its explicit currency arg; NO locale param on money; the multi-currency per-row currency pattern untouched.
- **status-label drift convergence (sanctioned + documented per task):** payroll/employeePayment `pending` «معلق» → canonical «بانتظار الموافقة»; leave `cancelled` gap → add to `leave` entity with leave-client's current wording; EvaluationsStatusCard pills reconcile with whatever entity decision T1 lands.
- **Guillemet cross-references** («موظف جديد» etc. quoting other buttons) — interpolate the SAME t() key into the description so they can't drift.
- **Hands-off:** raw fetch/useState data layers (leave-client documented + any others in crosscut §6); the 4 Sun-Thu weekend fallbacks, v1 leave-balances fallback read, and د.إ mislabel are USER CHIPS — do NOT fix in this phase (avoid conflicts with those sessions); >300-line files split ONLY if the task's brief says so.
- **Manifest (closure only):** the ~28 per-path entries from crosscut §7 (verify each exists — fail-loud manifest).
- **Cron routes + lib/pdf/** remain exempt (Phase 8/9).

## Tasks

### Task 1: Foundation — hr namespace + new statuses entities + shared keys
`messages/{ar,en}/hr.json` = `{"hr": {}}`; register in NAMESPACE_FILES + i18n/global.ts. statuses.json both locales: `timesheet` (+`timesheetPeriod` if wording differs), `onboarding`, `documentExpiry`, `employmentType`, `workLocation`, `paymentSourceType`, evaluation-domain decision per Global Constraints (verify lib/constants first). Widen StatusEntity union. api.json: `api.hr.*` dedup block from p5-hr-apis census (3+ route messages; REUSE the 4 cross-census matches with finance — e.g. dateRangeInvalid already exists). ICU throwaway proof for param keys. Tests green (extraction-fidelity pin only binds legacy-map-backed entities).
Commit: `feat(i18n): phase-5 foundation — hr ns + timesheet/onboarding/documentExpiry/employment entities (Phase 5.1)`.

### Task 2: Attendance + timesheet
Files: app/dashboard/attendance/** + components/attendance/** + app/dashboard/timesheet/**. Inventory: p5-attendance-timesheet.txt. Fills `hr.attendance.*`, `hr.timesheet.*`.
Notes: attendance uses `useStatusLabels('attendance')` (entity exists); timesheet local maps → new `timesheet`/`timesheetPeriod` entities; DAY_NAMES_AR → calendar.dayNames reuse; TodayClockCard live clock locale-parametrized in place; AdminAttendanceDialog + timesheet Tabs dir fixes; a11y aria-labels migrate (keyboard-grid aria kept per HR-bundle locks); weekend-fallback code UNTOUCHED (user chip); 'ar-EG' third-site parametrized.
Commit: `feat(i18n): attendance + timesheet (Phase 5.2)`.

### Task 3: Leave + balances + approvals
Files: app/dashboard/leave/** (incl. settings), app/dashboard/hr/leave-balances/** + components/hr/leave-balances/**, app/dashboard/approvals/**. Inventory: p5-leave.txt. Fills `hr.leave.*`, `hr.leaveBalances.*`, `hr.approvals.*`.
Notes: leave-type NAME comparisons untouched (hazard #1); display via DB name_ar chain; approvals-client raw-'Annual' bundled fix (call out); `leave` entity + local `cancelled` addition; colloquial verbatim; raw YYYY-MM-DD date renders → formatDate(x, undefined, locale) (bundled consistency fix — call out); balance math displays unchanged; v1-fallback route UNTOUCHED (user chip); data-layer raw fetch untouched.
Commit: `feat(i18n): leave + balances + approvals (Phase 5.3)`.

### Task 4: Payroll + my-payslips
Files: app/dashboard/payroll/** + components/payroll/** + app/dashboard/my-payslips/**. Inventory: p5-payroll.txt. Fills `hr.payroll.*`, `hr.payslips.*`.
Notes: `payroll` + `employeePayment` accessors (pending drift → canonical, call out); `paymentSourceType` entity replaces the 3-way inconsistent map (canonical = most complete; document); MONTH_NAMES_AR ×3 → monthNamesFor(locale); 4 toLocaleDateString('ar-AE') parametrized; my-payslips local formatCurrency output byte-identical (no consolidation); a11y aria attrs preserved; money currency args untouched.
Commit: `feat(i18n): payroll + payslips (Phase 5.4)`.

### Task 5: Evaluations
Files: app/dashboard/evaluations/** + components/evaluations/**. Inventory: p5-evaluations.txt. Fills `hr.evaluations.*`.
Notes: entity decision from T1 applied; bilingual DB rows (periods/criteria name_ar) → locale-conditional pattern at 6+ sites; category raw-key bundled fix via new `evaluationCriteriaCategory` keys (call out); evaluations-client is 1361 lines — string swaps only, NO split this phase (note for backlog); score/rating numbers locale-stable.
Commit: `feat(i18n): evaluations + bilingual period/criteria display (Phase 5.5)`.

### Task 6: HR admin suite A — overview + productivity + deriveAlerts server locale
Files: app/dashboard/hr/{page,hr-overview-client}.tsx, components/hr/overview/**, app/dashboard/hr/productivity/**, lib/hr/overview-helpers.ts, app/api/hr/overview/route.ts (t-injection + active_period only), app/api/hr/productivity + app/api/my-productivity response messages if any. Inventories: p5-hr-overview-productivity-supplement.txt (+ its inline detail in the ledgered agent report). Fills `hr.overview.*`, `hr.productivity.*`.
Notes: deriveAlerts Approach A per Global Constraints (+ unit-test stub t); Recharts closures pre-resolved strings ×2; HeadcountChart aria {list} param w/ locale separator; يوم/أيام shared ICU key ×2 sites; metadata ×2 → generateMetadata; formatDate ×6 locale; KPI ICU params.
Commit: `feat(i18n): HR overview + productivity — server-locale alerts (Phase 5.6)`.

### Task 7: HR admin suite B — documents vault + my-documents + work-schedules
Files: app/dashboard/hr/documents/** (+settings), components/hr/documents/**, app/dashboard/my-documents/**, lib/hr/document-expiry.ts, app/dashboard/hr/work-schedules/** + components/hr/work-schedules/**, PLUS the API additive change: /api/hr/documents + /api/my-documents JOIN returns `type_name` + PyraEmployeeDocument type widened. Inventories: p5-documents-supplement.txt + p5-work-schedules-supplement.txt. Fills `hr.documents.*`, `hr.myDocuments.*`, `hr.workSchedules.*`.
Notes: EXPIRY_BADGE refactor per Global Constraints; bilingual type-name pattern (4 frontend + 3 JOIN sites; DocTypeRow exception); terminology normalization at catalog level (تحميل canonical for download; fallback '—' unified) — each documented; day-names dayNamesFull pattern; mixed-direction paragraph fix; guillemet keys; 20-ميجابايت → {max} param.
Commit: `feat(i18n): documents vault + my-documents + work-schedules — bilingual type names (Phase 5.7)`.

### Task 8: Onboarding
Files: app/dashboard/hr/onboarding/** + components/hr/onboarding/** + wizard-helpers.ts + lib/constants/onboarding.ts (ASSET_TYPES_AR → catalog; ONBOARDING_STATUS_LABELS stays for non-UI callers if any — check; DEFAULT_ONBOARDING_TASKS untouched). Inventory: p5-onboarding-supplement.txt (+ ledgered full report). Fills `hr.onboarding.*` (list/detail/wizard/validation/checklist/documents/docNames groups).
Notes: return-key validateStep; docNames convergence (call out); onboarding entity accessor; employmentType/workLocation entities; asset condition exempt tags; Arabic data-field placeholders exempt; toLocale sites locale-parametrized in place (د.إ bug untouched — user chip); «8+ أحرف» {min} fix; guillemets via shared keys; emoji preserved in toast keys; OnboardingChecklist toLocaleDateString → formatDate(x, undefined, locale).
Commit: `feat(i18n): onboarding wizard + detail + documents (Phase 5.8)`.

### Task 9: HR API routes (39 + 5 libs)
Census: p5-hr-apis.txt — 156 (a) strings → api catalog via getTranslations('api'); 15 (b) + 3 (c) + 6 (pdf) + 2 (internal) same-line exempt tags; leave-type NAME comparisons untouched (censused sites); shared libs per-string per census; NO logic changes; weekend/v1-fallback code untouched (user chips).
Commit: `feat(i18n): HR API messages on api catalog (Phase 5.9)`.

### Task 10: Closure
MIGRATED_PATHS += crosscut §7 list (verify every path exists — fail-loud); full-manifest scan + straggler sweep; payload re-measure; full suite; final whole-branch review (most capable model, cross-task lens: catalog coherence, entity-path consistency, money invariance, server-locale defaults, out-of-scope files); fix wave; CLAUDE.md "(Phases 0–5 shipped)"; push; deploy confirm (Coolify status is the marker); production smoke both locales; ledger + memory. QA flags for Abdou: attendance clock + payroll month names in EN; approvals tab now shows Arabic leave-type names (bundled fix); evaluations bilingual period names.
