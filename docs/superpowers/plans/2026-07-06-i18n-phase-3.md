# i18n Phase 3 — CRM Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development, task-by-task.

**Goal:** Fully bilingual CRM — sales dashboard, pipeline (locked drag intact), lead detail stack, customers/follow-ups/approvals, contracts components, and all 19 CRM API routes — Arabic pixel-identical.

**Ground truth inventories:** `.superpowers/sdd/p3-discovery/p3-{crm-dashboard,pipeline,lead-detail,customers-followups,crm-apis,crm-crosscut}.txt` — every AR string verbatim with lines, ICU flags, the pipeline LOCK MAP, the API census (a=133/b=18/c=20/comments=4), dedup design, and the accessor swap-target table.

## Global Constraints

All Phase 0–2 constraints hold (AR verbatim; D7 fallback; NO JSON arrays in catalogs; digit-glyph preservation via in-place `'ar-EG'↔'en-GB'` parametrize; `localeCompare(x, locale)`; ICU `other` reproduces today's AR; per-task check+test+build; ICU runtime proof via throwaway createTranslator vitest; commit trailer; no push until closure). Phase-3-specific:

- **PIPELINE LOCK MAP (p3-pipeline.txt §3) is absolute.** No-touch: sensor kill-switch consts, useSensors config, handleDragStart/End logic, `pointerWithin` DndContext, DragOverlay `dropAnimation={null}`, the single `useDraggable` per lead.id, opacity-0 source wrapper + listeners-on-Link structure, selectionMode early-return ORDER, mobile stage button LOCATION (PipelineCard wrapper), per-card sheet useState. String swaps inside those regions are allowed ONLY as text-node/prop edits.
- **Scroller dir decision (locked here):** `pipeline-board.tsx` L164/L187 `dir="rtl"` become `dir={locale === 'ar' ? 'rtl' : 'ltr'}` — sanctioned BECAUSE the CRM board's collision is `pointerWithin`, chosen in Phase 7 precisely for being layout-direction-agnostic (unlike the boards module's closestCorners which stays ltr-locked). This is the ONE structural dir change; it carries a MANDATORY closure-QA flag: live drag in EN before/at deploy.
- **useMoveLeadStageWithToasts stays the single toast source** (Phase 10 lock) — swap strings/labels in place; control flow (incl. `startsWith('API error:')` and serverMessage passthrough) untouched.
- **LOST_REASON_CHIPS:** keep ONE array; labels via t(); the textarea-insert and selected-state comparison both use the LOCALIZED label (AR behavior byte-identical; stored lost_reason remains whatever the user's textarea holds — user-language data, as free text always was).
- **Server-computed display fields:** funnel `label_ar` → client stops reading it (lookup by `stage_id` via `useStatusLabels('pipelineStage')`); ai-insights → server renders per-request locale via `getTranslations`, response gains `message` (localized) while keeping `message_ar` populated (back-compat), client reads `message ?? message_ar`.
- **DB-data strings:** `stage.name_ar` renders stay as-is (DB rows are bilingual — `pyra_sales_pipeline_stages` has `name`+`name_ar`: render `locale==='ar' ? name_ar : (name || name_ar)` where trivially available, else keep name_ar + note); PIPELINE_STAGE_LABELS_AR **must remain in lib/constants** (move-stage route snapshots it into DB metadata — census hazard); activity descriptions stored in DB stay AR (exempt).
- **New statuses entities** (T1): `leadPriority` (vip/urgent/high/medium/low — عاجل/عالية/عادية/منخفضة + VIP), `insightSeverity` (critical/high/medium/low — حرج/عالي/متوسط/منخفض), `crmContract` (7 keys incl. in_progress جاري/paused متوقف/expired منتهي — separate entity; do NOT widen `contract`, its parity test pins the legacy map), `milestone` (completed/invoiced/pending/cancelled). Divergent SOURCE label maps (3 different sets) stay per-surface in `crm.*` keys — no unified entity (labels genuinely differ).
- **Known safe-to-fix bundled bugs:** degenerate plural ternaries (kpi-cards L45, filter-bar L181 — both branches identical) → proper ICU (AR `other` = today; EN real plurals); the convert-to-customer notify Arabic word-splice «مع/ون» (census hazard) → two complete exempt AR strings (text fix inside Phase-8-exempt zone, typo dies now).
- **Accessor swaps (crosscut §1):** pipelineStage/leadActivity/leadType/leadDealType/leadBillingCycle/followUp/leadTask/leadTaskPriority entities ALREADY exist — swap all listed import sites. `dashboard-data-sources.tsx` local formatTime is LOCKED (thread locale string in place, never consolidate). REUSE nav/common keys per crosscut §6 — don't re-mint.

## Tasks

### Task 1: Foundation — entities + crm skeleton + api dedup keys
statuses: +leadPriority/insightSeverity/crmContract/milestone (both locales; StatusEntity union). `messages/{ar,en}/crm.json` = `{ "crm": {} }`; NAMESPACE_FILES + global.ts. `api.json`: + `api.common.jsonBodyRequired` ('JSON body مطلوب') and `api.crm.*` dedup block per census (leadAccessDenied ×10, leadNotFound, clientNotFound, prospectNotFound, leadStateConflict, notSignedStage, assignPermissionRequired, assigneeInactive, isoDateInvalid {field}, taskNotFound/titleRequired/titleTooLong{max}/dueDateFormat/priorityInvalid/statusInvalid). Test: parity auto-covers; ICU throwaway for titleTooLong.
Commit: `feat(i18n): phase-3 foundation — CRM entities, api.crm dedup keys (Phase 3.1)`.

### Task 2: Sales dashboard
Files: app/dashboard/crm/{page,dashboard-client}.tsx + components/crm/dashboard/* (9) + app/api/crm/dashboard/** routes + hooks/useCRMDashboard.ts (type widening for `message`). Inventory: p3-crm-dashboard.txt. Fills `crm.dashboard.*` + `api.crm.insights.*`.
Keys: greeting (3 time-based subtitles + ICU name line), kpi cards (fix degenerate plural), funnel (client-side stage labels via accessor; drop label_ar reads; ICU deal counts), deals-at-risk (ICU duration pluralizer via keys preserving dual forms يومين; WA outbound template = exempt AR + decision note), activity feed (leadActivity accessor + formatRelativeDate locale), action cards (ICU counts; toLocaleString locale-parametrized), team performance (table headers; hardcoded 'AED' left as-is — money policy), insights (SEVERITY map → insightSeverity entity; server: getTranslations render, `message` + legacy `message_ar`; aria fixes), PERIOD_OPTIONS labels (keep Arabic-Indic ٣٠ verbatim in ar).
Commit: `feat(i18n): CRM sales dashboard + insight server-locale messages (Phase 3.2)`.

### Task 3: Pipeline stack (LOCK MAP task)
Files: app/dashboard/crm/pipeline/** + components/crm/pipeline/* (7 files) + hooks/useLeads.ts (toasts only). Inventory: p3-pipeline.txt (§3 lock map governs). Fills `crm.pipeline.*`.
Includes: metadata pattern; client strings + localeCompare fix; toast variants → ICU with {from}/{to} labels resolved via useStatusLabels('pipelineStage') INSIDE the hook (it's a hook — legal); scroller dir per Global Constraints (+ the mobile-sheet ChevronLeft gets `rtl:rotate-180` per Phase 15.1 §7); move-stage-confirm-modal full sweep (LOST_REASON per constraint; contract/invoice pickers with statuses accessors + formatDate locale; dir="rtl" → dirFor); filter bar (leadPriority entity + per-surface source labels in crm ns; degenerate plural fix); pipeline-empty split-node → single ICU; card daysAgoLabel → crm.timeAgo ICU keys preserving dual/week/month forms; deal_type raw render → leadDealType accessor.
Drag-integrity self-check (grep filter like P2-T6) mandatory in report — only sanctioned hits: the 2 dir lines.
Commit: `feat(i18n): CRM pipeline on catalogs — locked drag intact, locale scroller dir (Phase 3.3)`.

### Task 4: Lead detail — chrome + activity + modals
Files: app/dashboard/crm/leads/[id]/lead-detail-client.tsx (+page metadata), components/crm/lead-detail/{lead-header,lead-sidebar,lead-stat-strip,edit-lead-dialog,link-client-modal}.tsx, components/crm/activity/* , components/crm/{add-lead-modal,follow-up-modal}/*, components/crm/lead/{lead-source-icon,lead-priority-badge}.tsx. Inventory: p3-lead-detail.txt + crosscut. Fills `crm.lead.*`, `crm.activity.*`, `crm.modals.*`.
Notes: activity-item TYPE_SPECS module map → labels resolved in-component via leadActivity accessor (restructure like P2 PRIORITY pattern); highlight machinery (data-activity-id/CSS.escape) untouched; grid/Sheet side locks untouched (crosscut §5 no-touch); DialogContent dir="rtl" ×4 → dirFor; quick-due presets (بكره/بعد 3 أيام...) → keys; priority badge → leadPriority entity; source maps per-surface keys.
Commit: `feat(i18n): lead detail chrome + activity timeline + modals (Phase 3.4)`.

### Task 5: Lead detail — tabs + lists
Files: components/crm/lead-detail/{lead-tasks-tab,lead-task-row,lead-attachments-tab,lead-deals-tab,lead-overview-tab (if AR)}.tsx, app/dashboard/crm/leads/** list pages + archived. Inventory: p3-lead-detail.txt + crosscut §3. Fills `crm.leadTabs.*`.
Notes: lead-task-row formatTaskDueDate gets locale (kind already structural); leadTask/leadTaskPriority accessors (Object.keys canonical-value iteration preserved — labels only from accessor); attachments tab (upload/voice strings, formatRelativeDate locale ×3, Sheet side lock untouched); deals tab formatDate/Currency sites per crosscut.
Commit: `feat(i18n): lead tabs + lists (Phase 3.5)`.

### Task 6: Customers + follow-ups + approvals + contracts components
Files: app/dashboard/crm/customers/** + components/crm/customer/*, app/dashboard/crm/follow-ups/**, app/dashboard/crm/approvals/** + components/crm/approvals/*, components/crm/contracts/*. Inventory: p3-customers-followups.txt. Fills `crm.customers.*`, `crm.followUps.*`, `crm.approvals.*`, `crm.contracts.*`.
Notes: customer tabs/health/stat-strip maps → crm ns (+ customerHealth labels); contract-card local map → crmContract entity; milestones map → milestone entity (+inline متأخر key; dubaiDayKey usage untouched); followUp accessor swap; formatCurrencyMap displays untouched (locale-stable); reject modal dir → dirFor.
Commit: `feat(i18n): customers + follow-ups + approvals + contracts components (Phase 3.6)`.

### Task 7: CRM API routes (19 files)
Census: p3-crm-apis.txt — 133 (a) strings → api.crm.* (reuse T1 dedup keys; ~60 new uniques), 18 (b) + 20 (c) exempt-tagged, hazards documented in place (stage-labels-to-DB note above move-stage L244; mixed-enum stored-description note in tasks route; the «معون» splice fixed as two exempt strings). ICU: conditional-suffix failures (`{op}فشل …: {reason}` — keep passthrough shape via param), file-size/voice-duration/attachment-cap messages, PASSWORD_MIN {min}. getTranslations('api') once per handler. Zero un-exempted AR across app/api/crm/**.
Commit: `feat(i18n): CRM API messages on api catalog (Phase 3.7)`.

### Task 8: Closure
MIGRATED_PATHS += app/dashboard/crm, components/crm, app/api/crm, hooks/useLeads.ts (now AR-free) → i18n:check green; CLAUDE.md "(Phases 0–3 shipped)" + insight-message pattern line; full suite; final whole-branch review (fable) incl. pipeline drag-filter over the whole range; push; deploy watch (catalog-marker method); production smoke; ledger + memory.
**Closure QA flag for Abdou (mandatory, cannot be automated): live pipeline drag in BOTH locales + move-stage modal round-trip + an approval flow.**

## Execution notes
- Sequential SDD; T3 (lock map) and T7 (API) get the hardest reviewer scrutiny (drag-filter / logic-integrity respectively).
- Line numbers in inventories drift — match by literal.
- The `crm.json` namespace will be the largest catalog yet (~350+ keys) — payload re-measure at closure (last: 11.7KB gz AR vs 150KB threshold).
