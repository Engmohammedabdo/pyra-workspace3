# Bilingual i18n — Phase 0 (Infrastructure) + Phase 1 (App Shell) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the i18n foundation (next-intl, per-user `preferred_language`, cookie plumbing, locale switchers) and a fully bilingual app shell (sidebar/topbar/palette/breadcrumbs/login/shared UI/status labels) — with Arabic pixel-identical throughout.

**Architecture:** next-intl 4.x in "without i18n routing" mode — locale resolved per-request from the `pyra_locale` cookie (DB `preferred_language` is source of truth, cookie is a cache), messages loaded as per-namespace JSON files deep-merged so any missing English key renders Arabic. URLs never change. `<html lang dir>` + Toaster become dynamic.

**Tech Stack:** next-intl ^4.13.1, Next.js 15.3 App Router (turbopack dev / webpack build), Supabase, React Query, vitest.

**Spec:** `docs/superpowers/specs/2026-07-05-bilingual-i18n-design.md` (Approved). Read it before starting.

## Global Constraints

- Locales: `['ar', 'en']` — default `'ar'`. Cookie name `pyra_locale`, maxAge 1 year, `path=/`, `sameSite: 'lax'`, `httpOnly: false` (the pre-auth toggle writes it via `document.cookie`), `secure` in production.
- **Fallback rule (D7):** messages load as deep-merge of EN **over** AR — a missing EN key renders the Arabic string. Raw keys / blank strings are structurally impossible. Never bypass `loadMessages()`.
- **URLs never change** — no `[locale]` segment, no middleware locale logic (D5).
- **Arabic UX must be pixel-identical after every task.** Default (no cookie) = `ar` = today's rendering.
- DB source of truth: `pyra_users.preferred_language` + `pyra_clients.preferred_language` (`varchar(2) NOT NULL DEFAULT 'ar' CHECK IN ('ar','en')`, migration 035).
- Same Cairo font for both locales (D8) — no font changes anywhere.
- **AR catalog strings are EXTRACTED VERBATIM from code** — never re-author Arabic. EN strings are authored (this plan supplies them). If you find an Arabic string in a file this plan migrates that is missing from the key tables, add a key in the same namespace to BOTH `messages/ar/*` and `messages/en/*` following the surrounding naming pattern — never leave a hardcoded literal behind in a migrated file.
- Every task ends: `pnpm run check` → `pnpm test` → `pnpm build` → all green → commit (append the standard Co-Authored-By trailer per harness rules). Push at task 10 and task 19 (phase boundaries).
- pnpm ONLY (never npm). RTL rules stay: logical properties (`ms-`/`me-`/`ps-`/`pe-`), `rtl:rotate-180` for directional icons.
- Locked invariants untouched: kanban drag stack, `notify()` semantics, Dubai-day math, Arabic PDF engine, portal session cookie mechanics.
- Code comments/identifiers in English. UI strings via catalogs only (in migrated files).

## File Structure (created / modified across both phases)

```
NEW  lib/i18n/config.ts                 — LOCALES, Locale, DEFAULT_LOCALE, cookie consts, isLocale(), dirFor()
NEW  lib/i18n/messages.ts               — deepMerge() + loadMessages() (the D7 fallback lives HERE)
NEW  lib/i18n/date-locale.ts            — getDateFnsLocale()
NEW  lib/i18n/status-labels.ts          — useStatusLabels() / getStatusLabels() accessors (Phase 1)
NEW  i18n/request.ts                    — next-intl getRequestConfig (cookie → locale → messages)
NEW  i18n/global.ts                     — TypeScript AppConfig augmentation (typed keys + Locale)
NEW  messages/ar/{common,nav,auth,statuses}.json   — Arabic catalogs (verbatim extraction)
NEW  messages/en/{common,nav,auth,statuses}.json   — English catalogs (authored here)
NEW  components/layout/locale-switcher.tsx  — LocaleSwitcher (topbar icon) + LocaleSelect (profile) + shared hook
NEW  components/layout/locale-sync.tsx      — LocaleSync (DB→cookie heal on new device)
NEW  components/layout/locale-toggle-anon.tsx — pre-auth cookie-only toggle
NEW  components/layout/nav-config.ts        — single nav config (dedupes sidebar/mobile-nav/palette copies)
NEW  components/portal/portal-nav-config.ts — single portal nav config (dedupes sidebar/mobile-nav copies)
NEW  scripts/i18n-check.ts              — hardcoded-Arabic regression guard (pnpm i18n:check)
NEW  supabase/migrations/035_preferred_language.sql
MOD  next.config.ts                     — wrap with createNextIntlPlugin()
MOD  app/layout.tsx                     — async, getLocale(), dynamic lang/dir, NextIntlClientProvider, Toaster dir
MOD  lib/auth/guards.ts                 — AuthSession + preferred_language + role_name
MOD  lib/portal/auth.ts                 — CLIENT_SAFE_FIELDS + preferred_language
MOD  app/api/profile/route.ts           — PATCH accepts preferred_language + sets cookie
MOD  app/api/portal/profile/route.ts    — PATCH accepts preferred_language + sets cookie
MOD  app/api/portal/auth/login/route.ts — sets pyra_locale cookie on login
MOD  app/dashboard/layout.tsx           — <LocaleSync>
MOD  app/portal/(main)/layout.tsx       — <LocaleSync>
MOD  components/layout/topbar.tsx        — LocaleSwitcher + t() strings + locale-aware role name
MOD  components/portal/portal-topbar.tsx — LocaleSwitcher + t() strings
MOD  app/dashboard/profile/profile-client.tsx — LocaleSelect field
MOD  components/portal/profile/ProfileForm.tsx — LocaleSelect field
MOD  app/(auth)/login/page.tsx           — LocaleToggleAnon + t() strings
MOD  app/portal/(auth)/{login,forgot-password,reset-password}/page.tsx — LocaleToggleAnon + t() strings
MOD  lib/utils/format.ts                 — locale params on formatDate/formatRelativeDate/formatTime/formatTaskDueDate
MOD  components/ui/scroll-area.tsx       — dir from locale
MOD  components/layout/{sidebar,mobile-nav,CommandPalette,breadcrumb,NotificationBell}.tsx — t() strings
MOD  components/portal/{portal-sidebar,portal-mobile-nav}.tsx — t() strings via portal-nav-config
MOD  components/ui/{data-table,pagination,search-input,sheet,error-boundary,error-card,mention-textarea,page-guide}.tsx — t() strings
MOD  app/api/portal/auth/{login,logout,forgot-password,reset-password}/route.ts — getTranslations API messages (pattern-establishing)
MOD  types/database.ts, hooks/useClients.ts, hooks/usePortalProfile.ts — preferred_language field
MOD  package.json                        — next-intl dep + i18n:check script
MOD  CLAUDE.md                           — i18n architecture section (task 19)
```

Discovery facts this plan relies on (verified 2026-07-05): dashboard login is **browser-side Supabase** (`app/(auth)/login/page.tsx` never calls `/api/auth/login`) → the dashboard cookie is healed by `<LocaleSync>`, not by a login API. Portal login IS an API (`/api/portal/auth/login`) → cookie set server-side there. Only pre-existing custom cookie is `pyra_portal_session`; middleware CSRF is Origin-header-based — `pyra_locale` conflicts with nothing. `tsconfig.json` already has `resolveJsonModule: true`. Sidebar `navGroups` already carries `labelEn`/`titleEn` (used only as localStorage keys — preserve as `storageKey`).

---

# PHASE 0 — INFRASTRUCTURE

### Task 1: next-intl foundation (config, messages loader, request config, provider)

**Files:**
- Create: `lib/i18n/config.ts`, `lib/i18n/messages.ts`, `i18n/request.ts`, `i18n/global.ts`, `messages/ar/common.json`, `messages/en/common.json`
- Modify: `next.config.ts`, `app/layout.tsx`, `package.json` (dep)
- Test: `__tests__/i18n-messages.test.ts`

**Interfaces:**
- Consumes: nothing (first task).
- Produces (later tasks import these exact names):
  - `lib/i18n/config.ts`: `LOCALES: readonly ['ar','en']`, `type Locale = 'ar'|'en'`, `DEFAULT_LOCALE: Locale`, `LOCALE_COOKIE = 'pyra_locale'`, `LOCALE_COOKIE_MAX_AGE: number`, `isLocale(v: unknown): v is Locale`, `dirFor(locale: Locale): 'rtl'|'ltr'`
  - `lib/i18n/messages.ts`: `deepMerge(base, override)`, `loadMessages(locale: Locale): Promise<Messages>`, `NAMESPACE_FILES` (grows in later tasks)
  - next-intl context available app-wide: `useTranslations`/`useLocale` (client), `getTranslations`/`getLocale` (server)

- [ ] **Step 1: Install next-intl**

```bash
pnpm add next-intl
```
Expected: `next-intl ^4.13.x` added to package.json dependencies.

- [ ] **Step 2: Create `lib/i18n/config.ts`**

```ts
/**
 * i18n core config — single source of truth for locales + the locale cookie.
 * Design spec: docs/superpowers/specs/2026-07-05-bilingual-i18n-design.md
 */
export const LOCALES = ['ar', 'en'] as const;
export type Locale = (typeof LOCALES)[number];

export const DEFAULT_LOCALE: Locale = 'ar';

/** Cookie is a CACHE of the DB preferred_language — DB always wins (LocaleSync heals drift). */
export const LOCALE_COOKIE = 'pyra_locale';
export const LOCALE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365; // 1 year

export function isLocale(v: unknown): v is Locale {
  return typeof v === 'string' && (LOCALES as readonly string[]).includes(v);
}

export function dirFor(locale: Locale): 'rtl' | 'ltr' {
  return locale === 'ar' ? 'rtl' : 'ltr';
}
```

- [ ] **Step 3: Write the failing test for deepMerge + catalog parity**

Create `__tests__/i18n-messages.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { deepMerge } from '@/lib/i18n/messages';
import arCommon from '@/messages/ar/common.json';
import enCommon from '@/messages/en/common.json';

describe('deepMerge (D7 fallback rule)', () => {
  it('override wins on shared keys', () => {
    expect(deepMerge({ a: { x: 'ar' } }, { a: { x: 'en' } })).toEqual({ a: { x: 'en' } });
  });

  it('missing override keys fall back to base (the Arabic-fallback guarantee)', () => {
    expect(deepMerge({ a: { x: 'ar', y: 'ar-only' } }, { a: { x: 'en' } })).toEqual({
      a: { x: 'en', y: 'ar-only' },
    });
  });

  it('merges nested namespaces without dropping siblings', () => {
    const base = { common: { actions: { save: 'حفظ', cancel: 'إلغاء' } } };
    const override = { common: { actions: { save: 'Save' } } };
    expect(deepMerge(base, override)).toEqual({
      common: { actions: { save: 'Save', cancel: 'إلغاء' } },
    });
  });

  it('does not mutate its inputs', () => {
    const base = { a: { x: 1 } };
    deepMerge(base, { a: { x: 2 } });
    expect(base.a.x).toBe(1);
  });
});

// Structural guard: every EN key must exist in AR (AR is the source catalog;
// an EN-only key would silently render for Arabic users too).
function keyPaths(obj: Record<string, unknown>, prefix = ''): string[] {
  return Object.entries(obj).flatMap(([k, v]) =>
    v !== null && typeof v === 'object' && !Array.isArray(v)
      ? keyPaths(v as Record<string, unknown>, `${prefix}${k}.`)
      : [`${prefix}${k}`],
  );
}

describe('catalog parity', () => {
  it('en/common.json keys are a subset of ar/common.json keys', () => {
    const ar = new Set(keyPaths(arCommon));
    for (const p of keyPaths(enCommon)) {
      expect(ar.has(p), `EN-only key: ${p}`).toBe(true);
    }
  });
});
```

- [ ] **Step 4: Run test to verify it fails**

Run: `pnpm test __tests__/i18n-messages.test.ts`
Expected: FAIL — cannot resolve `@/lib/i18n/messages` / `@/messages/ar/common.json`.

- [ ] **Step 5: Create the Phase-0 message catalogs**

`messages/ar/common.json` (top-level key = namespace; one namespace per file, never split a namespace across files — the loader merges files with a shallow spread):

```json
{
  "common": {
    "language": {
      "label": "اللغة",
      "ar": "العربية",
      "en": "English",
      "switchTo": "التبديل إلى {language}",
      "updateFailed": "تعذر تغيير اللغة — حاول مرة أخرى"
    }
  }
}
```

`messages/en/common.json` (language names stay endonyms in both locales — standard practice):

```json
{
  "common": {
    "language": {
      "label": "Language",
      "ar": "العربية",
      "en": "English",
      "switchTo": "Switch to {language}",
      "updateFailed": "Could not change language — please try again"
    }
  }
}
```

- [ ] **Step 6: Create `lib/i18n/messages.ts`**

```ts
import type { Locale } from './config';

/**
 * One entry per messages/<locale>/<name>.json file. Each file holds exactly
 * ONE top-level namespace (its own name) — files merge with a shallow spread,
 * so splitting a namespace across two files would silently drop keys.
 * Grows per phase: Phase 1 adds 'nav', 'auth', 'statuses'.
 */
export const NAMESPACE_FILES = ['common'] as const;

type Messages = Record<string, unknown>;

/**
 * Recursive merge — `override` wins per-leaf; keys missing from `override`
 * keep the `base` value. This IS the D7 Arabic-fallback guarantee: EN is
 * merged OVER the full AR tree, so an untranslated key renders Arabic.
 * Pure (does not mutate inputs). Arrays are replaced wholesale, not merged.
 */
export function deepMerge(base: Messages, override: Messages): Messages {
  const out: Messages = { ...base };
  for (const [key, value] of Object.entries(override)) {
    const existing = out[key];
    if (
      value !== null && typeof value === 'object' && !Array.isArray(value) &&
      existing !== null && typeof existing === 'object' && !Array.isArray(existing)
    ) {
      out[key] = deepMerge(existing as Messages, value as Messages);
    } else {
      out[key] = value;
    }
  }
  return out;
}

async function loadLocaleFiles(locale: Locale): Promise<Messages> {
  const parts = await Promise.all(
    NAMESPACE_FILES.map(
      (name) => import(`../../messages/${locale}/${name}.json`).then((m) => m.default as Messages),
    ),
  );
  return Object.assign({}, ...parts);
}

export async function loadMessages(locale: Locale): Promise<Messages> {
  const ar = await loadLocaleFiles('ar');
  if (locale === 'ar') return ar;
  return deepMerge(ar, await loadLocaleFiles(locale));
}
```

- [ ] **Step 7: Run test to verify it passes**

Run: `pnpm test __tests__/i18n-messages.test.ts`
Expected: PASS (all 5 tests).

- [ ] **Step 8: Create `i18n/request.ts`** (next-intl plugin default path — do NOT rename)

```ts
import { cookies } from 'next/headers';
import { getRequestConfig } from 'next-intl/server';
import { DEFAULT_LOCALE, LOCALE_COOKIE, isLocale } from '@/lib/i18n/config';
import { loadMessages } from '@/lib/i18n/messages';

export default getRequestConfig(async () => {
  const store = await cookies();
  const raw = store.get(LOCALE_COOKIE)?.value;
  const locale = isLocale(raw) ? raw : DEFAULT_LOCALE;

  return {
    locale,
    messages: await loadMessages(locale),
  };
});
```

- [ ] **Step 9: Create `i18n/global.ts`** (typed keys — a typo'd key becomes a compile error)

```ts
import type common from '../messages/ar/common.json';

// Phase 1 extends this intersection: typeof common & typeof nav & typeof auth & typeof statuses
type Messages = typeof common;

declare module 'next-intl' {
  interface AppConfig {
    // Keep in sync with LOCALES in lib/i18n/config.ts (literal union here —
    // a type-only import of the const can't be used in typeof reliably).
    Locale: 'ar' | 'en';
    Messages: Messages;
  }
}
```

- [ ] **Step 10: Wrap `next.config.ts` with the plugin**

At the top of `next.config.ts` add:
```ts
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin();
```
and change the last line from `export default nextConfig;` to:
```ts
export default withNextIntl(nextConfig);
```
(No other changes — headers/webpack/images blocks stay byte-identical.)

- [ ] **Step 11: Mount `NextIntlClientProvider` in `app/layout.tsx`**

Add import: `import { NextIntlClientProvider } from 'next-intl';`
Wrap the existing body content (provider renders no DOM; zero-prop form auto-inherits locale+messages from the request config — verified against next-intl v4 docs):

```tsx
      <body
        className={`${cairo.variable} ${jetbrainsMono.variable} font-sans antialiased`}
      >
        <NextIntlClientProvider>
          <ThemeProvider
            attribute="class"
            defaultTheme="light"
            enableSystem
            disableTransitionOnChange
          >
            <QueryProvider>
              {children}
            </QueryProvider>
            <Toaster
              position="top-center"
              richColors
              closeButton
              dir="rtl"
            />
          </ThemeProvider>
        </NextIntlClientProvider>
        {/* sw registration script stays as-is below */}
```
Do NOT touch `<html lang="ar" dir="rtl">` in this task (Task 3 does).

- [ ] **Step 12: Full verification**

```bash
pnpm test
pnpm run check
pnpm build
```
Expected: all green. Then boot dev (turbopack + plugin compatibility check — next-intl ≥4.0.3 supports Next 15.3 turbopack):
```bash
pnpm dev
```
Expected: "Ready" line with no next-intl config errors; Ctrl-C after confirming `/login` renders unchanged (Arabic RTL).

- [ ] **Step 13: Commit**

```bash
git add lib/i18n messages i18n next.config.ts app/layout.tsx package.json pnpm-lock.yaml __tests__/i18n-messages.test.ts
git commit -m "feat(i18n): next-intl foundation — cookie locale, deep-merge AR fallback, typed catalogs (Phase 0.1)"
```

---

### Task 2: Migration 035 — `preferred_language` columns + type updates

**Files:**
- Create: `supabase/migrations/035_preferred_language.sql`
- Modify: `types/database.ts` (PyraUser + PyraClient), `hooks/useClients.ts` (Client), `hooks/usePortalProfile.ts` (ClientProfile), `lib/portal/auth.ts:12` (CLIENT_SAFE_FIELDS)

**Interfaces:**
- Consumes: nothing.
- Produces: DB columns `pyra_users.preferred_language`, `pyra_clients.preferred_language`; TS field `preferred_language?: 'ar' | 'en'` on all four shapes; `CLIENT_SAFE_FIELDS` includes it (portal session + portal profile GET return it).

- [ ] **Step 1: Pre-migration backup**

```bash
pnpm db:backup pre-035
```
Expected: snapshot lands in `backups/` (script prints the filename).

- [ ] **Step 2: Write the migration file** (ASCII-only SQL — inline apply is safe)

`supabase/migrations/035_preferred_language.sql` (follow `_template.sql` header style):

```sql
-- Migration: 035_preferred_language
-- Date: 2026-07-05
-- Risk tier: 1 (additive, no data touched)
-- Spec: docs/superpowers/specs/2026-07-05-bilingual-i18n-design.md §4.3
--
-- Adds per-user / per-client UI language preference. DB is the source of
-- truth; the pyra_locale cookie is a cache (healed by LocaleSync).

ALTER TABLE pyra_users
  ADD COLUMN IF NOT EXISTS preferred_language varchar(2) NOT NULL DEFAULT 'ar';

ALTER TABLE pyra_users
  DROP CONSTRAINT IF EXISTS pyra_users_preferred_language_check;
ALTER TABLE pyra_users
  ADD CONSTRAINT pyra_users_preferred_language_check
  CHECK (preferred_language IN ('ar', 'en'));

ALTER TABLE pyra_clients
  ADD COLUMN IF NOT EXISTS preferred_language varchar(2) NOT NULL DEFAULT 'ar';

ALTER TABLE pyra_clients
  DROP CONSTRAINT IF EXISTS pyra_clients_preferred_language_check;
ALTER TABLE pyra_clients
  ADD CONSTRAINT pyra_clients_preferred_language_check
  CHECK (preferred_language IN ('ar', 'en'));

-- DOWN (informational only — forward-only migration system):
-- -- ALTER TABLE pyra_users DROP COLUMN preferred_language;
-- -- ALTER TABLE pyra_clients DROP COLUMN preferred_language;
```

- [ ] **Step 3: Apply**

```bash
pnpm db:query supabase/migrations/035_preferred_language.sql
```
Expected: success, no errors.

- [ ] **Step 4: Verify BEFORE recording** (apply-then-verify-then-record — Phase 14.2 lock §3)

```bash
pnpm db:query "SELECT table_name, column_name, data_type, column_default, is_nullable FROM information_schema.columns WHERE column_name = 'preferred_language' AND table_name IN ('pyra_users','pyra_clients') ORDER BY table_name"
```
Expected: exactly 2 rows, `character varying`, default `'ar'::character varying`, `NO` nullable. Also verify the CHECK rejects bad values:
```bash
pnpm db:query "SELECT count(*) AS existing_rows_defaulted FROM pyra_users WHERE preferred_language = 'ar'"
```
Expected: count equals total user count (all rows defaulted).

- [ ] **Step 5: Record**

```bash
pnpm db:record 035_preferred_language --by=abdou --notes="i18n Phase 0 - per-user/client preferred_language (ar default)"
```

- [ ] **Step 6: TypeScript shapes**

In `types/database.ts` → `PyraUser` (after the `deactivated_at` field), add:
```ts
  /** Migration 035 — UI language preference ('ar' default). Cookie pyra_locale caches this. */
  preferred_language?: 'ar' | 'en';
```
In `types/database.ts` → `PyraClient` (after `created_at`), add:
```ts
  /** Migration 035 — portal UI language preference ('ar' default) */
  preferred_language?: 'ar' | 'en';
```
In `hooks/useClients.ts` → `Client` interface, add:
```ts
  preferred_language?: 'ar' | 'en';
```
In `hooks/usePortalProfile.ts` → `ClientProfile`, add the explicit field (alongside name/email/phone/company):
```ts
  preferred_language?: 'ar' | 'en';
```

- [ ] **Step 7: CLIENT_SAFE_FIELDS**

In `lib/portal/auth.ts` line 12, change:
```ts
export const CLIENT_SAFE_FIELDS = 'id, name, email, phone, company, last_login_at, is_active, created_at';
```
to:
```ts
export const CLIENT_SAFE_FIELDS = 'id, name, email, phone, company, last_login_at, is_active, created_at, preferred_language';
```

- [ ] **Step 8: Verify + commit**

```bash
pnpm run check && pnpm test && pnpm build
git add supabase/migrations/035_preferred_language.sql types/database.ts hooks/useClients.ts hooks/usePortalProfile.ts lib/portal/auth.ts
git commit -m "feat(i18n): migration 035 preferred_language on users+clients + type plumbing (Phase 0.2)"
```

---

### Task 3: Dynamic `<html lang dir>` + Toaster + ScrollArea direction

**Files:**
- Modify: `app/layout.tsx`, `components/ui/scroll-area.tsx`

**Interfaces:**
- Consumes: `getLocale` (next-intl/server — resolves from Task 1's request config), `dirFor` from `lib/i18n/config.ts`.
- Produces: the whole app renders RTL for `ar`, LTR for `en`, driven by the cookie. Radix ScrollArea follows the active direction.

- [ ] **Step 1: Make the root layout locale-aware**

In `app/layout.tsx`:
- Add imports: `import { getLocale } from 'next-intl/server';` and `import { dirFor, type Locale } from '@/lib/i18n/config';`
- Change the component to async and compute dir:

```tsx
export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const locale = (await getLocale()) as Locale;
  const dir = dirFor(locale);

  return (
    <html lang={locale} dir={dir} suppressHydrationWarning>
```
- Change the Toaster prop `dir="rtl"` → `dir={dir}`.

- [ ] **Step 2: ScrollArea follows the locale**

In `components/ui/scroll-area.tsx`: add `import { useLocale } from 'next-intl';` and replace the hardcoded `dir="rtl"` (line 16) with a hook-driven value. The component body becomes:

```tsx
const ScrollArea = React.forwardRef<
  React.ComponentRef<typeof ScrollAreaPrimitive.Root>,
  React.ComponentPropsWithoutRef<typeof ScrollAreaPrimitive.Root>
>(({ className, children, ...props }, ref) => {
  // Radix defaults to dir="ltr"; without an explicit dir the viewport/scrollbar
  // geometry shifts and CLIPS the inline-start edge in RTL. Follow the active
  // locale; callers may still override via props.
  const locale = useLocale();
  return (
    <ScrollAreaPrimitive.Root
      ref={ref}
      dir={locale === 'ar' ? 'rtl' : 'ltr'}
      className={cn('relative overflow-hidden', className)}
      {...props}
    >
      <ScrollAreaPrimitive.Viewport className="h-full w-full rounded-[inherit]">
        {children}
      </ScrollAreaPrimitive.Viewport>
      <ScrollBar />
      <ScrollAreaPrimitive.Corner />
    </ScrollAreaPrimitive.Root>
  );
});
```
(Keep `ScrollBar` and exports unchanged.)

- [ ] **Step 3: Verify both directions manually**

```bash
pnpm run check && pnpm build && pnpm dev
```
- Visit `/login` with no cookie → identical Arabic RTL page (`<html lang="ar" dir="rtl">` in devtools).
- In devtools console: `document.cookie = 'pyra_locale=en; path=/'` → reload → `<html lang="en" dir="ltr">`, layout mirrors (content still Arabic — expected: catalogs are empty, fallback shows AR text in LTR frame).
- Reset: `document.cookie = 'pyra_locale=ar; path=/'` → reload → back to today's exact rendering.

- [ ] **Step 4: Commit**

```bash
git add app/layout.tsx components/ui/scroll-area.tsx
git commit -m "feat(i18n): dynamic html lang/dir + Toaster + ScrollArea direction from locale (Phase 0.3)"
```

---

### Task 4: Dashboard locale chain — guard field, profile PATCH, LocaleSync

**Files:**
- Modify: `lib/auth/guards.ts`, `app/api/profile/route.ts`, `app/dashboard/layout.tsx`
- Create: `components/layout/locale-sync.tsx`

**Interfaces:**
- Consumes: `LOCALE_COOKIE`, `LOCALE_COOKIE_MAX_AGE`, `isLocale`, `DEFAULT_LOCALE` from `lib/i18n/config.ts`.
- Produces:
  - `AuthSession.pyraUser.preferred_language: 'ar' | 'en'` and `AuthSession.pyraUser.role_name: string | null` (Task 14 consumes role_name)
  - `PATCH /api/profile` accepts `{ preferred_language: 'ar'|'en' }`, persists it, and sets the `pyra_locale` cookie on the response
  - `<LocaleSync dbLocale={...} />` — mount-once client component that heals cookie↔DB drift (new device / cleared cookies)

- [ ] **Step 1: Extend the auth guard**

In `lib/auth/guards.ts`:
- `AuthSession.pyraUser` interface — add two fields after `role_name_ar: string;`:
```ts
    role_name: string | null;
    preferred_language: 'ar' | 'en';
```
- In `loadUserWithRole`'s return object (the select is already `*` + joined `pyra_roles(name, ...)` so both values are present), add after the `role_name_ar` line:
```ts
    role_name: role?.name ?? null,
    preferred_language: pyraUser.preferred_language === 'en' ? 'en' as const : 'ar' as const,
```

- [ ] **Step 2: Profile PATCH accepts preferred_language + sets the cookie**

In `app/api/profile/route.ts`:
- Add imports:
```ts
import { cookies } from 'next/headers';
import { LOCALE_COOKIE, LOCALE_COOKIE_MAX_AGE, isLocale } from '@/lib/i18n/config';
```
- After the existing `bank_details` block (do NOT add to the generic `allowed` array — it trims arbitrary strings; language needs strict validation), add:
```ts
    // preferred_language — self-service (i18n Phase 0). Strictly 'ar'|'en'.
    if (body.preferred_language !== undefined) {
      if (!isLocale(body.preferred_language)) {
        return apiValidationError('قيمة اللغة غير صالحة');
      }
      updates.preferred_language = body.preferred_language;
    }
```
- Append `, preferred_language` to the PATCH response `.select('id, username, display_name, email, phone, job_title, bio, avatar_url, bank_details')`.
- After the update succeeds (where `data` is returned, before the success response), refresh the cookie so the very next request renders in the new language:
```ts
    if (typeof updates.preferred_language === 'string') {
      const cookieStore = await cookies();
      cookieStore.set(LOCALE_COOKIE, updates.preferred_language, {
        httpOnly: false, // pre-auth toggle writes this cookie via document.cookie
        secure: process.env.NODE_ENV === 'production',
        sameSite: 'lax',
        maxAge: LOCALE_COOKIE_MAX_AGE,
        path: '/',
      });
    }
```

- [ ] **Step 3: Create `components/layout/locale-sync.tsx`**

```tsx
'use client';

import { useEffect, useRef } from 'react';
import { useRouter } from 'next/navigation';
import { useLocale } from 'next-intl';
import { LOCALE_COOKIE, LOCALE_COOKIE_MAX_AGE, isLocale } from '@/lib/i18n/config';

/**
 * Heals cookie↔DB drift: the DB preferred_language is the source of truth,
 * the pyra_locale cookie is a cache. On a new device / cleared cookies the
 * cookie is absent → request renders with DEFAULT_LOCALE while the user's DB
 * preference may differ. This mounts in the authed layouts, writes the cookie
 * once, and refreshes. One-shot ref guards against refresh loops.
 */
export function LocaleSync({ dbLocale }: { dbLocale: string | null | undefined }) {
  const active = useLocale();
  const router = useRouter();
  const synced = useRef(false);

  useEffect(() => {
    if (synced.current) return;
    if (!isLocale(dbLocale) || dbLocale === active) return;
    synced.current = true;
    document.cookie = `${LOCALE_COOKIE}=${dbLocale}; path=/; max-age=${LOCALE_COOKIE_MAX_AGE}; samesite=lax`;
    router.refresh();
  }, [dbLocale, active, router]);

  return null;
}
```

- [ ] **Step 4: Mount in the dashboard layout**

In `app/dashboard/layout.tsx`: add `import { LocaleSync } from '@/components/layout/locale-sync';` and render it first inside `<BreadcrumbProvider>`:
```tsx
    <BreadcrumbProvider>
      <LocaleSync dbLocale={session.pyraUser.preferred_language} />
      <div className="min-h-screen bg-background">
```

- [ ] **Step 5: Verify**

```bash
pnpm run check && pnpm test && pnpm build && pnpm dev
```
- Log in to the dashboard (Arabic, unchanged).
- `pnpm db:query "UPDATE pyra_users SET preferred_language = 'en' WHERE username = '<your-test-username>'"` → clear the `pyra_locale` cookie in devtools → reload `/dashboard` → page flips to LTR automatically (LocaleSync healed). Check no refresh loop (network tab: exactly one extra refresh).
- Reset: set the user back to `'ar'`, clear cookie, reload → RTL Arabic.

- [ ] **Step 6: Commit**

```bash
git add lib/auth/guards.ts app/api/profile/route.ts components/layout/locale-sync.tsx app/dashboard/layout.tsx
git commit -m "feat(i18n): dashboard locale chain — guard field, profile PATCH + cookie, LocaleSync heal (Phase 0.4)"
```

---

### Task 5: Portal locale chain — login cookie, portal profile PATCH, portal LocaleSync

**Files:**
- Modify: `app/api/portal/auth/login/route.ts`, `app/api/portal/profile/route.ts`, `app/portal/(main)/layout.tsx`

**Interfaces:**
- Consumes: `LOCALE_COOKIE`, `LOCALE_COOKIE_MAX_AGE`, `isLocale`, `DEFAULT_LOCALE` from `lib/i18n/config.ts`; `LocaleSync` from Task 4; `CLIENT_SAFE_FIELDS` now includes `preferred_language` (Task 2).
- Produces: portal login sets `pyra_locale` from the client's DB preference; `PATCH /api/portal/profile` accepts `{ preferred_language }` + sets cookie; portal layout heals drift.

- [ ] **Step 1: Portal login sets the locale cookie**

In `app/api/portal/auth/login/route.ts`: add imports
```ts
import { cookies } from 'next/headers';
import { DEFAULT_LOCALE, LOCALE_COOKIE, LOCALE_COOKIE_MAX_AGE, isLocale } from '@/lib/i18n/config';
```
After the `safeClient` re-fetch (it now includes `preferred_language` via CLIENT_SAFE_FIELDS) and BEFORE `return apiSuccess(...)`:
```ts
    // i18n Phase 0 — locale cookie mirrors the client's stored preference.
    const localeStore = await cookies();
    localeStore.set(
      LOCALE_COOKIE,
      isLocale(safeClient?.preferred_language) ? safeClient.preferred_language : DEFAULT_LOCALE,
      {
        httpOnly: false,
        secure: process.env.NODE_ENV === 'production',
        sameSite: 'lax',
        maxAge: LOCALE_COOKIE_MAX_AGE,
        path: '/',
      },
    );
```

- [ ] **Step 2: Portal profile PATCH accepts preferred_language + sets cookie**

In `app/api/portal/profile/route.ts`, after the existing `phone` block (the PATCH uses per-field destructure blocks, not an array whitelist):
```ts
    // preferred_language — self-service (i18n Phase 0). Strictly 'ar'|'en'.
    if (body.preferred_language !== undefined) {
      if (!isLocale(body.preferred_language)) {
        return apiValidationError('قيمة اللغة غير صالحة');
      }
      updates.preferred_language = body.preferred_language;
    }
```
(Adjust the destructure line to also pull it: `const { name, email, phone, preferred_language } = body;` is NOT needed — reference `body.preferred_language` directly as above to keep the existing destructure untouched.)
Add the same imports as Step 1, and after the successful `.update(...)` call add the same cookie-set block as Task 4 Step 2 (guard on `typeof updates.preferred_language === 'string'`).

- [ ] **Step 3: Mount LocaleSync in the portal layout**

In `app/portal/(main)/layout.tsx`: `import { LocaleSync } from '@/components/layout/locale-sync';` and render inside `<PortalBrandingWrapper>` first:
```tsx
    <PortalBrandingWrapper>
      <LocaleSync dbLocale={client.preferred_language} />
      <div className="min-h-screen bg-background">
```
(`client` comes from `getPortalSession()` which selects CLIENT_SAFE_FIELDS — the field is present after Task 2. If TypeScript complains about the session return type, extend the return type of `getPortalSession` in `lib/portal/auth.ts` accordingly — it should already flow through `PyraClient`.)

- [ ] **Step 4: Verify**

```bash
pnpm run check && pnpm test && pnpm build && pnpm dev
```
- Portal login with a test client → Arabic unchanged; devtools shows `pyra_locale=ar` cookie set.
- `pnpm db:query "UPDATE pyra_clients SET preferred_language = 'en' WHERE email = '<test-client-email>'"` → log out → log in → portal renders LTR immediately (cookie set at login). Reset to 'ar' after.

- [ ] **Step 5: Commit**

```bash
git add app/api/portal/auth/login/route.ts app/api/portal/profile/route.ts "app/portal/(main)/layout.tsx" lib/portal/auth.ts
git commit -m "feat(i18n): portal locale chain — login cookie, profile PATCH, LocaleSync (Phase 0.5)"
```

---

### Task 6: LocaleSwitcher component + topbar placements (dashboard + portal)

**Files:**
- Create: `components/layout/locale-switcher.tsx`
- Modify: `components/layout/topbar.tsx`, `components/portal/portal-topbar.tsx`

**Interfaces:**
- Consumes: `mutateAPI` from `@/hooks/api-helpers`; `common.language.*` keys (Task 1); `isLocale`/`Locale` from config.
- Produces: `LocaleSwitcher({ endpoint })`, `LocaleSelect({ endpoint })` and the shared `useChangeLocale(endpoint)` hook — `endpoint: '/api/profile' | '/api/portal/profile'`. Task 7 consumes `LocaleSelect`.

- [ ] **Step 1: Create `components/layout/locale-switcher.tsx`**

```tsx
'use client';

import { Languages } from 'lucide-react';
import { useLocale, useTranslations } from 'next-intl';
import { useRouter } from 'next/navigation';
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { toast } from 'sonner';
import { mutateAPI } from '@/hooks/api-helpers';
import { Button } from '@/components/ui/button';
import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip';
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select';
import type { Locale } from '@/lib/i18n/config';

type LocaleEndpoint = '/api/profile' | '/api/portal/profile';

/**
 * Persists the new preference to the DB (the API also refreshes the
 * pyra_locale cookie on its response), clears the React Query cache
 * (cached payloads may embed locale-shaped strings), and re-renders the
 * RSC tree in the new locale.
 */
function useChangeLocale(endpoint: LocaleEndpoint) {
  const router = useRouter();
  const queryClient = useQueryClient();
  const t = useTranslations('common');

  return useMutation({
    mutationFn: (next: Locale) =>
      mutateAPI(endpoint, 'PATCH', { preferred_language: next }),
    onSuccess: () => {
      queryClient.clear();
      router.refresh();
    },
    onError: () => toast.error(t('language.updateFailed')),
  });
}

/** Topbar icon toggle — one click flips AR ⇄ EN. */
export function LocaleSwitcher({ endpoint }: { endpoint: LocaleEndpoint }) {
  const locale = useLocale();
  const t = useTranslations('common');
  const next: Locale = locale === 'ar' ? 'en' : 'ar';
  const mutation = useChangeLocale(endpoint);

  return (
    <Tooltip>
      <TooltipTrigger asChild>
        <Button
          variant="ghost"
          size="icon"
          onClick={() => mutation.mutate(next)}
          disabled={mutation.isPending}
          aria-label={t('language.switchTo', { language: t(`language.${next}`) })}
        >
          <Languages className="h-4 w-4" />
        </Button>
      </TooltipTrigger>
      <TooltipContent>
        {t('language.switchTo', { language: t(`language.${next}`) })}
      </TooltipContent>
    </Tooltip>
  );
}

/** Labeled select for profile/settings pages — saves immediately on change. */
export function LocaleSelect({ endpoint }: { endpoint: LocaleEndpoint }) {
  const locale = useLocale();
  const t = useTranslations('common');
  const mutation = useChangeLocale(endpoint);

  return (
    <div className="space-y-2">
      <label className="text-sm font-medium">{t('language.label')}</label>
      <Select
        value={locale}
        onValueChange={(v) => v !== locale && mutation.mutate(v as Locale)}
        disabled={mutation.isPending}
      >
        <SelectTrigger>
          <SelectValue />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="ar">{t('language.ar')}</SelectItem>
          <SelectItem value="en">{t('language.en')}</SelectItem>
        </SelectContent>
      </Select>
    </div>
  );
}
```

- [ ] **Step 2: Dashboard topbar placement**

In `components/layout/topbar.tsx`: `import { LocaleSwitcher } from '@/components/layout/locale-switcher';` and insert directly BEFORE the `{/* Theme Toggle */}` block (line ~77):
```tsx
        <LocaleSwitcher endpoint="/api/profile" />
```

- [ ] **Step 3: Portal topbar placement**

In `components/portal/portal-topbar.tsx`: same import; insert inside the `ms-auto` actions div, right after `<PortalCommandSearch />`:
```tsx
          <LocaleSwitcher endpoint="/api/portal/profile" />
```

- [ ] **Step 4: Verify**

```bash
pnpm run check && pnpm build && pnpm dev
```
- Dashboard: click the Languages icon → whole app flips to EN/LTR (content mostly Arabic still — fallback); click again → back to AR/RTL. DB row reflects each click (`pnpm db:query "SELECT username, preferred_language FROM pyra_users WHERE username = '<you>'"`).
- Portal: same round-trip with a test client.
- Confirm React Query refetch happens (no stale views), and toasts still position correctly in both dirs.

- [ ] **Step 5: Commit**

```bash
git add components/layout/locale-switcher.tsx components/layout/topbar.tsx components/portal/portal-topbar.tsx
git commit -m "feat(i18n): LocaleSwitcher in dashboard + portal topbars (Phase 0.6)"
```

---

### Task 7: Profile-page language selects + pre-auth toggles

**Files:**
- Create: `components/layout/locale-toggle-anon.tsx`
- Modify: `app/dashboard/profile/profile-client.tsx`, `components/portal/profile/ProfileForm.tsx`, `app/(auth)/login/page.tsx`, `app/portal/(auth)/login/page.tsx`, `app/portal/(auth)/forgot-password/page.tsx`, `app/portal/(auth)/reset-password/page.tsx`

**Interfaces:**
- Consumes: `LocaleSelect` (Task 6), config consts.
- Produces: `LocaleToggleAnon` — cookie-only switcher for pre-auth pages.

- [ ] **Step 1: Create `components/layout/locale-toggle-anon.tsx`**

```tsx
'use client';

import { useRouter } from 'next/navigation';
import { useLocale } from 'next-intl';
import { Languages } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { LOCALE_COOKIE, LOCALE_COOKIE_MAX_AGE, type Locale } from '@/lib/i18n/config';

/**
 * Pre-auth language toggle — cookie only (no user/session yet; after login
 * the DB preference wins via login-cookie / LocaleSync). Shows the OTHER
 * language's endonym as the label.
 */
export function LocaleToggleAnon({ className }: { className?: string }) {
  const locale = useLocale();
  const router = useRouter();
  const next: Locale = locale === 'ar' ? 'en' : 'ar';

  return (
    <Button
      type="button"
      variant="ghost"
      size="sm"
      className={className}
      onClick={() => {
        document.cookie = `${LOCALE_COOKIE}=${next}; path=/; max-age=${LOCALE_COOKIE_MAX_AGE}; samesite=lax`;
        router.refresh();
      }}
    >
      <Languages className="h-4 w-4 me-1.5" />
      {next === 'en' ? 'English' : 'العربية'}
    </Button>
  );
}
```

- [ ] **Step 2: Dashboard profile — language field**

In `app/dashboard/profile/profile-client.tsx`: `import { LocaleSelect } from '@/components/layout/locale-switcher';` and add to the info tab's field grid, next to the phone field block (same `space-y-2` grid cell pattern):
```tsx
                <LocaleSelect endpoint="/api/profile" />
```

- [ ] **Step 3: Portal profile — language field**

In `components/portal/profile/ProfileForm.tsx`: same import; add `<LocaleSelect endpoint="/api/portal/profile" />` as a form field alongside name/email/phone. IMPORTANT: LocaleSelect saves immediately on change (own mutation) — it must NOT be wired into the form's submit payload.

- [ ] **Step 4: Pre-auth toggles**

Add `import { LocaleToggleAnon } from '@/components/layout/locale-toggle-anon';` and render in a top corner of the card/page (absolute or header row — match each page's structure):
- `app/(auth)/login/page.tsx`
- `app/portal/(auth)/login/page.tsx`
- `app/portal/(auth)/forgot-password/page.tsx`
- `app/portal/(auth)/reset-password/page.tsx`

Placement guidance: inside the form card header area, e.g. `<div className="flex justify-end"><LocaleToggleAnon /></div>` above the CardTitle. Visual: unobtrusive ghost button.

- [ ] **Step 5: Verify + commit**

```bash
pnpm run check && pnpm build && pnpm dev
```
- `/login` logged-out: toggle flips dir both ways; refresh persists (cookie).
- After dashboard login with a DB preference different from the toggle choice → DB wins (LocaleSync flips back). This is CORRECT per design §4.2.
- Profile pages: select changes language immediately; DB verified via db:query.

```bash
git add components/layout/locale-toggle-anon.tsx app/dashboard/profile/profile-client.tsx components/portal/profile/ProfileForm.tsx "app/(auth)/login/page.tsx" "app/portal/(auth)/login/page.tsx" "app/portal/(auth)/forgot-password/page.tsx" "app/portal/(auth)/reset-password/page.tsx"
git commit -m "feat(i18n): profile language selects + pre-auth locale toggles (Phase 0.7)"
```

---

### Task 8: Locale-aware format helpers

**Files:**
- Create: `lib/i18n/date-locale.ts`
- Modify: `lib/utils/format.ts`
- Test: `__tests__/format-locale.test.ts`

**Interfaces:**
- Consumes: `Locale` type.
- Produces: `getDateFnsLocale(locale)`; extended signatures (ALL backward-compatible — `'ar'` defaults keep every existing call site byte-identical in behavior):
  - `formatDate(date, pattern = 'dd-MM-yyyy', locale: Locale = 'ar')`
  - `formatRelativeDate(date, locale: Locale = 'ar')`
  - `formatTime(isoString, locale: Locale = 'ar')`
  - `formatTaskDueDate(due, today = new Date(), locale: Locale = 'ar')`
  - `formatCurrency`/`formatNumber`/`formatCurrencyMap`/`dubaiDayKey` UNCHANGED (locale-stable by design — spec §7).

- [ ] **Step 1: Write the failing test**

Create `__tests__/format-locale.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { getDateFnsLocale } from '@/lib/i18n/date-locale';
import { formatDate, formatRelativeDate, formatTaskDueDate, formatTime } from '@/lib/utils/format';

describe('locale-aware formatting', () => {
  const d = new Date('2026-01-15T10:00:00Z');

  it('getDateFnsLocale maps ar/en', () => {
    expect(getDateFnsLocale('ar').code).toBe('ar');
    expect(getDateFnsLocale('en').code).toBe('en-US');
  });

  it('formatDate defaults to Arabic (backward compatible)', () => {
    expect(formatDate(d, 'dd MMM')).toBe(formatDate(d, 'dd MMM', 'ar'));
  });

  it('formatDate en renders English month names', () => {
    expect(formatDate(d, 'dd MMM yyyy', 'en')).toContain('Jan');
  });

  it('formatRelativeDate en renders English', () => {
    expect(formatRelativeDate(new Date(Date.now() - 60_000), 'en')).toMatch(/minute|less than/i);
  });

  it('formatTaskDueDate en labels', () => {
    const today = new Date('2026-01-15T10:00:00Z');
    expect(formatTaskDueDate(null, today, 'en').label).toBe('No due date');
    expect(formatTaskDueDate('2026-01-15', today, 'en').label).toBe('Today');
    expect(formatTaskDueDate('2026-01-16', today, 'en').label).toBe('Tomorrow');
    expect(formatTaskDueDate('2026-01-12', today, 'en').label).toBe('Overdue by 3 days');
    expect(formatTaskDueDate('2026-01-18', today, 'en').label).toBe('In 3 days');
  });

  it('formatTaskDueDate ar labels unchanged (regression)', () => {
    const today = new Date('2026-01-15T10:00:00Z');
    expect(formatTaskDueDate(null, today).label).toBe('بدون موعد');
    expect(formatTaskDueDate('2026-01-15', today).label).toBe('اليوم');
    expect(formatTaskDueDate('2026-01-16', today).label).toBe('غداً');
  });

  it('formatTime en uses Latin digits', () => {
    expect(formatTime('2026-01-15T10:00:00Z', 'en')).toMatch(/\d/);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test __tests__/format-locale.test.ts` → FAIL (`date-locale` missing; extra args ignored by TS? — `check` would flag; test assertions on EN labels fail).

- [ ] **Step 3: Create `lib/i18n/date-locale.ts`**

```ts
import { ar, enUS } from 'date-fns/locale';
import type { Locale as DateFnsLocale } from 'date-fns';
import type { Locale } from './config';

export function getDateFnsLocale(locale: Locale): DateFnsLocale {
  return locale === 'ar' ? ar : enUS;
}
```

- [ ] **Step 4: Extend `lib/utils/format.ts`**

- Add import: `import { getDateFnsLocale } from '@/lib/i18n/date-locale';` and `import type { Locale } from '@/lib/i18n/config';` (remove the now-unneeded direct `import { ar } from 'date-fns/locale';` once all uses are swapped).
- `formatTime`:
```ts
export function formatTime(isoString: string | null | undefined, locale: Locale = 'ar'): string {
  if (!isoString) return '—';
  const d = new Date(isoString);
  return d.toLocaleTimeString(locale === 'ar' ? 'ar-AE' : 'en-AE', {
    hour: '2-digit',
    minute: '2-digit',
    timeZone: 'Asia/Dubai',
  });
}
```
- `formatDate`:
```ts
export function formatDate(
  date: string | Date | null | undefined,
  pattern: string = 'dd-MM-yyyy',
  locale: Locale = 'ar',
): string {
  if (!date) return '';
  const d = new Date(date);
  if (isNaN(d.getTime())) return '';
  return format(d, pattern, { locale: getDateFnsLocale(locale) });
}
```
- `formatRelativeDate`:
```ts
export function formatRelativeDate(date: string | Date | null | undefined, locale: Locale = 'ar'): string {
  if (!date) return '';
  const d = new Date(date);
  if (isNaN(d.getTime())) return '';
  return formatDistanceToNow(d, { addSuffix: true, locale: getDateFnsLocale(locale) });
}
```
- `formatTaskDueDate` — add the locale param + a tiny bilingual label table (formatting primitive: catalogs need React context which this pure util must not depend on — documented exception, same doctrine as module-guide's hardcoded text):
```ts
const DUE_LABELS = {
  ar: {
    none: 'بدون موعد',
    today: 'اليوم',
    tomorrow: 'غداً',
    overdue: (n: number) => `متأخر منذ ${n} يوم`,
    upcoming: (n: number) => `بعد ${n} أيام`,
  },
  en: {
    none: 'No due date',
    today: 'Today',
    tomorrow: 'Tomorrow',
    overdue: (n: number) => `Overdue by ${n} ${n === 1 ? 'day' : 'days'}`,
    upcoming: (n: number) => `In ${n} ${n === 1 ? 'day' : 'days'}`,
  },
} as const;

export function formatTaskDueDate(
  due: string | Date | null | undefined,
  today: Date = new Date(),
  locale: Locale = 'ar',
): TaskDueDateFormatted {
  const L = DUE_LABELS[locale];
  // ... replace each hardcoded Arabic label with L.none / L.today / L.tomorrow /
  // L.overdue(abs) / L.upcoming(diff), and the final branch's date-fns call with:
  // format(d, 'dd MMM', { locale: getDateFnsLocale(locale) })
}
```
(Keep every tone/isOverdue value byte-identical.)

- [ ] **Step 5: Run tests, verify, commit**

```bash
pnpm test && pnpm run check && pnpm build
git add lib/i18n/date-locale.ts lib/utils/format.ts __tests__/format-locale.test.ts
git commit -m "feat(i18n): locale params on date/time format helpers, ar-default backward compatible (Phase 0.8)"
```

---

### Task 9: `pnpm i18n:check` — hardcoded-Arabic regression guard

**Files:**
- Create: `scripts/i18n-check.ts`
- Modify: `package.json` (script)
- Test: `__tests__/i18n-check.test.ts`

**Interfaces:**
- Consumes: nothing.
- Produces: `pnpm i18n:check` — exits 1 listing `file:line` for any Arabic literal inside MIGRATED paths (`.ts`/`.tsx` only; `messages/*.json` naturally excluded); also fails on EN-catalog keys missing from AR. Exported pure fns for tests: `scanSource(source: string): number[]`, `findOrphanKeys(ar: object, en: object): string[]`. Task 19 (and every later phase) appends to `MIGRATED_PATHS`.

- [ ] **Step 1: Failing test first**

Create `__tests__/i18n-check.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { scanSource, findOrphanKeys } from '@/scripts/i18n-check';

describe('scanSource', () => {
  it('flags lines containing Arabic', () => {
    expect(scanSource(`const a = 'حفظ';\nconst b = 'save';`)).toEqual([1]);
  });
  it('returns empty for clean source', () => {
    expect(scanSource(`const b = 'save';`)).toEqual([]);
  });
  it('honors the i18n-exempt marker', () => {
    expect(scanSource(`const a = 'حفظ'; // i18n-exempt: PDF-only label`)).toEqual([]);
  });
});

describe('findOrphanKeys', () => {
  it('reports EN keys absent from AR', () => {
    expect(findOrphanKeys({ a: { x: '1' } }, { a: { x: '2', y: '3' } })).toEqual(['a.y']);
  });
  it('empty when EN ⊆ AR', () => {
    expect(findOrphanKeys({ a: { x: '1', y: '2' } }, { a: { x: '3' } })).toEqual([]);
  });
});
```

- [ ] **Step 2: Run to verify it fails** — `pnpm test __tests__/i18n-check.test.ts` → FAIL (module missing).

- [ ] **Step 3: Create `scripts/i18n-check.ts`**

```ts
/**
 * i18n regression guard (design §13.2).
 * 1. No hardcoded Arabic literals in MIGRATED source paths.
 * 2. No EN catalog key missing from AR (orphan-key guard).
 * Run: pnpm i18n:check   (wire into the pre-push routine alongside pnpm run check)
 * Escape hatch: append `// i18n-exempt: <reason>` on the same line.
 */
import { readFileSync, readdirSync, statSync } from 'node:fs';
import { join, relative } from 'node:path';

const ROOT = process.cwd();

/** Paths fully migrated to catalogs — GROWS at the end of every phase. */
const MIGRATED_PATHS: string[] = [
  'i18n',
  'lib/i18n',
  'components/layout/locale-switcher.tsx',
  'components/layout/locale-sync.tsx',
  'components/layout/locale-toggle-anon.tsx',
  'scripts/i18n-check.ts',
];

const ARABIC = /[؀-ۿ]/;
const EXEMPT = 'i18n-exempt';

export function scanSource(source: string): number[] {
  const hits: number[] = [];
  source.split('\n').forEach((line, i) => {
    if (ARABIC.test(line) && !line.includes(EXEMPT)) hits.push(i + 1);
  });
  return hits;
}

export function findOrphanKeys(
  ar: Record<string, unknown>,
  en: Record<string, unknown>,
  prefix = '',
): string[] {
  const orphans: string[] = [];
  for (const [key, value] of Object.entries(en)) {
    const arValue = ar[key];
    if (arValue === undefined) {
      orphans.push(`${prefix}${key}`);
    } else if (
      value !== null && typeof value === 'object' && !Array.isArray(value) &&
      arValue !== null && typeof arValue === 'object' && !Array.isArray(arValue)
    ) {
      orphans.push(...findOrphanKeys(
        arValue as Record<string, unknown>,
        value as Record<string, unknown>,
        `${prefix}${key}.`,
      ));
    }
  }
  return orphans;
}

function collectSourceFiles(path: string): string[] {
  const full = join(ROOT, path);
  const st = statSync(full, { throwIfNoEntry: false });
  if (!st) return [];
  if (st.isFile()) return /\.tsx?$/.test(path) ? [full] : [];
  return readdirSync(full, { recursive: true, encoding: 'utf8' })
    .filter((f) => /\.tsx?$/.test(f))
    .map((f) => join(full, f));
}

function main(): void {
  let failed = false;

  for (const path of MIGRATED_PATHS) {
    for (const file of collectSourceFiles(path)) {
      const hits = scanSource(readFileSync(file, 'utf8'));
      for (const line of hits) {
        console.error(`ARABIC LITERAL  ${relative(ROOT, file)}:${line}`);
        failed = true;
      }
    }
  }

  const namespaces = readdirSync(join(ROOT, 'messages/ar')).filter((f) => f.endsWith('.json'));
  for (const file of namespaces) {
    const ar = JSON.parse(readFileSync(join(ROOT, 'messages/ar', file), 'utf8'));
    let en: Record<string, unknown> = {};
    try {
      en = JSON.parse(readFileSync(join(ROOT, 'messages/en', file), 'utf8'));
    } catch {
      // Missing EN file is fine — full-fallback to Arabic.
    }
    for (const orphan of findOrphanKeys(ar, en)) {
      console.error(`ORPHAN EN KEY   messages/en/${file} → ${orphan} (missing from ar)`);
      failed = true;
    }
  }

  if (failed) process.exit(1);
  console.log('i18n:check ✓ clean');
}

if (process.argv[1]?.includes('i18n-check')) main();
```

- [ ] **Step 4: package.json script**

Add to `"scripts"`:
```json
    "i18n:check": "npx tsx scripts/i18n-check.ts",
```

- [ ] **Step 5: Tests pass + live run + commit**

```bash
pnpm test __tests__/i18n-check.test.ts
pnpm i18n:check
```
Expected: tests PASS; live run prints `i18n:check ✓ clean` exit 0.
```bash
git add scripts/i18n-check.ts package.json __tests__/i18n-check.test.ts
git commit -m "feat(i18n): pnpm i18n:check regression guard — Arabic literals + orphan EN keys (Phase 0.9)"
```

---

### Task 10: Phase 0 exit verification + push

**Files:** none (verification only; fix regressions found).

- [ ] **Step 1: Full suite**

```bash
pnpm run check && pnpm test && pnpm i18n:check && pnpm build
```
Expected: all green.

- [ ] **Step 2: Manual QA matrix (dev server)**

| Check | AR | EN |
|---|---|---|
| `/login` renders, toggle flips dir, cookie persists | ☐ | ☐ |
| Dashboard login → LocaleSync respects DB pref | ☐ | ☐ |
| Topbar switcher round-trip (DB row + cookie + UI all flip) | ☐ | ☐ |
| Profile select (dashboard + portal) persists | ☐ | ☐ |
| Portal login sets cookie from client pref | ☐ | ☐ |
| Toasts positioned/aligned correctly | ☐ | ☐ |
| Dark mode unaffected in both dirs | ☐ | ☐ |
| **AR with no cookie = pixel-identical to pre-Phase-0** (spot-check dashboard home, boards, invoices list) | ☐ | — |

- [ ] **Step 3: Push**

```bash
git push
```
Watch Coolify deploy; smoke-test production `/login` (Arabic unchanged) + switcher round-trip with your own account, then switch yourself back to Arabic.

---

# PHASE 1 — APP SHELL

### Task 11: Status-label catalogs + accessor

**Files:**
- Create: `messages/ar/statuses.json`, `messages/en/statuses.json`, `lib/i18n/status-labels.ts`
- Modify: `lib/i18n/messages.ts` (NAMESPACE_FILES), `i18n/global.ts` (Messages type)
- Test: `__tests__/status-labels.test.ts`

**Interfaces:**
- Consumes: message loader (Task 1).
- Produces: `statuses.*` namespace; `useStatusLabels(entity)` (client) + `getStatusLabels(t)` helper — **NO consumer swaps in this task** (module phases 2–6 swap their own `*_STATUS_LABELS` imports). The legacy Arabic maps in `lib/constants/statuses.ts` stay exported and untouched.

- [ ] **Step 1: Author `messages/ar/statuses.json`** — extraction MUST be verbatim from `lib/constants/statuses.ts` (the parity test in Step 3 enforces this). Full content:

```json
{
  "statuses": {
    "invoice": { "draft": "مسودة", "sent": "مرسلة", "partially_paid": "مدفوعة جزئياً", "paid": "مدفوعة", "overdue": "متأخرة", "cancelled": "ملغية" },
    "quote": { "draft": "مسودة", "pending_approval": "بانتظار الموافقة", "sent": "مرسل", "viewed": "تم العرض", "signed": "موقّع", "invoiced": "تم الفوترة", "rejected": "مرفوض", "expired": "منتهي", "cancelled": "ملغي" },
    "contract": { "draft": "مسودة", "active": "نشط", "completed": "مكتمل", "cancelled": "ملغي" },
    "expense": { "pending": "بانتظار الموافقة", "approved": "معتمد", "rejected": "مرفوض" },
    "leave": { "pending": "بانتظار الموافقة", "approved": "موافق عليه", "rejected": "مرفوض" },
    "payroll": { "draft": "مسودة", "calculated": "محسوب", "approved": "معتمد", "paid": "مدفوع" },
    "po": { "draft": "مسودة", "sent": "مرسل", "acknowledged": "تم الاستلام", "received": "تم التوريد", "invoiced": "تم الفوترة", "cancelled": "ملغي" },
    "creditNote": { "draft": "مسودة", "issued": "صادر", "applied": "مطبّق", "cancelled": "ملغي" },
    "subscription": { "active": "نشط", "cancelled": "ملغي", "expired": "منتهي" },
    "paymentMethod": { "bank_transfer": "تحويل بنكي", "cash": "نقدي", "cheque": "شيك", "credit_card": "بطاقة ائتمان", "online": "دفع إلكتروني", "stripe": "Stripe", "credit_note": "إشعار دائن (رد)", "refund": "استرداد", "other": "أخرى" },
    "billingCycle": { "weekly": "أسبوعي", "monthly": "شهري", "quarterly": "ربع سنوي", "yearly": "سنوي" },
    "employeePayment": { "pending": "بانتظار الموافقة", "approved": "معتمد", "paid": "مدفوع", "rejected": "مرفوض" },
    "evaluation": { "draft": "مسودة", "in_progress": "جاري التقييم", "completed": "مكتمل" },
    "contentPipeline": { "pending": "بانتظار البدء", "in_progress": "قيد التنفيذ", "review": "قيد المراجعة", "approved": "معتمد", "published": "منشور" },
    "followUp": { "pending": "بانتظار التنفيذ", "completed": "مكتمل", "overdue": "متأخر", "cancelled": "ملغي" },
    "client": { "active": "نشط", "inactive": "غير نشط" },
    "lead": { "new": "جديد", "contacted": "تم التواصل", "qualified": "مؤهل", "proposal": "عرض سعر", "negotiation": "تفاوض", "won": "تم الكسب", "lost": "خسارة" },
    "pipelineStage": { "stg_reshuffle": "ريشفل", "stg_new_inquiry": "استفسار جديد", "stg_discovery_call": "مكالمة استكشافية", "stg_proposal_sent": "تم إرسال العرض", "stg_negotiation": "تفاوض", "stg_contract_signed": "تم توقيع العقد", "stg_closed_won": "فوز بالصفقة", "stg_closed_lost": "خسارة" },
    "leadType": { "b2b": "شركة (B2B)", "b2c": "فرد (B2C)" },
    "leadDealType": { "web_design": "تصميم موقع", "social_media_retainer": "إدارة سوشيال (Retainer)", "branding": "هوية بصرية", "payer_ai": "Payer AI", "video_production": "إنتاج فيديو", "performance_ads": "حملات إعلانية", "hybrid_package": "باقة مدمجة", "other": "غير ذلك" },
    "leadBillingCycle": { "one_time": "مرة واحدة", "monthly": "شهري", "quarterly": "ربع سنوي", "annual": "سنوي" },
    "leadActivity": { "lead_created": "تم إنشاء الـ Lead", "stage_change": "انتقلت المرحلة", "note": "ملاحظة", "call_logged": "تم تسجيل مكالمة", "meeting_scheduled": "اجتماع محدد", "whatsapp_inbound": "رسالة واتساب واردة", "whatsapp_outbound": "رسالة واتساب صادرة", "email_sent": "تم إرسال إيميل", "file_attached": "تم إرفاق ملف", "field_updated": "تحديث حقل", "assignment_changed": "تغيير المسؤول", "closed_won_pending": "بانتظار اعتماد Closed Won", "closed_won_approved": "تم اعتماد Closed Won", "closed_won_rejected": "تم رفض Closed Won", "follow_up_created": "متابعة جديدة", "follow_up_completed": "تم تنفيذ المتابعة", "follow_up_overdue": "متابعة متأخرة", "idle_warning": "تنبيه — Lead بدون نشاط" },
    "conversation": { "open": "مفتوحة", "pending": "بانتظار الرد", "resolved": "محلولة" },
    "conversationPriority": { "low": "منخفضة", "normal": "عادية", "high": "عالية", "urgent": "عاجلة" },
    "leadTask": { "pending": "معلّقة", "in_progress": "قيد التنفيذ", "completed": "مكتملة", "cancelled": "ملغية" },
    "leadTaskPriority": { "low": "منخفضة", "medium": "متوسطة", "high": "عالية", "urgent": "عاجلة" },
    "calendarEventSource": { "task": "مهمة", "follow_up": "متابعة", "meeting": "اجتماع" },
    "attendance": { "present": "حاضر", "absent": "غائب", "late": "متأخر", "early_leave": "انصراف مبكر", "holiday": "إجازة رسمية", "weekend": "عطلة" }
  }
}
```

- [ ] **Step 2: Author `messages/en/statuses.json`** — same structure, English values:

```json
{
  "statuses": {
    "invoice": { "draft": "Draft", "sent": "Sent", "partially_paid": "Partially Paid", "paid": "Paid", "overdue": "Overdue", "cancelled": "Cancelled" },
    "quote": { "draft": "Draft", "pending_approval": "Pending Approval", "sent": "Sent", "viewed": "Viewed", "signed": "Signed", "invoiced": "Invoiced", "rejected": "Rejected", "expired": "Expired", "cancelled": "Cancelled" },
    "contract": { "draft": "Draft", "active": "Active", "completed": "Completed", "cancelled": "Cancelled" },
    "expense": { "pending": "Pending Approval", "approved": "Approved", "rejected": "Rejected" },
    "leave": { "pending": "Pending Approval", "approved": "Approved", "rejected": "Rejected" },
    "payroll": { "draft": "Draft", "calculated": "Calculated", "approved": "Approved", "paid": "Paid" },
    "po": { "draft": "Draft", "sent": "Sent", "acknowledged": "Acknowledged", "received": "Received", "invoiced": "Invoiced", "cancelled": "Cancelled" },
    "creditNote": { "draft": "Draft", "issued": "Issued", "applied": "Applied", "cancelled": "Cancelled" },
    "subscription": { "active": "Active", "cancelled": "Cancelled", "expired": "Expired" },
    "paymentMethod": { "bank_transfer": "Bank Transfer", "cash": "Cash", "cheque": "Cheque", "credit_card": "Credit Card", "online": "Online Payment", "stripe": "Stripe", "credit_note": "Credit Note (Refund)", "refund": "Refund", "other": "Other" },
    "billingCycle": { "weekly": "Weekly", "monthly": "Monthly", "quarterly": "Quarterly", "yearly": "Yearly" },
    "employeePayment": { "pending": "Pending Approval", "approved": "Approved", "paid": "Paid", "rejected": "Rejected" },
    "evaluation": { "draft": "Draft", "in_progress": "In Progress", "completed": "Completed" },
    "contentPipeline": { "pending": "Not Started", "in_progress": "In Progress", "review": "In Review", "approved": "Approved", "published": "Published" },
    "followUp": { "pending": "Pending", "completed": "Completed", "overdue": "Overdue", "cancelled": "Cancelled" },
    "client": { "active": "Active", "inactive": "Inactive" },
    "lead": { "new": "New", "contacted": "Contacted", "qualified": "Qualified", "proposal": "Proposal", "negotiation": "Negotiation", "won": "Won", "lost": "Lost" },
    "pipelineStage": { "stg_reshuffle": "Reshuffle", "stg_new_inquiry": "New Inquiry", "stg_discovery_call": "Discovery Call", "stg_proposal_sent": "Proposal Sent", "stg_negotiation": "Negotiation", "stg_contract_signed": "Contract Signed", "stg_closed_won": "Closed Won", "stg_closed_lost": "Closed Lost" },
    "leadType": { "b2b": "Company (B2B)", "b2c": "Individual (B2C)" },
    "leadDealType": { "web_design": "Web Design", "social_media_retainer": "Social Media (Retainer)", "branding": "Branding", "payer_ai": "Payer AI", "video_production": "Video Production", "performance_ads": "Performance Ads", "hybrid_package": "Hybrid Package", "other": "Other" },
    "leadBillingCycle": { "one_time": "One-time", "monthly": "Monthly", "quarterly": "Quarterly", "annual": "Annual" },
    "leadActivity": { "lead_created": "Lead created", "stage_change": "Stage changed", "note": "Note", "call_logged": "Call logged", "meeting_scheduled": "Meeting scheduled", "whatsapp_inbound": "WhatsApp message received", "whatsapp_outbound": "WhatsApp message sent", "email_sent": "Email sent", "file_attached": "File attached", "field_updated": "Field updated", "assignment_changed": "Owner changed", "closed_won_pending": "Awaiting Closed-Won approval", "closed_won_approved": "Closed Won approved", "closed_won_rejected": "Closed Won rejected", "follow_up_created": "Follow-up created", "follow_up_completed": "Follow-up completed", "follow_up_overdue": "Follow-up overdue", "idle_warning": "Alert — idle lead" },
    "conversation": { "open": "Open", "pending": "Awaiting Reply", "resolved": "Resolved" },
    "conversationPriority": { "low": "Low", "normal": "Normal", "high": "High", "urgent": "Urgent" },
    "leadTask": { "pending": "Pending", "in_progress": "In Progress", "completed": "Completed", "cancelled": "Cancelled" },
    "leadTaskPriority": { "low": "Low", "medium": "Medium", "high": "High", "urgent": "Urgent" },
    "calendarEventSource": { "task": "Task", "follow_up": "Follow-up", "meeting": "Meeting" },
    "attendance": { "present": "Present", "absent": "Absent", "late": "Late", "early_leave": "Early Leave", "holiday": "Public Holiday", "weekend": "Weekend" }
  }
}
```

- [ ] **Step 3: Failing parity test** — `__tests__/status-labels.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import arStatuses from '@/messages/ar/statuses.json';
import enStatuses from '@/messages/en/statuses.json';
import {
  INVOICE_STATUS_LABELS, QUOTE_STATUS_LABELS, CONTRACT_STATUS_LABELS,
  EXPENSE_STATUS_LABELS, LEAVE_STATUS_LABELS, PAYROLL_STATUS_LABELS,
  PO_STATUS_LABELS, CREDIT_NOTE_STATUS_LABELS, SUBSCRIPTION_STATUS_LABELS,
  PAYMENT_METHOD_LABELS, BILLING_CYCLE_LABELS, EMPLOYEE_PAYMENT_STATUS_LABELS,
  EVALUATION_STATUS_LABELS, CONTENT_PIPELINE_STATUS_LABELS, FOLLOW_UP_STATUS_LABELS,
  CLIENT_STATUS_LABELS, LEAD_STATUS_LABELS, PIPELINE_STAGE_LABELS_AR,
  LEAD_TYPE_LABELS, LEAD_DEAL_TYPE_LABELS, LEAD_BILLING_CYCLE_LABELS,
  LEAD_ACTIVITY_LABELS_AR, CONVERSATION_STATUS_LABELS, CONVERSATION_PRIORITY_LABELS,
  LEAD_TASK_STATUS_LABELS_AR, LEAD_TASK_PRIORITY_LABELS_AR,
  CALENDAR_EVENT_SOURCE_LABELS_AR, ATTENDANCE_STATUS_LABELS,
} from '@/lib/constants/statuses';

const LEGACY_TO_CATALOG: Array<[Record<string, string>, keyof typeof arStatuses.statuses]> = [
  [INVOICE_STATUS_LABELS, 'invoice'], [QUOTE_STATUS_LABELS, 'quote'],
  [CONTRACT_STATUS_LABELS, 'contract'], [EXPENSE_STATUS_LABELS, 'expense'],
  [LEAVE_STATUS_LABELS, 'leave'], [PAYROLL_STATUS_LABELS, 'payroll'],
  [PO_STATUS_LABELS, 'po'], [CREDIT_NOTE_STATUS_LABELS, 'creditNote'],
  [SUBSCRIPTION_STATUS_LABELS, 'subscription'], [PAYMENT_METHOD_LABELS, 'paymentMethod'],
  [BILLING_CYCLE_LABELS, 'billingCycle'], [EMPLOYEE_PAYMENT_STATUS_LABELS, 'employeePayment'],
  [EVALUATION_STATUS_LABELS, 'evaluation'], [CONTENT_PIPELINE_STATUS_LABELS, 'contentPipeline'],
  [FOLLOW_UP_STATUS_LABELS, 'followUp'], [CLIENT_STATUS_LABELS, 'client'],
  [LEAD_STATUS_LABELS, 'lead'], [PIPELINE_STAGE_LABELS_AR, 'pipelineStage'],
  [LEAD_TYPE_LABELS, 'leadType'], [LEAD_DEAL_TYPE_LABELS, 'leadDealType'],
  [LEAD_BILLING_CYCLE_LABELS, 'leadBillingCycle'], [LEAD_ACTIVITY_LABELS_AR, 'leadActivity'],
  [CONVERSATION_STATUS_LABELS, 'conversation'], [CONVERSATION_PRIORITY_LABELS, 'conversationPriority'],
  [LEAD_TASK_STATUS_LABELS_AR, 'leadTask'], [LEAD_TASK_PRIORITY_LABELS_AR, 'leadTaskPriority'],
  [CALENDAR_EVENT_SOURCE_LABELS_AR, 'calendarEventSource'], [ATTENDANCE_STATUS_LABELS, 'attendance'],
];

describe('statuses catalog extraction fidelity', () => {
  it.each(LEGACY_TO_CATALOG.map(([m, k]) => [k, m] as const))(
    'ar statuses.%s matches the legacy Arabic map verbatim',
    (catalogKey, legacyMap) => {
      expect(arStatuses.statuses[catalogKey]).toEqual(legacyMap);
    },
  );

  it('en mirrors ar key-structure exactly for every entity', () => {
    for (const [entity, arMap] of Object.entries(arStatuses.statuses)) {
      const enMap = (enStatuses.statuses as Record<string, Record<string, string>>)[entity];
      expect(enMap, `missing en entity: ${entity}`).toBeDefined();
      expect(Object.keys(enMap).sort()).toEqual(Object.keys(arMap).sort());
    }
  });
});
```

Run: `pnpm test __tests__/status-labels.test.ts` → must PASS on first run IF extraction was verbatim; any mismatch = fix the JSON (never the legacy map).

- [ ] **Step 4: Create `lib/i18n/status-labels.ts`**

```ts
'use client';

import { useTranslations } from 'next-intl';

export type StatusEntity =
  | 'invoice' | 'quote' | 'contract' | 'expense' | 'leave' | 'payroll'
  | 'po' | 'creditNote' | 'subscription' | 'paymentMethod' | 'billingCycle'
  | 'employeePayment' | 'evaluation' | 'contentPipeline' | 'followUp'
  | 'client' | 'lead' | 'pipelineStage' | 'leadType' | 'leadDealType'
  | 'leadBillingCycle' | 'leadActivity' | 'conversation' | 'conversationPriority'
  | 'leadTask' | 'leadTaskPriority' | 'calendarEventSource' | 'attendance';

/**
 * Locale-aware status label lookup. Falls back to the raw status value when
 * a key is unknown (never throws in render paths).
 * Module phases 2–6 swap their *_STATUS_LABELS imports onto this.
 */
export function useStatusLabels(entity: StatusEntity): (status: string) => string {
  const t = useTranslations(`statuses.${entity}`);
  // Cast: status arrives as a runtime string (DB value); t.has() guards it.
  return (status: string) =>
    t.has(status as Parameters<typeof t>[0])
      ? t(status as Parameters<typeof t>[0])
      : status;
}
```
(Server variant when a module phase needs it: `const t = await getTranslations(\`statuses.${entity}\`)` inline — no wrapper needed yet, YAGNI.)

- [ ] **Step 5: Register the namespace**

- `lib/i18n/messages.ts`: `export const NAMESPACE_FILES = ['common', 'statuses'] as const;`
- `i18n/global.ts`:
```ts
import type common from '../messages/ar/common.json';
import type statuses from '../messages/ar/statuses.json';
type Messages = typeof common & typeof statuses;
```

- [ ] **Step 6: Verify + commit**

```bash
pnpm test && pnpm run check && pnpm i18n:check && pnpm build
git add messages/ar/statuses.json messages/en/statuses.json lib/i18n/status-labels.ts lib/i18n/messages.ts i18n/global.ts __tests__/status-labels.test.ts
git commit -m "feat(i18n): status-label catalogs (28 entities, AR verbatim + EN) + useStatusLabels accessor (Phase 1.1)"
```

---

### Task 12: `common` expansion + `nav` + `auth` catalogs

**Files:**
- Modify: `messages/ar/common.json`, `messages/en/common.json`, `lib/i18n/messages.ts` (NAMESPACE_FILES), `i18n/global.ts`
- Create: `messages/ar/nav.json`, `messages/en/nav.json`, `messages/ar/auth.json`, `messages/en/auth.json`

**Interfaces:**
- Consumes: loader from Task 1.
- Produces: the full `common.*`, `nav.*`, `auth.*` namespaces that Tasks 13–18 reference. **Key names here are the contract — later tasks use them verbatim.**

- [ ] **Step 1: Expand `messages/ar/common.json`** (merge INTO the existing `common` object from Task 1 — keep `language` as-is):

```json
{
  "common": {
    "language": { "label": "اللغة", "ar": "العربية", "en": "English", "switchTo": "التبديل إلى {language}", "updateFailed": "تعذر تغيير اللغة — حاول مرة أخرى" },
    "actions": { "save": "حفظ", "cancel": "إلغاء", "confirm": "تأكيد", "close": "إغلاق", "retry": "إعادة المحاولة", "search": "بحث...", "clearSearch": "مسح البحث" },
    "table": { "selectedCount": "{count} محدد", "clearSelection": "إلغاء التحديد", "selectAll": "تحديد الكل", "selectRow": "تحديد الصف" },
    "pagination": { "prev": "الصفحة السابقة", "next": "الصفحة التالية", "pageOf": "صفحة {page} من {totalPages}" },
    "errors": { "unexpected": "حدث خطأ غير متوقع", "sectionLoad": "عذراً، حدث خطأ أثناء تحميل هذا القسم. يرجى المحاولة مرة أخرى.", "loadFailed": "فشل في تحميل البيانات", "serverConnection": "حدث خطأ أثناء الاتصال بالخادم. يرجى المحاولة مرة أخرى.", "checkInternet": "تحقق من اتصال الإنترنت" },
    "mentions": { "loading": "جاري تحميل الأعضاء...", "noMatch": "لا يوجد أعضاء مطابقين", "teamMembers": "أعضاء الفريق" },
    "guide": { "aria": "دليل الاستخدام", "help": "مساعدة", "title": "دليل الاستخدام", "goal": "الهدف", "tips": "نصائح الاستخدام", "viewAll": "عرض دليل جميع الوحدات" },
    "srClose": "إغلاق"
  }
}
```

`messages/en/common.json`:

```json
{
  "common": {
    "language": { "label": "Language", "ar": "العربية", "en": "English", "switchTo": "Switch to {language}", "updateFailed": "Could not change language — please try again" },
    "actions": { "save": "Save", "cancel": "Cancel", "confirm": "Confirm", "close": "Close", "retry": "Try Again", "search": "Search...", "clearSearch": "Clear search" },
    "table": { "selectedCount": "{count} selected", "clearSelection": "Clear selection", "selectAll": "Select all", "selectRow": "Select row" },
    "pagination": { "prev": "Previous page", "next": "Next page", "pageOf": "Page {page} of {totalPages}" },
    "errors": { "unexpected": "An unexpected error occurred", "sectionLoad": "Sorry, something went wrong loading this section. Please try again.", "loadFailed": "Failed to load data", "serverConnection": "A server connection error occurred. Please try again.", "checkInternet": "Check your internet connection" },
    "mentions": { "loading": "Loading members...", "noMatch": "No matching members", "teamMembers": "Team members" },
    "guide": { "aria": "User guide", "help": "Help", "title": "User Guide", "goal": "Goal", "tips": "Usage tips", "viewAll": "View all module guides" },
    "srClose": "Close"
  }
}
```

- [ ] **Step 2: Create `messages/ar/nav.json`** — Arabic values are VERBATIM from the current sidebar/mobile-nav/palette/breadcrumb/bell/topbar code (discovery-verified). Full content:

```json
{
  "nav": {
    "groups": {
      "main":        { "title": "الرئيسية", "description": "لوحة التحكم والإشعارات ومهامك الشخصية" },
      "business":    { "title": "الأعمال", "description": "إدارة المشاريع والعملاء والفواتير وعروض الأسعار" },
      "content":     { "title": "المحتوى والملفات", "description": "مراجعات المحتوى والملفات والتخزين" },
      "crm":         { "title": "CRM", "description": "إدارة العملاء المحتملين، خط المبيعات، WhatsApp، والمتابعات" },
      "hr":          { "title": "الموارد البشرية", "description": "نظرة إدارية على الحضور والإجازات والرواتب والوثائق والتقييمات" },
      "selfService": { "title": "الخدمة الذاتية", "description": "مهامي الشخصية: حضور، إجازات، رواتب، وثائق، ودليل الفريق" },
      "finance":     { "title": "المالية", "description": "المصاريف والعقود والاشتراكات والتقارير المالية" },
      "team":        { "title": "إدارة الفريق", "description": "إدارة المستخدمين وتوزيع الأدوار والصلاحيات" },
      "tools":       { "title": "الأدوات", "description": "إعدادات النظام والأتمتة وقاعدة المعرفة والتكاملات" },
      "security":    { "title": "الأمان والمراقبة", "description": "سجلات الدخول والنشاط والجلسات النشطة" }
    },
    "items": {
      "dashboard": "الرئيسية", "notifications": "الإشعارات", "profile": "ملفي الشخصي", "myTasks": "مهامي", "calendar": "التقويم",
      "projects": "المشاريع", "clients": "العملاء", "quotes": "عروض الأسعار", "quotesAnalytics": "تحليلات العروض", "invoices": "الفواتير", "boards": "لوحات العمل",
      "scriptReviews": "مراجعات السكريبتات", "contentPipeline": "خط الإنتاج", "files": "الملفات", "favorites": "المفضلة", "reviews": "المراجعات", "trash": "المحذوفات", "storage": "التخزين",
      "crmDashboard": "لوحة المبيعات", "pipeline": "خط المبيعات", "customers": "العملاء", "whatsappChat": "محادثات واتساب", "whatsappAnalytics": "تحليلات واتساب", "whatsappCampaigns": "حملات الرسائل", "followUps": "المتابعات", "crmApprovals": "اعتمادات تنتظرك", "quoteApprovals": "موافقات العروض", "salesSettings": "الإعدادات",
      "hrOverview": "نظرة عامة", "productivity": "تقرير الإنتاجية", "teamApprovals": "الموافقات", "leaveSettings": "إعدادات الإجازات", "leaveBalances": "أرصدة الإجازات", "evaluationSettings": "إعدادات التقييم", "hrDocuments": "وثائق الموظفين", "documentTypes": "أنواع الوثائق", "onboarding": "تعيين موظفين", "workSchedules": "جداول العمل",
      "timesheet": "ساعات العمل", "attendance": "الحضور", "leave": "الإجازات", "myPayslips": "راتبي", "evaluations": "تقييم الأداء", "myDocuments": "وثائقي", "directory": "دليل الفريق", "announcements": "الإعلانات", "orgChart": "الهيكل التنظيمي",
      "financeHome": "الإدارة المالية", "expenses": "المصاريف", "cards": "البطاقات", "contracts": "العقود", "recurring": "الفواتير المتكررة", "creditNotes": "إشعارات دائنة", "suppliers": "الموردين", "purchaseOrders": "أوامر الشراء", "financeReports": "التقارير المالية", "revenueTargets": "أهداف الإيرادات", "payroll": "الرواتب",
      "users": "المستخدمون", "teams": "الفرق", "roles": "الأدوار", "permissions": "الصلاحيات",
      "settings": "الإعدادات", "reports": "التقارير", "automations": "الأتمتة", "knowledgeBase": "قاعدة المعرفة", "integrations": "التكاملات",
      "activityLog": "سجل النشاط", "loginHistory": "سجل الدخول", "sessions": "الجلسات", "errorLogs": "سجل الأخطاء"
    },
    "portal": {
      "items": {
        "home": "الرئيسية", "projects": "المشاريع", "files": "الملفات", "quotes": "عروض الأسعار", "contracts": "العقود", "invoices": "الفواتير", "creditNotes": "الإشعارات الدائنة", "recurring": "الفواتير المتكررة", "statement": "كشف الحساب", "scripts": "السكريبتات", "help": "مركز المساعدة", "notifications": "الإشعارات", "profile": "الملف الشخصي"
      },
      "clientPortal": "بوابة العملاء", "sidebarAria": "قائمة بوابة العملاء", "menuSr": "القائمة",
      "pushEnabled": "إشعارات سطح المكتب مفعّلة", "pushEnable": "تفعيل إشعارات سطح المكتب"
    },
    "sidebar": {
      "pinned": "المثبّتات", "pin": "تثبيت", "unpin": "إزالة من المثبّتات",
      "badgeNew": "{count} جديد", "mainNavAria": "القائمة الجانبية الرئيسية",
      "guide": "دليل الاستخدام", "guideHint": "شرح جميع وحدات النظام ونصائح الاستخدام",
      "collapse": "طي القائمة", "expand": "توسيع القائمة"
    },
    "topbar": {
      "toggleTheme": "تبديل الوضع", "lightMode": "الوضع النهاري", "darkMode": "الوضع الليلي",
      "userMenu": "قائمة المستخدم", "profile": "الملف الشخصي", "logout": "تسجيل الخروج",
      "roleAdmin": "مسؤول", "roleEmployee": "موظف"
    },
    "breadcrumb": { "navAria": "التنقل", "back": "رجوع" },
    "palette": {
      "searchAria": "البحث", "placeholder": "ابحث عن صفحة أو إجراء...", "noResults": "لا توجد نتائج",
      "quickActions": "إجراءات سريعة", "recent": "تمت زيارتها مؤخراً",
      "groupWork": "العمل", "groupHr": "الموارد البشرية", "groupFinance": "المالية", "groupAdmin": "الإدارة",
      "searchButton": "بحث...",
      "actions": { "newInvoice": "فاتورة جديدة", "newQuote": "عرض سعر جديد", "newClient": "عميل جديد", "newProject": "مشروع جديد" },
      "items": {
        "dashboard": "لوحة التحكم", "projects": "المشاريع", "clients": "العملاء", "quotes": "عروض الأسعار", "invoices": "الفواتير", "boards": "لوحات العمل", "scripts": "السكريبتات", "contentPipeline": "خط الإنتاج", "files": "الملفات", "favorites": "المفضلة", "reviews": "المراجعات", "trash": "سلة المحذوفات", "storage": "التخزين", "notifications": "الإشعارات",
        "attendance": "الحضور والانصراف", "leave": "الإجازات", "payroll": "الرواتب", "myPayslip": "كشف راتبي", "timesheet": "ساعات العمل", "evaluations": "تقييم الأداء", "directory": "دليل الفريق", "announcements": "الإعلانات", "orgChart": "الهيكل التنظيمي",
        "financeHome": "الإدارة المالية", "expenses": "المصاريف", "cards": "البطاقات", "contracts": "العقود", "recurring": "الفواتير المتكررة", "financeReports": "التقارير المالية", "revenueTargets": "أهداف الإيرادات", "financeAlerts": "التنبيهات المالية",
        "settings": "الإعدادات", "users": "المستخدمون", "teams": "الفرق", "roles": "الأدوار", "permissions": "الصلاحيات", "automations": "الأتمتة", "knowledgeBase": "قاعدة المعرفة", "integrations": "التكاملات", "activityLog": "سجل النشاط", "loginHistory": "سجل الدخول", "sessions": "الجلسات", "reports": "التقارير"
      }
    },
    "bell": {
      "title": "الإشعارات", "today": "اليوم", "yesterday": "أمس",
      "muteSound": "كتم صوت الإشعارات", "unmuteSound": "تفعيل صوت الإشعارات",
      "markAllRead": "تعليم الكل كمقروء", "empty": "لا توجد إشعارات", "viewAll": "عرض كل الإشعارات",
      "unreadAria": "{count} إشعار جديد"
    },
    "segments": {
      "dashboard": "الرئيسية", "files": "الملفات", "users": "المستخدمون", "teams": "الفرق", "permissions": "الصلاحيات", "quotes": "عروض الأسعار", "invoices": "الفواتير", "notifications": "الإشعارات", "activity": "النشاط", "trash": "المحذوفات", "settings": "الإعدادات", "clients": "العملاء", "projects": "المشاريع", "reviews": "المراجعات", "finance": "الإدارة المالية", "expenses": "المصاريف", "contracts": "العقود", "cards": "البطاقات", "recurring": "الفواتير المتكررة", "targets": "أهداف الإيرادات", "statements": "البيانات المالية", "favorites": "المفضلة", "script-reviews": "مراجعات السكريبتات", "knowledge-base": "قاعدة المعرفة", "login-history": "سجل الدخول", "sessions": "الجلسات", "integrations": "التكاملات", "reports": "التقارير", "new": "جديد", "boards": "لوحات العمل", "roles": "الأدوار", "automations": "الأتمتة", "attendance": "الحضور", "leave": "الإجازات", "payroll": "الرواتب", "evaluations": "التقييمات", "directory": "الدليل", "announcements": "الإعلانات", "timesheet": "ساعات العمل", "org-chart": "الهيكل التنظيمي", "content-pipeline": "خط الإنتاج", "my-payslips": "كشف الراتب", "my-tasks": "مهامي", "storage": "التخزين", "guide": "الدليل الإرشادي", "portal": "البوابة", "help": "مركز المساعدة", "scripts": "السكريبتات", "profile": "الملف الشخصي",
      "crm": "CRM", "sales": "المبيعات", "pipeline": "خط المبيعات", "customers": "العملاء", "follow-ups": "المتابعات", "approvals": "الموافقات", "hr": "الموارد البشرية", "my-documents": "وثائقي", "calendar": "التقويم", "onboarding": "تعيين موظفين", "work-schedules": "جداول العمل", "leave-balances": "أرصدة الإجازات", "chat": "محادثات واتساب", "whatsapp-analytics": "تحليلات واتساب", "whatsapp-campaigns": "حملات الرسائل", "credit-notes": "الإشعارات الدائنة", "suppliers": "الموردين", "purchase-orders": "أوامر الشراء", "admin": "الإدارة", "error-logs": "سجل الأخطاء", "documents": "الوثائق", "productivity": "تقرير الإنتاجية", "analytics": "التحليلات"
    }
  }
}
```

- [ ] **Step 3: Create `messages/en/nav.json`** — identical structure, English values:

```json
{
  "nav": {
    "groups": {
      "main":        { "title": "Main", "description": "Dashboard, notifications and your personal tasks" },
      "business":    { "title": "Business", "description": "Projects, clients, invoices and quotes" },
      "content":     { "title": "Content & Files", "description": "Content reviews, files and storage" },
      "crm":         { "title": "CRM", "description": "Leads, sales pipeline, WhatsApp and follow-ups" },
      "hr":          { "title": "Human Resources", "description": "Admin view of attendance, leave, payroll, documents and evaluations" },
      "selfService": { "title": "Self-Service", "description": "My personal items: attendance, leave, payslips, documents and team directory" },
      "finance":     { "title": "Finance", "description": "Expenses, contracts, subscriptions and financial reports" },
      "team":        { "title": "Team Management", "description": "Users, roles and permissions" },
      "tools":       { "title": "Tools", "description": "System settings, automations, knowledge base and integrations" },
      "security":    { "title": "Security & Monitoring", "description": "Login history, activity logs and active sessions" }
    },
    "items": {
      "dashboard": "Home", "notifications": "Notifications", "profile": "My Profile", "myTasks": "My Tasks", "calendar": "Calendar",
      "projects": "Projects", "clients": "Clients", "quotes": "Quotes", "quotesAnalytics": "Quote Analytics", "invoices": "Invoices", "boards": "Boards",
      "scriptReviews": "Script Reviews", "contentPipeline": "Content Pipeline", "files": "Files", "favorites": "Favorites", "reviews": "Reviews", "trash": "Trash", "storage": "Storage",
      "crmDashboard": "Sales Dashboard", "pipeline": "Pipeline", "customers": "Customers", "whatsappChat": "WhatsApp Chats", "whatsappAnalytics": "WhatsApp Analytics", "whatsappCampaigns": "Message Campaigns", "followUps": "Follow-ups", "crmApprovals": "Pending Approvals", "quoteApprovals": "Quote Approvals", "salesSettings": "Sales Settings",
      "hrOverview": "Overview", "productivity": "Productivity Report", "teamApprovals": "Approvals", "leaveSettings": "Leave Settings", "leaveBalances": "Leave Balances", "evaluationSettings": "Evaluation Settings", "hrDocuments": "Employee Documents", "documentTypes": "Document Types", "onboarding": "Onboarding", "workSchedules": "Work Schedules",
      "timesheet": "Timesheet", "attendance": "Attendance", "leave": "Leave", "myPayslips": "My Payslips", "evaluations": "Performance", "myDocuments": "My Documents", "directory": "Team Directory", "announcements": "Announcements", "orgChart": "Org Chart",
      "financeHome": "Finance", "expenses": "Expenses", "cards": "Cards", "contracts": "Contracts", "recurring": "Recurring Invoices", "creditNotes": "Credit Notes", "suppliers": "Suppliers", "purchaseOrders": "Purchase Orders", "financeReports": "Financial Reports", "revenueTargets": "Revenue Targets", "payroll": "Payroll",
      "users": "Users", "teams": "Teams", "roles": "Roles", "permissions": "Permissions",
      "settings": "Settings", "reports": "Reports", "automations": "Automations", "knowledgeBase": "Knowledge Base", "integrations": "Integrations",
      "activityLog": "Activity Log", "loginHistory": "Login History", "sessions": "Sessions", "errorLogs": "Error Logs"
    },
    "portal": {
      "items": {
        "home": "Home", "projects": "Projects", "files": "Files", "quotes": "Quotes", "contracts": "Contracts", "invoices": "Invoices", "creditNotes": "Credit Notes", "recurring": "Recurring Invoices", "statement": "Statement", "scripts": "Scripts", "help": "Help Center", "notifications": "Notifications", "profile": "Profile"
      },
      "clientPortal": "Client Portal", "sidebarAria": "Client portal navigation", "menuSr": "Menu",
      "pushEnabled": "Desktop notifications enabled", "pushEnable": "Enable desktop notifications"
    },
    "sidebar": {
      "pinned": "Pinned", "pin": "Pin", "unpin": "Remove from pinned",
      "badgeNew": "{count} new", "mainNavAria": "Main sidebar navigation",
      "guide": "User Guide", "guideHint": "Explains every module with usage tips",
      "collapse": "Collapse menu", "expand": "Expand menu"
    },
    "topbar": {
      "toggleTheme": "Toggle theme", "lightMode": "Light mode", "darkMode": "Dark mode",
      "userMenu": "User menu", "profile": "Profile", "logout": "Log out",
      "roleAdmin": "Admin", "roleEmployee": "Employee"
    },
    "breadcrumb": { "navAria": "Navigation", "back": "Back" },
    "palette": {
      "searchAria": "Search", "placeholder": "Search for a page or action...", "noResults": "No results",
      "quickActions": "Quick Actions", "recent": "Recently Visited",
      "groupWork": "Work", "groupHr": "Human Resources", "groupFinance": "Finance", "groupAdmin": "Administration",
      "searchButton": "Search...",
      "actions": { "newInvoice": "New Invoice", "newQuote": "New Quote", "newClient": "New Client", "newProject": "New Project" },
      "items": {
        "dashboard": "Dashboard", "projects": "Projects", "clients": "Clients", "quotes": "Quotes", "invoices": "Invoices", "boards": "Boards", "scripts": "Scripts", "contentPipeline": "Content Pipeline", "files": "Files", "favorites": "Favorites", "reviews": "Reviews", "trash": "Trash", "storage": "Storage", "notifications": "Notifications",
        "attendance": "Attendance", "leave": "Leave", "payroll": "Payroll", "myPayslip": "My Payslip", "timesheet": "Timesheet", "evaluations": "Performance", "directory": "Team Directory", "announcements": "Announcements", "orgChart": "Org Chart",
        "financeHome": "Finance", "expenses": "Expenses", "cards": "Cards", "contracts": "Contracts", "recurring": "Recurring Invoices", "financeReports": "Financial Reports", "revenueTargets": "Revenue Targets", "financeAlerts": "Finance Alerts",
        "settings": "Settings", "users": "Users", "teams": "Teams", "roles": "Roles", "permissions": "Permissions", "automations": "Automations", "knowledgeBase": "Knowledge Base", "integrations": "Integrations", "activityLog": "Activity Log", "loginHistory": "Login History", "sessions": "Sessions", "reports": "Reports"
      }
    },
    "bell": {
      "title": "Notifications", "today": "Today", "yesterday": "Yesterday",
      "muteSound": "Mute notification sound", "unmuteSound": "Unmute notification sound",
      "markAllRead": "Mark all as read", "empty": "No notifications", "viewAll": "View all notifications",
      "unreadAria": "{count} new notifications"
    },
    "segments": {
      "dashboard": "Home", "files": "Files", "users": "Users", "teams": "Teams", "permissions": "Permissions", "quotes": "Quotes", "invoices": "Invoices", "notifications": "Notifications", "activity": "Activity", "trash": "Trash", "settings": "Settings", "clients": "Clients", "projects": "Projects", "reviews": "Reviews", "finance": "Finance", "expenses": "Expenses", "contracts": "Contracts", "cards": "Cards", "recurring": "Recurring Invoices", "targets": "Revenue Targets", "statements": "Statements", "favorites": "Favorites", "script-reviews": "Script Reviews", "knowledge-base": "Knowledge Base", "login-history": "Login History", "sessions": "Sessions", "integrations": "Integrations", "reports": "Reports", "new": "New", "boards": "Boards", "roles": "Roles", "automations": "Automations", "attendance": "Attendance", "leave": "Leave", "payroll": "Payroll", "evaluations": "Evaluations", "directory": "Directory", "announcements": "Announcements", "timesheet": "Timesheet", "org-chart": "Org Chart", "content-pipeline": "Content Pipeline", "my-payslips": "My Payslips", "my-tasks": "My Tasks", "storage": "Storage", "guide": "Guide", "portal": "Portal", "help": "Help Center", "scripts": "Scripts", "profile": "Profile",
      "crm": "CRM", "sales": "Sales", "pipeline": "Pipeline", "customers": "Customers", "follow-ups": "Follow-ups", "approvals": "Approvals", "hr": "Human Resources", "my-documents": "My Documents", "calendar": "Calendar", "onboarding": "Onboarding", "work-schedules": "Work Schedules", "leave-balances": "Leave Balances", "chat": "WhatsApp Chats", "whatsapp-analytics": "WhatsApp Analytics", "whatsapp-campaigns": "Message Campaigns", "credit-notes": "Credit Notes", "suppliers": "Suppliers", "purchase-orders": "Purchase Orders", "admin": "Admin", "error-logs": "Error Logs", "documents": "Documents", "productivity": "Productivity", "analytics": "Analytics"
    }
  }
}
```

- [ ] **Step 4: Create `messages/ar/auth.json` + `messages/en/auth.json`**

`messages/ar/auth.json` (AR values verbatim from the login/forgot/reset pages — see discovery inventory):

```json
{
  "auth": {
    "login": {
      "title": "تسجيل الدخول", "dashboardSubtitle": "أدخل بياناتك للوصول إلى لوحة التحكم",
      "email": "البريد الإلكتروني", "password": "كلمة المرور",
      "submit": "تسجيل الدخول", "submitting": "جاري تسجيل الدخول...",
      "invalidCredentials": "البريد الإلكتروني أو كلمة المرور غير صحيحة",
      "emailNotConfirmed": "لم يتم تأكيد البريد الإلكتروني",
      "unexpected": "حدث خطأ غير متوقع. حاول مرة أخرى.",
      "tagline": "منصة إدارة الملفات والمشاريع الذكية"
    },
    "portalLogin": {
      "badge": "بوابة العملاء", "subtitle": "أدخل بياناتك للوصول إلى بوابة العملاء",
      "tagline": "تابع مشاريعك، استعرض الملفات، وتواصل مع فريق العمل بسهولة",
      "rememberMe": "تذكرني", "forgotPassword": "نسيت كلمة المرور؟",
      "loginError": "حدث خطأ أثناء تسجيل الدخول"
    },
    "forgot": {
      "title": "استعادة كلمة المرور",
      "subtitle": "أدخل بريدك الإلكتروني وسنرسل لك رابط إعادة تعيين كلمة المرور",
      "sent": "تم إرسال الرابط بنجاح",
      "sentDetail": "إذا كان البريد الإلكتروني مسجلاً لدينا، ستصلك رسالة تحتوي على رابط إعادة تعيين كلمة المرور.",
      "backToLogin": "العودة لتسجيل الدخول",
      "submit": "إرسال رابط الاستعادة", "submitting": "جاري الإرسال...",
      "requestError": "حدث خطأ أثناء إرسال الطلب"
    },
    "reset": {
      "title": "إعادة تعيين كلمة المرور", "subtitle": "أدخل كلمة المرور الجديدة لحسابك",
      "newPassword": "كلمة المرور الجديدة", "confirmPassword": "تأكيد كلمة المرور",
      "minLength": "يجب أن تتكون كلمة المرور من {min} أحرف على الأقل",
      "mismatch": "كلمتا المرور غير متطابقتين",
      "missingToken": "رمز إعادة التعيين مفقود. يرجى استخدام الرابط المرسل إلى بريدك الإلكتروني.",
      "success": "تم إعادة تعيين كلمة المرور بنجاح",
      "redirecting": "سيتم تحويلك لصفحة تسجيل الدخول خلال لحظات...",
      "loginNow": "تسجيل الدخول الآن",
      "submit": "إعادة تعيين كلمة المرور", "submitting": "جاري إعادة التعيين...",
      "resetError": "حدث خطأ أثناء إعادة تعيين كلمة المرور"
    },
    "api": {
      "emailRequired": "البريد الإلكتروني مطلوب",
      "rateLimitMinutes": "تجاوزت الحد المسموح. حاول مرة أخرى بعد {retry} دقيقة",
      "resetLinkSent": "تم إرسال رابط إعادة تعيين كلمة المرور إلى بريدك الإلكتروني",
      "tokenRequired": "رمز إعادة التعيين مطلوب",
      "passwordRequired": "كلمة المرور مطلوبة ({min} أحرف على الأقل)",
      "tokenInvalidOrExpired": "رمز إعادة التعيين غير صالح أو منتهي الصلاحية",
      "tokenExpired": "رمز إعادة التعيين منتهي الصلاحية",
      "tokenInvalid": "رمز إعادة التعيين غير صالح",
      "clientNotFound": "العميل غير موجود",
      "accountDisabled": "تم تعطيل حسابك. يرجى التواصل مع فريق الدعم",
      "resetUnsupported": "حسابك لا يدعم إعادة تعيين كلمة المرور عبر هذا الرابط. يرجى التواصل مع فريق الدعم",
      "updateFailed": "فشل في تحديث كلمة المرور. يرجى المحاولة مرة أخرى",
      "resetSuccess": "تم إعادة تعيين كلمة المرور بنجاح. يرجى تسجيل الدخول بكلمة المرور الجديدة"
    }
  }
}
```

`messages/en/auth.json`:

```json
{
  "auth": {
    "login": {
      "title": "Sign In", "dashboardSubtitle": "Enter your credentials to access the dashboard",
      "email": "Email", "password": "Password",
      "submit": "Sign In", "submitting": "Signing in...",
      "invalidCredentials": "Incorrect email or password",
      "emailNotConfirmed": "Email has not been confirmed",
      "unexpected": "An unexpected error occurred. Please try again.",
      "tagline": "Smart file & project management platform"
    },
    "portalLogin": {
      "badge": "Client Portal", "subtitle": "Enter your credentials to access the client portal",
      "tagline": "Track your projects, browse files, and stay in touch with the team",
      "rememberMe": "Remember me", "forgotPassword": "Forgot your password?",
      "loginError": "An error occurred while signing in"
    },
    "forgot": {
      "title": "Recover Password",
      "subtitle": "Enter your email and we will send you a password reset link",
      "sent": "Link sent successfully",
      "sentDetail": "If this email is registered with us, you will receive a message containing a password reset link.",
      "backToLogin": "Back to sign in",
      "submit": "Send Recovery Link", "submitting": "Sending...",
      "requestError": "An error occurred while sending the request"
    },
    "reset": {
      "title": "Reset Password", "subtitle": "Enter the new password for your account",
      "newPassword": "New Password", "confirmPassword": "Confirm Password",
      "minLength": "Password must be at least {min} characters",
      "mismatch": "Passwords do not match",
      "missingToken": "Reset token is missing. Please use the link sent to your email.",
      "success": "Password reset successfully",
      "redirecting": "Redirecting you to the sign-in page...",
      "loginNow": "Sign in now",
      "submit": "Reset Password", "submitting": "Resetting...",
      "resetError": "An error occurred while resetting the password"
    },
    "api": {
      "emailRequired": "Email is required",
      "rateLimitMinutes": "Too many attempts. Try again in {retry} minutes",
      "resetLinkSent": "A password reset link has been sent to your email",
      "tokenRequired": "Reset token is required",
      "passwordRequired": "Password is required (at least {min} characters)",
      "tokenInvalidOrExpired": "Reset token is invalid or expired",
      "tokenExpired": "Reset token has expired",
      "tokenInvalid": "Reset token is invalid",
      "clientNotFound": "Client not found",
      "accountDisabled": "Your account has been disabled. Please contact support",
      "resetUnsupported": "Your account does not support password reset via this link. Please contact support",
      "updateFailed": "Failed to update the password. Please try again",
      "resetSuccess": "Password reset successfully. Please sign in with your new password"
    }
  }
}
```

- [ ] **Step 5: Register namespaces + types**

- `lib/i18n/messages.ts`: `export const NAMESPACE_FILES = ['common', 'nav', 'auth', 'statuses'] as const;`
- `i18n/global.ts`:
```ts
import type common from '../messages/ar/common.json';
import type nav from '../messages/ar/nav.json';
import type auth from '../messages/ar/auth.json';
import type statuses from '../messages/ar/statuses.json';
type Messages = typeof common & typeof nav & typeof auth & typeof statuses;
```

- [ ] **Step 6: Verify + commit**

```bash
pnpm test && pnpm run check && pnpm i18n:check && pnpm build
git add messages lib/i18n/messages.ts i18n/global.ts
git commit -m "feat(i18n): common/nav/auth catalogs — full app-shell key contract AR+EN (Phase 1.2)"
```

---

### Task 13: Nav config extraction + sidebar + mobile-nav on catalog keys

**Files:**
- Create: `components/layout/nav-config.ts`
- Modify: `components/layout/sidebar.tsx`, `components/layout/mobile-nav.tsx`

**Interfaces:**
- Consumes: `nav.groups.*` / `nav.items.*` / `nav.sidebar.*` keys (Task 12).
- Produces: `NAV_GROUPS: NavGroupConfig[]`, `ALL_NAV_ITEMS: NavItemConfig[]` from `components/layout/nav-config.ts` — Task 15 (palette) may reference; shapes:
```ts
import type nav from '@/messages/ar/nav.json';

// Deriving key unions from the AR catalog makes t(`items.${item.key}`)
// type-check against next-intl's typed keys (template literals over literal
// unions infer a key union — no casts needed in the render paths).
export type NavItemKey = keyof typeof nav.nav.items;
export type NavGroupKey = keyof typeof nav.nav.groups;

export interface NavItemConfig {
  key: NavItemKey;   // nav.items.<key>
  href: string;
  icon: React.ComponentType<{ className?: string }>;
  permission?: string;
  badgeKey?: 'notifications' | 'overdue_invoices' | 'pending_approvals' | 'unassigned_conversations' | 'team_approvals' | 'follow_ups_pending' | 'crm_pending_approvals';
}
export interface NavGroupConfig {
  key: NavGroupKey;   // nav.groups.<key>
  storageKey: string; // legacy titleEn — PRESERVED verbatim (localStorage collapse-state key)
  items: NavItemConfig[];
}
```

- [ ] **Step 1: Create `components/layout/nav-config.ts`**

MOVE the `navGroups` array out of `sidebar.tsx` unchanged EXCEPT: drop `label`/`labelEn`/`title`/`titleEn`/`description` fields; add `key` per this DEFINITIVE href→key mapping (keys are the `nav.items.*`/`nav.groups.*` names from Task 12 — they were designed 1:1); set `storageKey` to the OLD `titleEn` value verbatim (`'Main'`, `'Business'`, `'Content & Files'`, `'CRM'`, `'HR'`, `'Self-Service'`, `'Finance'`, `'Team Management'`, `'Tools'`, `'Security'`). Preserve every `icon`/`permission`/`badgeKey` byte-identical.

Group keys in order: `main`, `business`, `content`, `crm`, `hr`, `selfService`, `finance`, `team`, `tools`, `security`.

Item href→key mapping (complete — 64 items):

| href | key |  | href | key |
|---|---|---|---|---|
| /dashboard | dashboard | | /dashboard/timesheet | timesheet |
| /dashboard/notifications | notifications | | /dashboard/attendance | attendance |
| /dashboard/profile | profile | | /dashboard/leave | leave |
| /dashboard/my-tasks | myTasks | | /dashboard/my-payslips | myPayslips |
| /dashboard/calendar | calendar | | /dashboard/evaluations | evaluations |
| /dashboard/projects | projects | | /dashboard/my-documents | myDocuments |
| /dashboard/clients | clients | | /dashboard/directory | directory |
| /dashboard/quotes | quotes | | /dashboard/announcements | announcements |
| /dashboard/quotes/analytics | quotesAnalytics | | /dashboard/org-chart | orgChart |
| /dashboard/invoices | invoices | | /dashboard/finance | financeHome |
| /dashboard/boards | boards | | /dashboard/finance/expenses | expenses |
| /dashboard/script-reviews | scriptReviews | | /dashboard/finance/cards | cards |
| /dashboard/content-pipeline | contentPipeline | | /dashboard/finance/contracts | contracts |
| /dashboard/files | files | | /dashboard/finance/recurring | recurring |
| /dashboard/favorites | favorites | | /dashboard/finance/credit-notes | creditNotes |
| /dashboard/reviews | reviews | | /dashboard/finance/suppliers | suppliers |
| /dashboard/trash | trash | | /dashboard/finance/purchase-orders | purchaseOrders |
| /dashboard/storage | storage | | /dashboard/finance/reports | financeReports |
| /dashboard/crm | crmDashboard | | /dashboard/finance/targets | revenueTargets |
| /dashboard/crm/pipeline | pipeline | | /dashboard/payroll | payroll |
| /dashboard/crm/customers | customers | | /dashboard/users | users |
| /dashboard/sales/chat | whatsappChat | | /dashboard/teams | teams |
| /dashboard/sales/whatsapp-analytics | whatsappAnalytics | | /dashboard/roles | roles |
| /dashboard/sales/whatsapp-campaigns | whatsappCampaigns | | /dashboard/permissions | permissions |
| /dashboard/crm/follow-ups | followUps | | /dashboard/settings | settings |
| /dashboard/crm/approvals | crmApprovals | | /dashboard/reports | reports |
| /dashboard/sales/approvals | quoteApprovals | | /dashboard/automations | automations |
| /dashboard/sales/settings | salesSettings | | /dashboard/knowledge-base | knowledgeBase |
| /dashboard/hr | hrOverview | | /dashboard/integrations | integrations |
| /dashboard/hr/productivity | productivity | | /dashboard/activity | activityLog |
| /dashboard/approvals | teamApprovals | | /dashboard/login-history | loginHistory |
| /dashboard/leave/settings | leaveSettings | | /dashboard/sessions | sessions |
| /dashboard/hr/leave-balances | leaveBalances | | /dashboard/admin/error-logs | errorLogs |
| /dashboard/evaluations/settings | evaluationSettings | | | |
| /dashboard/hr/documents | hrDocuments | | | |
| /dashboard/hr/documents/settings | documentTypes | | | |
| /dashboard/hr/onboarding | onboarding | | | |
| /dashboard/hr/work-schedules | workSchedules | | | |

Also export: `export const ALL_NAV_ITEMS = NAV_GROUPS.flatMap((g) => g.items);`

- [ ] **Step 2: Rewire `sidebar.tsx`**

- Import: `import { useTranslations } from 'next-intl';` + `import { NAV_GROUPS, ALL_NAV_ITEMS, type NavItemConfig } from './nav-config';` — delete the in-file `navGroups`/interfaces/ALL_NAV_ITEMS.
- Inside the component: `const t = useTranslations('nav');`
- Replace renders: `{item.label}` → `{t(\`items.${item.key}\`)}` (item render + collapsed tooltip); `{group.title}` → `{t(\`groups.${group.key}.title\`)}`; `{group.description}` → `{t(\`groups.${group.key}.description\`)}`.
- localStorage collapse logic: `group.titleEn` → `group.storageKey` (values unchanged → users keep their collapse state), incl. `NEW_GROUP_NAMES = NAV_GROUPS.map((g) => g.storageKey)`.
- Chrome strings → keys: L442 pin/unpin → `t('sidebar.pin')`/`t('sidebar.unpin')`; L472 → `t('sidebar.badgeNew', { count: badgeCount })`; L483 → `t('sidebar.mainNavAria')`; L518 → `t('sidebar.pinned')`; L592/596 → `t('sidebar.guide')`/`t('sidebar.guideHint')`; L605/612 → `t('sidebar.collapse')`/`t('sidebar.expand')`.
- The `MODULE_GUIDES[item.href]?.description` tooltip line stays (Arabic guide content = Phase 6) — it is data from lib/config, not a literal in this file.

- [ ] **Step 3: Rewire `mobile-nav.tsx`**

Delete its inline duplicate nav list; import `NAV_GROUPS` and render from the shared config with `t(\`items.${item.key}\`)` — keep its own markup/permission handling identical (it renders the same groups; adapt its item loop to the config shape). Translate any chrome strings in the file via `nav.sidebar.*` / add keys if new ones surface (Global Constraints rule).

- [ ] **Step 4: Verify**

```bash
pnpm run check && pnpm build && pnpm dev
```
- AR: sidebar visually IDENTICAL (labels, order, badges, pinned, collapse state preserved across reload).
- EN: all groups/items/descriptions English; permission filtering unchanged (log in as a sales agent if available — same items as before per role).
- Mobile viewport: hamburger nav mirrors sidebar in both locales.

- [ ] **Step 5: Commit**

```bash
git add components/layout/nav-config.ts components/layout/sidebar.tsx components/layout/mobile-nav.tsx
git commit -m "feat(i18n): single nav-config + sidebar/mobile-nav on nav catalog keys (Phase 1.3)"
```

---

### Task 14: Breadcrumb + Topbar

**Files:**
- Modify: `components/layout/breadcrumb.tsx`, `components/layout/topbar.tsx`, `app/dashboard/layout.tsx`

**Interfaces:**
- Consumes: `nav.segments.*`, `nav.breadcrumb.*`, `nav.topbar.*` (Task 12); `AuthSession.pyraUser.role_name` (Task 4).
- Produces: locale-aware breadcrumbs incl. the previously-missing CRM/HR segments; topbar fully translated with locale-aware role display.

- [ ] **Step 1: Breadcrumb**

In `components/layout/breadcrumb.tsx`:
- Delete the `routeLabels` map. Add `const t = useTranslations('nav.segments');`.
- Both lookups (mobile L89 + desktop L101) become (cast: `segment` is a runtime URL string; `t.has()` guards it):
```ts
const segmentKey = segment as Parameters<typeof t>[0];
const label = t.has(segmentKey) ? t(segmentKey) : decodeURIComponent(segment);
```
- Add a second hook `const tb = useTranslations('nav.breadcrumb');` — L73 `aria-label="التنقل"` → `aria-label={tb('navAria')}`; L81 back `aria-label` → `tb('back')`.
- Back-button icon direction fix (currently `ArrowRight` with no rtl handling — wrong in LTR): swap to LTR-semantic + utility per the Phase 15.1 §7 lock:
```tsx
<ArrowLeft className="h-4 w-4 rtl:rotate-180" />
```
(RTL renders it pointing right = back in Arabic reading flow — visually identical to today; LTR points left = back. Update the lucide import.)

- [ ] **Step 2: Topbar**

In `components/layout/topbar.tsx`:
- `const t = useTranslations('nav.topbar');` + `const locale = useLocale();`
- L84 → `aria-label={t('toggleTheme')}`; L90 → `{theme === 'dark' ? t('lightMode') : t('darkMode')}`; L96 → `t('userMenu')`; L116 → `t('profile')`; L121 → `t('logout')`.
- Role line (L109) becomes locale-aware:
```tsx
{(locale === 'ar' ? user.role_name_ar : user.role_name) ??
  t(user.role === 'admin' ? 'roleAdmin' : 'roleEmployee')} · @{user.username}
```
- Extend the Topbar `user` prop type with `role_name?: string | null;`.

- [ ] **Step 3: Pass role_name through the layout**

In `app/dashboard/layout.tsx` add to `layoutUser`:
```ts
    role_name: session.pyraUser.role_name,
```

- [ ] **Step 4: Verify + commit**

AR identical / EN translated (breadcrumbs on `/dashboard/crm/pipeline` + `/dashboard/hr/documents` now show proper labels instead of raw segments — in BOTH languages; this fixes a pre-existing gap).

```bash
pnpm run check && pnpm build
git add components/layout/breadcrumb.tsx components/layout/topbar.tsx app/dashboard/layout.tsx
git commit -m "feat(i18n): breadcrumb segments + topbar on catalogs, locale-aware role, back-arrow dir fix (Phase 1.4)"
```

---

### Task 15: CommandPalette

**Files:**
- Modify: `components/layout/CommandPalette.tsx`

**Interfaces:**
- Consumes: `nav.palette.*` keys (Task 12).
- Produces: fully localized palette; recents stored by path (locale-independent).

- [ ] **Step 1: Item arrays → keys**

`const t = useTranslations('nav.palette');` + `const locale = useLocale();`
- `QUICK_ACTIONS`: replace each `label: '<Arabic>'` with `key: 'newInvoice' | 'newQuote' | 'newClient' | 'newProject'`; render `t(\`actions.${item.key}\`)`. APPEND the English label words to each `keywords` string (e.g. `keywords: 'new invoice فاتورة جديدة إنشاء'` — already mixed; ensure the EN label words are present so EN users can search).
- `WORK_ITEMS`/`HR_ITEMS`/`FINANCE_ITEMS`/`ADMIN_ITEMS`: replace `label` with `key` per this mapping (labels shown are the CURRENT Arabic — for verification):

| Arabic label (current) | key | | Arabic label (current) | key |
|---|---|---|---|---|
| لوحة التحكم | dashboard | | الإدارة المالية | financeHome |
| المشاريع | projects | | المصاريف | expenses |
| العملاء | clients | | البطاقات | cards |
| عروض الأسعار | quotes | | العقود | contracts |
| الفواتير | invoices | | الفواتير المتكررة | recurring |
| لوحات العمل | boards | | التقارير المالية | financeReports |
| السكريبتات | scripts | | أهداف الإيرادات | revenueTargets |
| خط الإنتاج | contentPipeline | | التنبيهات المالية | financeAlerts |
| الملفات | files | | الإعدادات | settings |
| المفضلة | favorites | | المستخدمون | users |
| المراجعات | reviews | | الفرق | teams |
| سلة المحذوفات | trash | | الأدوار | roles |
| التخزين | storage | | الصلاحيات | permissions |
| الإشعارات | notifications | | الأتمتة | automations |
| الحضور والانصراف | attendance | | قاعدة المعرفة | knowledgeBase |
| الإجازات | leave | | التكاملات | integrations |
| الرواتب | payroll | | سجل النشاط | activityLog |
| كشف راتبي | myPayslip | | سجل الدخول | loginHistory |
| ساعات العمل | timesheet | | الجلسات | sessions |
| تقييم الأداء | evaluations | | التقارير | reports |
| دليل الفريق | directory | | | |
| الإعلانات | announcements | | | |
| الهيكل التنظيمي | orgChart | | | |

Render with `t(\`items.${item.key}\`)`; append EN label words to each `keywords`. Type the arrays' `key` fields against the catalog so dynamic keys type-check:
```ts
import type navMessages from '@/messages/ar/nav.json';
type PaletteItemKey = keyof typeof navMessages.nav.palette.items;
type PaletteActionKey = keyof typeof navMessages.nav.palette.actions;
```

- [ ] **Step 2: Chrome strings**

L210 → `t('searchAria')`; L215 placeholder → `t('placeholder')` and `dir="rtl"` → `dir={locale === 'ar' ? 'rtl' : 'ltr'}`; L217 → `t('noResults')`; group headings L220/239/262/277/292/307 → `t('quickActions')` / `t('recent')` / `t('groupWork')` / `t('groupHr')` / `t('groupFinance')` / `t('groupAdmin')`; L334 SearchTrigger → `t('searchButton')`; L246 search-value keyword string: keep functional (`value={\`${label} recent أخيرة\`}` with the resolved label).

- [ ] **Step 3: Locale-independent recents**

`addRecentPage(path, label)` currently persists the ARABIC label → stale after a locale switch. Change:
- Store `{ path }` only (keep reading legacy `{ path, label }` entries).
- At render, resolve the label: find the item across the four arrays (or `ALL_NAV_ITEMS` from Task 13) by `path` → `t(\`items.${key}\`)`; fall back to the legacy stored `label`, then to the raw path.

- [ ] **Step 4: Verify + commit**

AR: palette identical (labels, groups, quick actions, recents keep working). EN: everything English; searching "invoices" AND "فواتير" both hit.

```bash
pnpm run check && pnpm build
git add components/layout/CommandPalette.tsx
git commit -m "feat(i18n): command palette on catalogs — bilingual search, locale-safe recents (Phase 1.5)"
```

---

### Task 16: Shared UI chrome (bell, guide, table, pagination, inputs, errors)

**Files:**
- Modify: `components/layout/NotificationBell.tsx`, `components/ui/page-guide.tsx`, `components/ui/data-table.tsx`, `components/ui/pagination.tsx`, `components/ui/search-input.tsx`, `components/ui/sheet.tsx`, `components/ui/error-boundary.tsx`, `components/ui/error-card.tsx`, `components/ui/mention-textarea.tsx`

**Interfaces:**
- Consumes: `common.*` + `nav.bell.*` keys (Task 12).
- Produces: every shared primitive bilingual. `EmptyState`/`DataTable` remain prop-driven (callers translated in module phases).

- [ ] **Step 1: Per-file key swaps** (each file: add `useTranslations`, replace literals):

| File | Literal → Key |
|---|---|
| NotificationBell.tsx | day buckets `'اليوم'`/`'أمس'` → `nav.bell.today`/`yesterday`; title+aria `الإشعارات` → `nav.bell.title`; mute/unmute → `nav.bell.muteSound`/`unmuteSound`; `تعليم الكل كمقروء` → `nav.bell.markAllRead`; EmptyState title `لا توجد إشعارات` → `nav.bell.empty`; `عرض كل الإشعارات` → `nav.bell.viewAll` |
| page-guide.tsx | L45 aria → `common.guide.aria`; L48 → `common.guide.help`; L63 → `common.guide.title`; L74 → `common.guide.goal`; L84 → `common.guide.tips`; L114 → `common.guide.viewAll` (MODULE_GUIDES content itself stays Arabic — Phase 6) |
| data-table.tsx | L232 → `t('common.table.selectedCount', { count: selectedIds.size })`; L254 → `common.table.clearSelection`; L284 → `common.table.selectAll`; L357 → `common.table.selectRow` |
| pagination.tsx | L24 → `common.pagination.prev`; L29 → `t('common.pagination.pageOf', { page, totalPages })`; L38 → `common.pagination.next` |
| sheet.tsx | L68 sr-only → `common.srClose` |
| mention-textarea.tsx | L284 → `common.mentions.loading`; L289 → `common.mentions.noMatch`; L294 → `common.mentions.teamMembers` |

- [ ] **Step 2: Default-prop patterns** (hooks can't run in default params):

`search-input.tsx` — change the default:
```tsx
// before: placeholder = 'بحث...',
// after:
placeholder,
...
const t = useTranslations('common');
const effectivePlaceholder = placeholder ?? t('actions.search');
```
(+ L34 aria → `t('actions.clearSearch')`.)

`error-card.tsx` — same pattern: `title`/`message` props default to `undefined`; in-body `title ?? t('errors.loadFailed')`, `message ?? t('errors.serverConnection')`; L35 → `t('errors.checkInternet')`; L41 → `t('actions.retry')`.

- [ ] **Step 3: `error-boundary.tsx` (class component — hooks forbidden)**

Extract the fallback JSX into a small functional component in the same file:
```tsx
function ErrorFallback({ onRetry }: { onRetry: () => void }) {
  const t = useTranslations('common');
  return (
    /* same markup as today, with: */
    // heading  → {t('errors.unexpected')}
    // body     → {t('errors.sectionLoad')}
    // button   → {t('actions.retry')}
  );
}
```
The class's `render()` returns `<ErrorFallback onRetry={this.handleRetry} />` (wire to the existing reset handler). Markup/classes byte-identical.

- [ ] **Step 4: Verify + commit**

```bash
pnpm run check && pnpm build && pnpm dev
```
AR identical everywhere; EN: bell panel, palette trigger, a table with selection (e.g. invoices list), pagination, an error boundary (force one via React devtools if convenient — optional).

```bash
git add components/layout/NotificationBell.tsx components/ui/page-guide.tsx components/ui/data-table.tsx components/ui/pagination.tsx components/ui/search-input.tsx components/ui/sheet.tsx components/ui/error-boundary.tsx components/ui/error-card.tsx components/ui/mention-textarea.tsx
git commit -m "feat(i18n): shared UI chrome on catalogs — bell/guide/table/pagination/inputs/errors (Phase 1.6)"
```

---

### Task 17: Dashboard login page

**Files:**
- Modify: `app/(auth)/login/page.tsx`

**Interfaces:**
- Consumes: `auth.login.*` keys (Task 12); `LocaleToggleAnon` already placed (Task 7).
- Produces: bilingual dashboard login.

- [ ] **Step 1: Key swaps** — `const t = useTranslations('auth.login');`

| Line | Literal → Key |
|---|---|
| 37 | `t('invalidCredentials')` |
| 39 | `t('emailNotConfirmed')` |
| 41 | **raw `authError.message` passthrough → `t('unexpected')`** (Supabase English internals must not leak; the two specific cases above are already mapped) |
| 49 | `t('unexpected')` |
| 75 | `t('tagline')` |
| 92 | `t('title')` |
| 95 | `t('dashboardSubtitle')` |
| 101 | `t('email')` |
| 116 | `t('password')` |
| 155 | `t('submitting')` |
| 160 | `t('submit')` |

English brand text (`PYRAMEDIA X`, copyright) and placeholders stay as-is.

- [ ] **Step 2: Verify + commit**

Logged-out `/login` in both locales (toggle from Task 7): all strings flip; login works in both.

```bash
pnpm run check && pnpm build
git add "app/(auth)/login/page.tsx"
git commit -m "feat(i18n): dashboard login page bilingual + no raw Supabase error passthrough (Phase 1.7)"
```

---

### Task 18: Portal shell + portal auth pages + portal auth API messages

**Files:**
- Create: `components/portal/portal-nav-config.ts`
- Modify: `components/portal/portal-sidebar.tsx`, `components/portal/portal-mobile-nav.tsx`, `components/portal/portal-topbar.tsx`, `app/portal/(auth)/login/page.tsx`, `app/portal/(auth)/forgot-password/page.tsx`, `app/portal/(auth)/reset-password/page.tsx`, `app/api/portal/auth/login/route.ts`, `app/api/portal/auth/logout/route.ts`, `app/api/portal/auth/forgot-password/route.ts`, `app/api/portal/auth/reset-password/route.ts`

**Interfaces:**
- Consumes: `nav.portal.*`, `nav.topbar.*`, `auth.*` (Task 12).
- Produces: `PORTAL_NAV_ITEMS` config; **the getTranslations-in-route-handler pattern** every later module phase copies for API messages:
```ts
import { getTranslations } from 'next-intl/server';
// inside the handler:
const t = await getTranslations('auth.api');
return apiValidationError(t('emailRequired'));
```
(The request config reads the caller's cookie — no locale arg needed. Cron/webhook routes have no cookie → they resolve DEFAULT_LOCALE 'ar', which is correct.)

- [ ] **Step 1: `components/portal/portal-nav-config.ts`**

```ts
import {
  LayoutDashboard, FolderKanban, FolderOpen, FileText, FileSignature,
  Receipt, ReceiptText, RefreshCw, Wallet, ScrollText, HelpCircle, Bell, User,
} from 'lucide-react';

import type nav from '@/messages/ar/nav.json';

export type PortalNavItemKey = keyof typeof nav.nav.portal.items;

export interface PortalNavItemConfig {
  key: PortalNavItemKey; // nav.portal.items.<key>
  href: string;
  icon: React.ComponentType<{ className?: string }>;
  badgeKey?: 'notifications' | null;
}

export const PORTAL_NAV_ITEMS: PortalNavItemConfig[] = [
  { key: 'home', href: '/portal', icon: LayoutDashboard },
  { key: 'projects', href: '/portal/projects', icon: FolderKanban },
  { key: 'files', href: '/portal/files', icon: FolderOpen },
  { key: 'quotes', href: '/portal/quotes', icon: FileText },
  { key: 'contracts', href: '/portal/contracts', icon: FileSignature },
  { key: 'invoices', href: '/portal/invoices', icon: Receipt },
  { key: 'creditNotes', href: '/portal/credit-notes', icon: ReceiptText },
  { key: 'recurring', href: '/portal/recurring', icon: RefreshCw },
  { key: 'statement', href: '/portal/statement', icon: Wallet },
  { key: 'scripts', href: '/portal/scripts', icon: ScrollText },
  { key: 'help', href: '/portal/help', icon: HelpCircle },
  { key: 'notifications', href: '/portal/notifications', icon: Bell, badgeKey: 'notifications' },
  { key: 'profile', href: '/portal/profile', icon: User },
];
```
(Cross-check icons against the current `portal-sidebar.tsx` before deleting its inline list — icons above are from discovery.)

- [ ] **Step 2: Portal sidebar + mobile nav** — both consume `PORTAL_NAV_ITEMS` + `t(\`portal.items.${item.key}\`)` (namespace `nav`). Chrome: sidebar aria → `nav.portal.sidebarAria`; `بوابة العملاء` sub-label → `nav.portal.clientPortal`; mobile-nav sr-only `القائمة` → `nav.portal.menuSr`, SheetTitle → `nav.portal.sidebarAria`.

- [ ] **Step 3: Portal topbar** — push toggle aria+tooltip → `nav.portal.pushEnabled`/`pushEnable`; theme → `nav.topbar.lightMode`/`darkMode`; bell aria/tooltip → `t('bell.unreadAria', {count})` / `nav.bell.title`; dropdown → `nav.topbar.profile`/`logout`.

- [ ] **Step 4: Portal auth pages** — swap every Arabic literal per the `auth.portalLogin`/`auth.forgot`/`auth.reset` key tables (Task 12 Step 4 lists every string with its key; `minLength` uses `{min: PASSWORD_MIN_LENGTH}`).

- [ ] **Step 5: Portal auth API routes** — apply the getTranslations pattern (Interfaces block above) to the four routes. Key mapping: use `auth.api.*` — every Arabic literal in forgot/reset/logout maps 1:1 to the keys authored in Task 12 (`rateLimitMinutes` takes `{retry: retryMinutes}`, `passwordRequired` takes `{min: PASSWORD_MIN_LENGTH}`). For `login/route.ts` + `logout/route.ts` strings without a listed key: add keys under `auth.api` (both catalogs) per the Global Constraints rule. EXCEPTION: the `'عميل'` fallback display-name strings inside notification payloads are notification CONTENT (Phase 8 scope) — keep them, marked `// i18n-exempt: notification content (Phase 8)`.

- [ ] **Step 6: Verify + commit**

Portal in both locales: sidebar/topbar/mobile-nav chrome; login→forgot→reset flow shows localized API errors (submit an unknown email in EN → English error). AR flow byte-identical.

```bash
pnpm run check && pnpm build
git add components/portal app/portal/\(auth\) app/api/portal/auth
git commit -m "feat(i18n): portal shell + auth pages + auth API messages — establishes route-handler t() pattern (Phase 1.8)"
```

---

### Task 19: Phase 1 closure — guard manifest, full QA, CLAUDE.md, push

**Files:**
- Modify: `scripts/i18n-check.ts` (MIGRATED_PATHS), `CLAUDE.md`

- [ ] **Step 1: Expand MIGRATED_PATHS**

```ts
const MIGRATED_PATHS: string[] = [
  'i18n',
  'lib/i18n',
  'scripts/i18n-check.ts',
  // Phase 0
  'components/layout/locale-switcher.tsx',
  'components/layout/locale-sync.tsx',
  'components/layout/locale-toggle-anon.tsx',
  // Phase 1 — app shell
  'components/layout/nav-config.ts',
  'components/layout/sidebar.tsx',
  'components/layout/mobile-nav.tsx',
  'components/layout/topbar.tsx',
  'components/layout/breadcrumb.tsx',
  'components/layout/CommandPalette.tsx',
  'components/layout/NotificationBell.tsx',
  'components/ui/page-guide.tsx',
  'components/ui/data-table.tsx',
  'components/ui/pagination.tsx',
  'components/ui/search-input.tsx',
  'components/ui/sheet.tsx',
  'components/ui/error-boundary.tsx',
  'components/ui/error-card.tsx',
  'components/ui/mention-textarea.tsx',
  'components/portal/portal-nav-config.ts',
  'components/portal/portal-sidebar.tsx',
  'components/portal/portal-mobile-nav.tsx',
  'components/portal/portal-topbar.tsx',
  'app/(auth)/login/page.tsx',
  'app/portal/(auth)',
  'app/api/portal/auth',
];
```
Run `pnpm i18n:check` → must print clean (any hit = an unmigrated literal — fix it, don't remove the path).

- [ ] **Step 2: Full verification suite**

```bash
pnpm run check && pnpm test && pnpm i18n:check && pnpm build
```

- [ ] **Step 3: Manual QA matrix** — for EACH of AR and EN × light/dark × desktop/mobile-width:
  - Sidebar (groups, badges, pinned, collapse), topbar (switcher, theme, bell, user menu, role label), palette (search both languages, recents), breadcrumbs on a deep CRM route, dashboard login round-trip, portal login→forgot→reset, portal shell, a DataTable page with selection + pagination.
  - **AR-no-cookie session must be pixel-identical to pre-Phase-1** on: dashboard home, boards board view (drag a task — locked invariants), invoices list, portal home.

- [ ] **Step 4: Document in CLAUDE.md**

Append this section after "## Status Constants":

```markdown
## i18n — Bilingual AR/EN (Phases 0–1 shipped)

next-intl WITHOUT locale routing — URLs never change. Locale = `pyra_locale`
cookie (cache) ← `pyra_users.preferred_language` / `pyra_clients.preferred_language`
(source of truth, migration 035). `<html lang dir>` + Toaster are dynamic in
`app/layout.tsx`. Switchers: topbar `LocaleSwitcher`, profile `LocaleSelect`,
pre-auth `LocaleToggleAnon`; `LocaleSync` heals cookie↔DB drift in both authed
layouts.

**Rules (do NOT regress):**
- Messages live in `messages/{ar,en}/<namespace>.json` — ONE top-level
  namespace per file. Loader `lib/i18n/messages.ts` deep-merges EN OVER AR:
  a missing EN key renders Arabic — never a raw key. AR strings are extracted
  VERBATIM from code, never re-authored.
- Client components: `useTranslations`; server pages + route handlers:
  `getTranslations` (cookie-resolved — pass an explicit locale only for
  recipient-language rendering or cron/webhook contexts).
- Status labels: new/migrated modules use `useStatusLabels(entity)` from
  `lib/i18n/status-labels.ts` — the legacy `*_STATUS_LABELS` Arabic maps remain
  ONLY for not-yet-migrated modules.
- Nav lives in `components/layout/nav-config.ts` + `messages/*/nav.json`
  (sidebar, mobile-nav, palette, breadcrumb all consume it — never re-inline
  a label list).
- `pnpm i18n:check` gates hardcoded Arabic in migrated paths (manifest inside
  `scripts/i18n-check.ts` — append every newly migrated path). Escape hatch:
  `// i18n-exempt: <reason>`.
- Adding a translated module (Phases 2+): create `messages/{ar,en}/<mod>.json`,
  register in `NAMESPACE_FILES` + `i18n/global.ts`, swap the module's UI +
  API strings, extend MIGRATED_PATHS, QA both locales.
- Dates: `formatDate/formatRelativeDate/formatTime/formatTaskDueDate` take a
  trailing `locale` param (default `'ar'`). Currency/number formatting is
  locale-stable (`en-AE`) by design — do not localize digits in money.
```

- [ ] **Step 5: Commit + push + production smoke**

```bash
git add scripts/i18n-check.ts CLAUDE.md
git commit -m "feat(i18n): Phase 1 closure — regression manifest, docs (Phase 1.9)"
git push
```
After Coolify deploys: production round-trip AR⇄EN with a real account (switch back to AR when done), portal client check, then notify Abdou that Phase 0+1 are live.

---

## Execution notes

**Documented deviations from the spec (discovery-driven — intentional):**
1. **No `useAppLocale` hook** (spec §7 named one): the `AppConfig` type augmentation makes next-intl's own `useLocale()` return the typed `'ar'|'en'` union — a wrapper would be a pass-through. Use `useLocale()` directly.
2. **No login-time 2FA surface exists to translate** (spec Phase 1 listed "login/2FA"): discovery confirmed there is no OTP-entry step during login anywhere; the only 2FA UI is `components/auth/two-factor-setup.tsx`, which is imported NOWHERE (dormant). It is skipped — translating dead code violates YAGNI. It gets translated if/when 2FA-enforce ships (Phase 14.3 backlog).
3. **Portal shell chrome pulled INTO Phase 1** (spec put all portal work in Phase 7): the Phase-0 switcher ships on the portal topbar — an EN-preferring client would otherwise stare at all-Arabic chrome for months. The shell is 13 nav labels + ~10 topbar strings; portal PAGES remain Phase 7.
4. **Dashboard login cookie is healed by LocaleSync, not set at login** — the login page authenticates browser-side via Supabase (never calls `/api/auth/login`), so there is no server response to attach the cookie to. Portal login (a real API) does set it server-side.

- Tasks 1→10 are strictly ordered. Tasks 11–12 can run in either order; 13–18 depend on 12 (13 also feeds 15); 19 is last.
- Any deviation discovered mid-task (a string the discovery missed, a component shape that differs): follow the Global Constraints rule (add keys to BOTH catalogs), note it in the task's commit body.
- Phase 2+ (module-by-module) gets its own plan once Phase 1 ships — same recipe: catalog file → UI swap → API swap → manifest → QA.


