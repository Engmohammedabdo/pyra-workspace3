# 🗺️ Pyra Workspace 3 — خطة التطوير المرحلية

---

## 🔥 المرحلة 1: أولوية عالية (البنية جاهزة — ناقص الواجهة)
> **الجهد المتوقع:** 8-10 ساعات
> **الهدف:** إكمال الميزات اللي بنيتها التحتية جاهزة بس مفيش UI

---

### 1.1 — File Versioning UI (تاريخ نسخ الملفات)
**الحالة:** الجدول `pyra_file_versions` موجود + الـ Storage بيحفظ نسخ في `.versions/` — بس مفيش واجهة
**الجهد:** 3-4 ساعات

**الخطوات:**
1. **API Routes:**
   - `GET /api/files/versions?path=` — جلب كل نسخ ملف معين
   - `POST /api/files/versions/[id]/restore` — استعادة نسخة قديمة
   - `DELETE /api/files/versions/[id]` — حذف نسخة معينة

2. **Frontend — VersionHistory Component:**
   - كومبوننت `components/files/version-history.tsx`
   - يظهر كـ Sheet/Drawer جانبي عند الضغط على "تاريخ النسخ" في file preview
   - كل نسخة تعرض: رقم النسخة، الحجم، المنشئ، التاريخ، تعليق
   - أزرار: استعادة، تحميل، حذف

3. **تكامل مع File Preview:**
   - زر "📋 تاريخ النسخ" في `file-preview.tsx`
   - Badge تعرض عدد النسخ المتاحة

4. **Auto-versioning على Upload:**
   - عند رفع ملف بنفس الاسم → تلقائياً يحفظ النسخة القديمة في `pyra_file_versions`

**الملفات المتأثرة:**
- `app/api/files/versions/route.ts` (جديد)
- `app/api/files/versions/[id]/route.ts` (جديد)
- `components/files/version-history.tsx` (جديد)
- `components/files/file-preview.tsx` (تعديل)

---

### 1.2 — Favorites UI (واجهة المفضلة)
**الحالة:** API جاهز `GET/POST/DELETE /api/favorites` + جدول `pyra_favorites` — بس مفيش UI
**الجهد:** 2 ساعات

**الخطوات:**
1. **زر ⭐ في File Manager:**
   - إضافة زر نجمة بجانب كل ملف/مجلد في `app/dashboard/files/`
   - Toggle: نجمة فارغة ↔ نجمة مملوءة (صفراء)
   - يستدعي `POST /api/favorites` (toggle)

2. **صفحة المفضلة:**
   - `app/dashboard/favorites/page.tsx` — صفحة تعرض كل الملفات المفضلة
   - Grid/List view مع إمكانية فتح الملف أو إزالته من المفضلة
   - Empty state مناسب

3. **Sidebar:**
   - إضافة "⭐ المفضلة" في مجموعة "إدارة الملفات" في الـ sidebar

4. **Quick Access في Dashboard:**
   - عرض آخر 5 ملفات مفضلة في الداشبورد الرئيسي

**الملفات المتأثرة:**
- `app/dashboard/files/` (تعديل — زر النجمة)
- `app/dashboard/favorites/page.tsx` (جديد)
- `components/layout/sidebar.tsx` (تعديل)
- `app/dashboard/page.tsx` (تعديل — quick access)

---

### 1.3 — Dashboard محسّن (عرض كل الإحصائيات)
**الحالة:** API بيرجع 13 إحصائية بس الصفحة بتعرض 6 بس
**الجهد:** 1-2 ساعة

**الخطوات:**
1. **إعادة تصميم بطاقات الإحصائيات:**
   - صف أول: الملفات (عدد + مساحة)، المشاريع (نشط + مكتمل)، العملاء
   - صف تاني: عروض الأسعار (إجمالي + موقّعة)، الموافقات المعلقة، السلة
   - كل بطاقة قابلة للنقر (موجود بالفعل)

2. **Storage Breakdown:**
   - شريط تقدم يعرض المساحة المستخدمة
   - تقسيم: صور / فيديو / PDF / مستندات (من `v_storage_summary`)

3. **Quick Actions:**
   - أزرار سريعة: "رفع ملف"، "مشروع جديد"، "عرض سعر جديد"

4. **Recent Activity محسّن:**
   - أيقونات ملونة حسب نوع النشاط
   - "عرض الكل" يودي لصفحة سجل النشاط

**الملفات المتأثرة:**
- `app/dashboard/page.tsx` (تعديل كبير)

---

### 1.4 — Projects List محسّن
**الحالة:** `v_project_summary` بيرجع `file_count`, `comment_count`, `approved_count` بس الصفحة مش بتعرضهم
**الجهد:** 1 ساعة

**الخطوات:**
1. **بطاقات المشروع:**
   - إضافة badges: 📁 عدد الملفات، 💬 عدد التعليقات، ✅ موافقات
   - شريط تقدم الموافقات (approved/total)

2. **Filter بالحالة:**
   - أزرار فلتر سريعة: الكل، نشط، قيد المراجعة، مكتمل

3. **عرض التعليقات غير المقروءة:**
   - Badge أحمر لو فيه `unread_team_comments > 0`

**الملفات المتأثرة:**
- `app/dashboard/projects/page.tsx` (تعديل)

---
---

## 🟡 المرحلة 2: أولوية متوسطة (تحسينات مهمة)
> **الجهد المتوقع:** 12-15 ساعة
> **الهدف:** تحسينات تخلي التجربة أكثر احترافية واكتمالاً

---

### 2.1 — Dark/Light Theme Toggle
**الجهد:** 1-2 ساعة

**الخطوات:**
1. **ThemeProvider:**
   - التأكد من إن `next-themes` مثبت ومفعل
   - تفعيل ThemeProvider في `app/layout.tsx`

2. **زر التبديل:**
   - إضافة 🌙/☀️ toggle في الـ top bar (header)
   - حفظ التفضيل في localStorage

3. **تصحيح الألوان:**
   - مراجعة كل الصفحات للتأكد إن الألوان شغالة في الـ dark mode
   - تصحيح أي عناصر مش واضحة في الـ light mode

**الملفات المتأثرة:**
- `app/layout.tsx` (تعديل)
- `components/layout/header.tsx` أو `sidebar.tsx` (تعديل)

---

### 2.2 — DOCX Preview (معاينة Word)
**الجهد:** 2 ساعات

**الخطوات:**
1. **تثبيت mammoth.js:**
   - `npm install mammoth`

2. **كومبوننت DocxViewer:**
   - `components/files/docx-viewer.tsx`
   - يحمّل الملف → يحوله لـ HTML → يعرضه بتنسيق جميل

3. **تكامل مع File Preview:**
   - إضافة case لـ `.docx` و `.doc` في `file-preview.tsx`
   - Fallback لتحميل الملف لو الـ preview فشل

**الملفات المتأثرة:**
- `package.json` (إضافة mammoth)
- `components/files/docx-viewer.tsx` (جديد)
- `components/files/file-preview.tsx` (تعديل)

---

### 2.3 — Batch Download (تحميل ZIP)
**الجهد:** 3 ساعات

**الخطوات:**
1. **API Endpoint:**
   - `POST /api/files/download-batch` — يستقبل قائمة مسارات → يرجع ZIP

2. **Backend:**
   - استخدام `archiver` أو `jszip` لإنشاء ملف ZIP
   - تجميع الملفات من Storage → ضغطهم → إرجاع كـ stream

3. **Frontend:**
   - Checkbox لتحديد ملفات متعددة في file manager
   - زر "تحميل المحدد" يظهر لما يتم تحديد 2+ ملف
   - عداد "تم تحديد X ملف"

**الملفات المتأثرة:**
- `package.json` (إضافة archiver/jszip)
- `app/api/files/download-batch/route.ts` (جديد)
- `app/dashboard/files/` (تعديل)

---

### 2.4 — Activity Log Export (تصدير CSV)
**الجهد:** 1 ساعة

**الخطوات:**
1. **API Endpoint:**
   - `GET /api/activity/export?format=csv` — يرجع CSV file

2. **Frontend:**
   - زر "📥 تصدير CSV" في صفحة سجل النشاط
   - فلتر بالتاريخ قبل التصدير (من — إلى)

**الملفات المتأثرة:**
- `app/api/activity/export/route.ts` (جديد)
- `app/dashboard/activity/page.tsx` (تعديل)

---

### 2.5 — Dashboard Charts (رسوم بيانية)
**الجهد:** 3 ساعات

**الخطوات:**
1. **تثبيت مكتبة الرسوم:**
   - `npm install recharts` (خفيفة ومتوافقة مع React)

2. **Charts المطلوبة:**
   - **Storage Breakdown:** Pie chart (صور، فيديو، PDF، مستندات)
   - **Activity Trend:** Line chart (نشاط آخر 7 أيام)
   - **Project Status:** Bar chart (نشط، مراجعة، مكتمل)

3. **API Enhancement:**
   - `GET /api/dashboard/charts` — بيانات مخصصة للرسوم
   - Activity by day (آخر 7 أيام)
   - Storage by type

**الملفات المتأثرة:**
- `package.json` (إضافة recharts)
- `app/api/dashboard/charts/route.ts` (جديد)
- `app/dashboard/page.tsx` (تعديل)
- `components/dashboard/charts.tsx` (جديد)

---

### 2.6 — Client Portal تحسينات
**الجهد:** 3 ساعات

**الخطوات:**
1. **إشعارات محسّنة:**
   - عداد الإشعارات غير المقروءة في الـ header
   - تحديث تلقائي كل 30 ثانية (polling)

2. **ملفات جديدة Badge:**
   - علامة "جديد" على الملفات اللي اترفعت من آخر زيارة

3. **تحسين تجربة الموافقة:**
   - نموذج أوضح للموافقة/طلب تعديل
   - إمكانية إرفاق تعليق مع الموافقة

**الملفات المتأثرة:**
- `app/portal/` (عدة ملفات)

---
---

## 🟢 المرحلة 3: أفكار جديدة (ميزات متقدمة)
> **الجهد المتوقع:** 20-30 ساعة
> **الهدف:** ميزات تفرّق النظام وتخليه منافس حقيقي

---

### 3.1 — File Tagging (وسوم الملفات)
**الجهد:** 3 ساعات

**الخطوات:**
1. **Database:**
   - جدول `pyra_file_tags` (id, file_path, tag_name, color, created_by)
   - أو JSONB column في `pyra_file_index`

2. **API:**
   - `GET/POST/DELETE /api/files/tags`
   - إضافة/إزالة tags من ملف

3. **Frontend:**
   - Popover لإضافة tags عند right-click أو في file details
   - فلتر الملفات حسب الـ tag
   - ألوان مخصصة لكل tag

**الملفات المتأثرة:**
- `database/` (migration جديد)
- `app/api/files/tags/route.ts` (جديد)
- `app/dashboard/files/` (تعديل)

---

### 3.2 — Project Kanban Board
**الجهد:** 5+ ساعات

**الخطوات:**
1. **تثبيت مكتبة Drag & Drop:**
   - `npm install @dnd-kit/core @dnd-kit/sortable`

2. **Kanban Component:**
   - أعمدة: Draft → Active → In Progress → Review → Completed
   - سحب وإفلات المشروع بين الأعمدة
   - يحدّث الحالة تلقائياً عبر API

3. **Toggle View:**
   - زر تبديل بين List View و Kanban View في صفحة المشاريع

**الملفات المتأثرة:**
- `package.json` (إضافة dnd-kit)
- `components/projects/kanban-board.tsx` (جديد)
- `app/dashboard/projects/page.tsx` (تعديل)

---

### 3.3 — Email Notifications
**الجهد:** 3 ساعات

**الخطوات:**
1. **Email Service Setup:**
   - Resend أو Nodemailer + SMTP
   - قوالب HTML للإيميلات (عربي + إنجليزي)

2. **Trigger Points:**
   - تعليق جديد من عميل → إيميل للفريق
   - موافقة/طلب تعديل → إيميل للعميل
   - ملف جديد اترفع → إيميل للعميل
   - عرض سعر جديد → إيميل للعميل

3. **Settings:**
   - إعداد في صفحة الإعدادات لتفعيل/تعطيل الإيميلات
   - تخصيص عنوان المرسل

**الملفات المتأثرة:**
- `package.json` (إضافة resend/nodemailer)
- `lib/email/` (مجلد جديد)
- `app/api/` (تعديل عدة endpoints)
- `app/dashboard/settings/` (تعديل)

---

### 3.4 — Real-time Updates (تحديث لحظي)
**الجهد:** 4 ساعات

**الخطوات:**
1. **Supabase Realtime:**
   - الاشتراك في تغييرات `pyra_notifications`
   - الاشتراك في تغييرات `pyra_client_comments`

2. **Frontend Hook:**
   - `hooks/useRealtimeNotifications.ts`
   - يعرض toast عند وصول إشعار جديد
   - يحدّث العداد تلقائياً

3. **Live Comments:**
   - التعليقات تظهر فوراً بدون refresh
   - مؤشر "يكتب..." عند كتابة الطرف الآخر

**الملفات المتأثرة:**
- `hooks/useRealtimeNotifications.ts` (جديد)
- `hooks/useRealtimeComments.ts` (جديد)
- `components/layout/header.tsx` (تعديل)
- `app/portal/` (تعديل)

---

### 3.5 — User Profile Page
**الجهد:** 2 ساعات

**الخطوات:**
1. **صفحة البروفايل:**
   - `app/dashboard/profile/page.tsx`
   - عرض: الاسم، الدور، تاريخ الإنشاء
   - تغيير كلمة المرور
   - إحصائيات شخصية (من `v_user_activity_summary`)

2. **Avatar Upload:**
   - رفع صورة شخصية
   - عرضها في الـ sidebar و header

**الملفات المتأثرة:**
- `app/dashboard/profile/page.tsx` (جديد)
- `app/api/users/profile/route.ts` (جديد)

---

### 3.6 — Two-Factor Auth (2FA)
**الجهد:** 5+ ساعات

**الخطوات:**
1. **TOTP Setup:**
   - مكتبة `otplib` لإنشاء أكواد TOTP
   - QR code للمسح من Google Authenticator

2. **Database:**
   - حقل `totp_secret` في `pyra_users`
   - حقل `is_2fa_enabled` boolean

3. **Login Flow:**
   - بعد إدخال كلمة المرور الصحيحة → صفحة إدخال كود 2FA
   - خيار "تذكرني 30 يوم" على هذا الجهاز

**الملفات المتأثرة:**
- `package.json` (إضافة otplib, qrcode)
- `database/` (migration)
- `app/api/auth/` (تعديل)
- `app/dashboard/profile/` (تعديل — إعداد 2FA)

---

### 3.7 — PWA Support
**الجهد:** 2 ساعات

**الخطوات:**
1. **Manifest:**
   - `public/manifest.json` مع أيقونات وألوان
   - Service Worker أساسي

2. **Next.js Config:**
   - استخدام `next-pwa` أو `serwist`
   - Offline fallback page

3. **Install Prompt:**
   - زر "تثبيت التطبيق" في الـ header للموبايل

**الملفات المتأثرة:**
- `public/manifest.json` (جديد)
- `next.config.ts` (تعديل)
- `app/layout.tsx` (تعديل)

---

### 3.8 — Quote PDF Export
**الجهد:** 3 ساعات

**الخطوات:**
1. **PDF Template:**
   - تصميم قالب PDF لعرض السعر (شعار + بيانات + جدول + توقيع)
   - دعم العربي (RTL) في PDF

2. **API Endpoint:**
   - `GET /api/quotes/[id]/pdf` — يرجع PDF جاهز

3. **Frontend:**
   - زر "📄 تحميل PDF" في صفحة عرض السعر
   - زر "📧 إرسال PDF" للعميل

**الملفات المتأثرة:**
- `package.json` (إضافة @react-pdf/renderer أو puppeteer)
- `app/api/quotes/[id]/pdf/route.ts` (جديد)
- `app/dashboard/quotes/` (تعديل)

---
---

## 📅 الجدول الزمني المقترح

```
المرحلة 1 (🔥 عالية):     الأسبوع 1     ← 8-10 ساعات
المرحلة 2 (🟡 متوسطة):    الأسبوع 2-3   ← 12-15 ساعة
المرحلة 3 (🟢 جديدة):     الأسبوع 4-6   ← 20-30 ساعة
──────────────────────────────────────────
الإجمالي:                                  ← 40-55 ساعة
```

## 📊 ملخص سريع

| المرحلة | عدد الميزات | الجهد | الأثر |
|---------|-------------|-------|-------|
| 🔥 المرحلة 1 | 4 ميزات | 8-10 ساعات | عالي جداً (إكمال ناقص) |
| 🟡 المرحلة 2 | 6 ميزات | 12-15 ساعة | عالي (تحسين التجربة) |
| 🟢 المرحلة 3 | 8 ميزات | 20-30 ساعة | متوسط-عالي (ميزات تنافسية) |
